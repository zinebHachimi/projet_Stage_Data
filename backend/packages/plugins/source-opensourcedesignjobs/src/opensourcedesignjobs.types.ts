/**
 * Shape of a parsed RSS item from Open Source Design Jobs.
 * Design jobs for open source projects (UI/UX, branding, etc.).
 */
export interface OpensourcedesignjobsRssItem {
  title: string | null;
  link: string | null;
  guid: string | null;
  description: string | null;
  pubDate: string | null;
}
