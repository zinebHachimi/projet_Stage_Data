# Source Adoption Backlog

> Inbound queue of job-source platforms identified for adoption as Ever Jobs source
> plugins. Each entry becomes a `.specify/specs/<NNN>-source-<id>/` spec when we
> commit to building it.
>
> Do **not** add motivation or rationale here that references third-party tools.
> Adoption rationale (market share, public API stability, data quality) belongs
> in the per-spec `spec.md`.

## Status legend

- **proposed** — listed; no spec yet.
- **scoped** — a `spec.md` exists.
- **planned** — a `plan.md` and `tasks.md` exist.
- **in-progress** — code work has started.
- **shipped** — published and registered in `PluginRegistry`.

---

## Backlog

| Status     | Plugin id (planned)        | Category   | Platform / source                                   | Notes |
| ---------- | -------------------------- | ---------- | --------------------------------------------------- | ----- |
| shipped    | `source-ats-avature`       | `ats`      | **Avature** career-site platform.                   | Used by large enterprises. Shipped as `source-ats-avature`. |
| shipped    | `source-ats-gem`           | `ats`      | **Gem** ATS / recruiting CRM (`gem.com`).           | Shipped as `source-ats-gem`. |
| shipped    | `source-ats-joincom`       | `ats`      | **Join.com** ATS (`join.com/companies/<slug>/jobs`).| Public REST API. Shipped as `source-ats-joincom`. |
| shipped    | `source-ats-oracle`        | `ats`      | **Oracle HCM Cloud** Recruiting (`*.fa.<region>.oraclecloud.com`). | Distinct from `source-ats-taleo` (legacy Oracle). Shipped as `source-ats-oracle`. |
| shipped    | `source-ats-mercor`        | `niche`    | **Mercor** talent marketplace (`mercor.com`).       | Shipped as `source-ats-mercor`. |
| shipped    | (Lever company-source pipeline) | `company` | **Lever** company-direct pipeline (`jobs.lever.co/<slug>`, public Postings API). | Spec 1194 (run #442). Third backend sibling of the Greenhouse + Ashby company-source pipelines: `probe-lever` + `scaffold-lever` reusing the backend-agnostic `wire-company-source`. First batch: 180 plugins, Specs 1195–1374. |
| proposed   | `source-company-tesla`     | `company`  | **Tesla** careers site (reverse-engineered API).    | Single company-direct plugin. |
| shipped    | `source-ats-ceipal`        | `ats`      | **Ceipal** US staffing ATS.                         | Spec 319, run #404. Anonymous career-portal API `api.ceipal.com/{apiKey}/job-postings/`. Heuristic confidence (key rotation). |
| shipped    | `source-ats-softgarden`    | `ats`      | **Softgarden** German ATS.                          | Spec 320, run #404. Public schema.org `{tenantOrigin}/jobs.feed.json`. |
| shipped    | `source-ats-recruitis`     | `ats`      | **Recruitis** Czech ATS (`recruitis.io`).           | Spec 321, run #404. `cheerio` scrape of `jobs.recruitis.io/{tenant}`. |
| shipped    | `source-ats-flatchr`       | `ats`      | **Flatchr** French ATS (`flatchr.io`).              | Spec 322, run #404. Public `careers.flatchr.io/company/{slug}.json`. |
| shipped    | `source-ats-jobsoid`       | `ats`      | **Jobsoid** recruitment ATS (`jobsoid.com`).        | Spec 323, run #404. Public `{tenant}.jobsoid.com/api/v1/jobs`. |
| shipped    | `source-ats-skeeled`       | `ats`      | **Skeeled** Luxembourg ATS (`skeeled.com`).         | Spec 324, run #404. SSR Nuxt data island on `app.skeeled.com/board/{id}`. |
| shipped    | `source-ats-teamdash`      | `ats`      | **Teamdash** Estonian ATS (`teamdash.com`).         | Spec 325, run #404. SSR career page `window.context` JSON. |
| shipped    | `source-ats-digitalrecruiters` | `ats`  | **DigitalRecruiters** French ATS (`digitalrecruiters.com`). | Spec 326, run #404. Public `api.digitalrecruiters.com/public/v1/careers-site/job-ads`. |
| shipped    | `source-ats-concludis`     | `ats`      | **concludis** German e-recruiting ATS (`concludis.de`). | Spec 327, run #404. Server-rendered listing + JSON-LD detail. |
| shipped    | `source-ats-rexx`          | `ats`      | **rexx systems** German HR suite (`rexx-systems.com`). | Spec 328, run #404. Listing scrape + schema.org JSON-LD detail. |
| shipped    | `source-ats-pcrecruiter`   | `ats`      | **PCRecruiter** US staffing ATS (`pcrecruiter.net`). | Spec 329, run #404. Public job board + JSON-LD detail. |
| shipped    | `source-ats-prescreen`     | `ats`      | **Prescreen** Austrian ATS (`prescreen.io`/`onlyfy.jobs`). | Spec 330, run #404. SSR candidate portal + JSON-LD detail. |
| shipped    | `source-ats-jobdiva`       | `ats`      | **JobDiva** US staffing ATS (`jobdiva.com`).        | Spec 339, run #406. Public candidate-portal XML feeds `getportaljobs.jsp?a={portalId}` (+ `listofportaljobs.jsp` fallback); cheerio XML parse. |
| shipped    | `source-ats-easycruit`     | `ats`      | **EasyCruit** Nordic ATS (Visma, `easycruit.com`).  | Spec 340, run #406. Public per-tenant vacancy-list XML `https://{tenant}.easycruit.com/export/xml/vacancy/list.xml` (`urn:EasyCruit`). |
| shipped    | `source-ats-varbi`         | `ats`      | **Varbi** Swedish recruitment ATS (`varbi.com`).    | Spec 341, run #406. Public tenant career page `https://{tenant}.varbi.com/en/` listing + per-role job-ad enrichment. |
| shipped    | `source-ats-talentsoft`    | `ats`      | **Talentsoft** French/EU ATS (Cegid, `talentsoft.com`). | Spec 342, run #406. Public RSS offer export `/handlers/offerRss.ashx?LCID={lcid}` on `{tenant}-recrute.talent-soft.com`. |
| shipped    | `source-ats-beetween`      | `ats`      | **Beetween** French ATS (`beetween.com`).           | Spec 343, run #406. Public per-tenant jobs surface → JobPostDto. |
| shipped    | `source-ats-applicantpro`  | `ats`      | **ApplicantPro** US SMB ATS (`applicantpro.com`).   | Spec 344, run #406. Public tenant job board `https://{tenant}.applicantpro.com/jobs/` + schema.org JobPosting. |
| shipped    | `source-ats-darwinbox`     | `ats`      | **Darwinbox** Asian HR suite (`darwinbox.com`).     | Spec 345, run #406. Public candidate careers surface. |
| shipped    | `source-ats-talentreef`    | `ats`      | **TalentReef** US hourly-hiring ATS (Mitratech, `talentreef.com`). | Spec 346, run #406. Public job boards + schema.org JobPosting. |
| shipped    | `source-ats-applicantstack`| `ats`      | **ApplicantStack** US SMB ATS (SwipeClock/WorkforceHub). | Spec 347, run #407. Public `{tenant}.applicantstack.com/x/openings` listing + `/x/detail/{jobId}` enrichment. Verified live. |
| shipped    | `source-ats-paycor`        | `ats`      | **Paycor Recruiting** US ATS (formerly Newton Software, `paycor.com`). | Spec 348, run #407. Public clientId-addressed career portal (`/career/CareerHome.action` + `/career/JobIntroduction.action`). Verified live. |
| shipped    | `source-ats-arcoro`        | `ats`      | **Arcoro** US construction/blue-collar ATS (formerly BirdDogHR, `arcoro.com`). | Spec 349, run #407. Public `/job/{id}` schema.org JSON-LD detail on `jobs.ourcareerpages.com` / `{tenant}.birddoghr.com`. Verified live. |
| shipped    | `source-ats-reachmee`      | `ats`      | **ReachMee** Nordic ATS (Talentech, `reachmee.com`). | Spec 350, run #407. Public per-installation RSS export on `site{NNN}.reachmee.com/Public/rssfeed/external.ashx`. Verified live. |
| shipped    | `source-ats-jobtrain`      | `ats`      | **Jobtrain** UK ATS (`jobtrain.co.uk`).             | Spec 351, run #407. Per-tenant `_JobCard` HTML partial enumeration + per-detail schema.org JSON-LD. Verified live. |
| shipped    | `source-ats-avionte`       | `ats`      | **Avionté** US staffing ATS (AviontéBOLD, `avionte.com`). | Spec 352, run #407. Public per-build RSS/XML feed. Surface documented from public patterns; **flagged for live re-confirmation**. |
| shipped    | `source-ats-exacthire`     | `ats`      | **ExactHire** US SMB ATS (HireCentric, `exacthire.com`). | Spec 353, run #407. Public `{tenant}.hirecentric.com` XML sitemap → `/jobs/{id}.html` schema.org JSON-LD. **Flagged for live re-confirmation**. |
| shipped    | `source-ats-hireful`       | `ats`      | **Hireful** UK ATS (LiveVacancies, `hireful.com`).  | Spec 354, run #407. Public `{tenant}.livevacancies.co.uk` XML sitemap → per-vacancy schema.org JSON-LD. **Flagged for live re-confirmation**. |
| shipped    | `source-ats-hirehive`      | `ats`      | **Hirehive** IE/EU SMB ATS (`hirehive.com`).        | Spec 395, run #412. Public anonymous CareerSite JSON feed `GET https://{tenant}.hirehive.com/api/v2/jobs?source=CareerSite`. Verified live. |
| shipped    | `source-ats-eddy`          | `ats`      | **Eddy** US SMB HRIS (`eddy.com`).                  | Spec 396, run #412. Public anonymous JSON API on `app.eddy.com` keyed by org UUID (`/api/ats/public/job-opening/organization/{uuid}` + per-role detail). Verified live. |
| shipped    | `source-ats-peoplestrong`  | `ats`      | **PeopleStrong** India/APAC enterprise HCM (`peoplestrong.com`). | Spec 397, run #412. Public candidate portal `{tenant}.peoplestrong.com`; tenant-scoped JSON board probe + JSON-LD fallback; canonical `/job/detail/{id}`. Host/URL verified live, JSON payload documented-but-unverified. |
| shipped    | `source-ats-zimyo`         | `ats`      | **Zimyo** India HRMS (`zimyo.com`).                 | Spec 398, run #412. Public anonymous widget API `ats.zimyo.work/ats/ats/widget/joblist2?id={orgId}` + per-role `jobDetails`. Verified live. |
| shipped    | `source-ats-greythr`       | `ats`      | **GreytHR** India SMB HR (Greytip, `greythr.com`).  | Spec 399, run #412. Public anonymous `POST https://{tenant}.greythr.com/hire/api/career/published_jobs/`. Verified live (tenants `greytip`, `fint`). |
| shipped    | `source-ats-recruitly`     | `ats`      | **Recruitly** UK recruitment CRM (`recruitly.io`).  | Spec 400, run #412. Public anonymous job feed `GET https://api.recruitly.io/api/job?apiKey={apiKey}`; tenant keyed by public board API key. Verified live. |
| shipped    | `source-ats-sagepeople`    | `ats`      | **Sage People** UK/EU enterprise HCM (Fairsail, Force.com, `sage.com/people`). | Spec 401, run #412. Public Visualforce/Experience-Cloud career sites. DISTINCT from `source-ats-sagehr`. **Flagged for live re-confirmation**. |
| shipped    | `source-ats-cezanne`       | `ats`      | **Cezanne HR** UK/EU mid-market HR (`cezannehr.com`). | Spec 402, run #412. Public per-tenant vacancy board / feed. **Flagged for live re-confirmation**. |
| shipped    | `source-ats-workforce`     | `ats`      | **Workforce.com** US/AU/UK hourly hiring (`workforce.com`). | Spec 403, run #412. Public `/ats/apply/job/{uuid}` board surface. DISTINCT from `source-ats-workstream`. **Flagged for live re-confirmation**. |
| shipped    | `source-ats-hrpartner`     | `ats`      | **HR Partner** AU/global SMB HR (`hrpartner.io`).   | Spec 404, run #412. Public per-tenant careers/recruitment portal. **Flagged for live re-confirmation**. |
| shipped    | `source-company-anthropic` | `company`  | **Anthropic** (`anthropic.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/anthropic/jobs`). | Spec 020, run #230. Thin wrapper following the `source-company-stripe` pattern. |
| shipped    | `source-company-databricks`| `company`  | **Databricks** (`databricks.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/databricks/jobs`). | Spec 021, run #231. Thin wrapper following the `source-company-anthropic` pattern. |
| shipped    | `source-company-discord`   | `company`  | **Discord** (`discord.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/discord/jobs`). | Spec 022, run #232. Thin wrapper following the `source-company-databricks` pattern. |
| shipped    | `source-company-coinbase`  | `company`  | **Coinbase** (`coinbase.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/coinbase/jobs`). | Spec 023, run #233. Thin wrapper following the `source-company-discord` pattern. |
| shipped    | `source-company-doordash`  | `company`  | **DoorDash** (`doordash.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/doordash/jobs`). | Spec 024, run #234. Thin wrapper following the `source-company-coinbase` pattern. |
| shipped    | `source-company-airbnb`    | `company`  | **Airbnb** (`airbnb.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/airbnb/jobs`). | Spec 025, run #235. Thin wrapper following the `source-company-doordash` pattern. |
| shipped    | `source-company-robinhood` | `company`  | **Robinhood** (`robinhood.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/robinhoodjobs/jobs` — note: tenant slug is `robinhoodjobs`, not the bare `robinhood`). | Spec 026, run #236. Thin wrapper following the `source-company-airbnb` pattern. |
| shipped    | `source-company-reddit`    | `company`  | **Reddit** (`reddit.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/reddit/jobs`). | Spec 027, run #237. Thin wrapper following the `source-company-robinhood` pattern. |
| shipped    | `source-company-pinterest` | `company`  | **Pinterest** (`pinterest.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/pinterest/jobs`). | Spec 028, run #238. Thin wrapper following the `source-company-reddit` pattern. |
| shipped    | `source-company-lyft`      | `company`  | **Lyft** (`lyft.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/lyft/jobs`). | Spec 029, run #239. Thin wrapper following the `source-company-pinterest` pattern. |
| shipped    | `source-company-plaid`     | `company`  | **Plaid** (`plaid.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/plaid/jobs`). | Spec 030, run #240. Thin wrapper following the `source-company-lyft` pattern. |
| shipped    | `source-company-asana`     | `company`  | **Asana** (`asana.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/asana/jobs`). | Spec 031, run #241. Thin wrapper following the `source-company-plaid` pattern. |
| shipped    | `source-company-figma`     | `company`  | **Figma** (`figma.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/figma/jobs`). | Spec 032, run #242. Thin wrapper following the `source-company-asana` pattern. |
| shipped    | `source-company-gitlab`    | `company`  | **Gitlab** (`about.gitlab.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/gitlab/jobs`). | Spec 033, run #243. Thin wrapper following the `source-company-figma` pattern. |
| shipped    | `source-company-twitch`    | `company`  | **Twitch** (`twitch.tv`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/twitch/jobs`). | Spec 034, run #244. Thin wrapper following the `source-company-gitlab` pattern. |
| shipped    | `source-company-twilio`    | `company`  | **Twilio** (`twilio.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/twilio/jobs`). | Spec 035, run #245. Thin wrapper following the `source-company-twitch` pattern. |
| shipped    | `source-company-cloudflare`| `company`  | **Cloudflare** (`cloudflare.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/cloudflare/jobs`). | Spec 036, run #246. Thin wrapper following the `source-company-twilio` pattern. |
| shipped    | `source-company-mongodb`   | `company`  | **MongoDB** (`mongodb.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/mongodb/jobs`). | Spec 037, run #247. Thin wrapper following the `source-company-cloudflare` pattern. |
| shipped    | `source-company-datadog`   | `company`  | **Datadog** (`datadoghq.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/datadog/jobs`). | Spec 038, run #248. Thin wrapper following the `source-company-mongodb` pattern. |
| shipped    | `source-company-instacart` | `company`  | **Instacart** (`instacart.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/instacart/jobs`). | Spec 039, run #249. Thin wrapper following the `source-company-datadog` pattern. |
| shipped    | `source-company-dropbox`   | `company`  | **Dropbox** (`dropbox.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/dropbox/jobs`). | Spec 040, run #250. Thin wrapper following the `source-company-instacart` pattern. |
| shipped    | `source-company-roblox`    | `company`  | **Roblox** (`roblox.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/roblox/jobs`). | Spec 041, run #251. Thin wrapper following the `source-company-dropbox` pattern. |
| shipped    | `source-company-block`     | `company`  | **Block** (`block.xyz`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/block/jobs`). | Spec 042, run #252. Thin wrapper following the `source-company-roblox` pattern. |
| shipped    | `source-company-vercel`    | `company`  | **Vercel** (`vercel.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/vercel/jobs`). | Spec 043, run #253. Thin wrapper following the `source-company-block` pattern. |
| shipped    | `source-company-affirm`    | `company`  | **Affirm** (`affirm.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/affirm/jobs`). | Spec 044, run #254. Thin wrapper following the `source-company-vercel` pattern (second cohort member to use Greenhouse's new `job-boards.greenhouse.io` permalink subdomain). |
| shipped    | `source-company-klaviyo`   | `company`  | **Klaviyo** (`klaviyo.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/klaviyo/jobs`). | Spec 045, run #255. Thin wrapper following the `source-company-affirm` pattern with two D-04 / D-08 deviations: (a) `absolute_url` is a marketing-site proxy `klaviyo.com/careers/jobs?gh_jid=<id>` rather than a Greenhouse permalink subdomain (first cohort member), and (b) the description-cleanup pipeline runs `decodeHtmlEntities` before `stripHtmlTags` because this tenant emits entity-encoded content. |
| shipped    | `source-company-duolingo`  | `company`  | **Duolingo** (`duolingo.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/duolingo/jobs`). | Spec 046, run #256. Thin wrapper following the `source-company-klaviyo` pattern with one D-04 deviation: `absolute_url` is a marketing-site careers-subdomain proxy `careers.duolingo.com/jobs/<id>?gh_jid=<id>` (fourth distinct wire-shape variant — careers subdomain, path-AND-query). The D-08 entity-decode-then-tag-strip description pipeline is identical to Klaviyo's because Duolingo's `content` is also entity-encoded. |
| shipped    | `source-company-brex`      | `company`  | **Brex** (`brex.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/brex/jobs`). | Spec 047, run #257. Thin wrapper following the `source-company-duolingo` pattern with two D-04 / D-09 deviations: (a) `absolute_url` is an apex-www marketing-site proxy `www.brex.com/careers/<id>?gh_jid=<id>` (fifth distinct wire-shape variant — apex-www domain, path-AND-query), and (b) the wire `title` is `.trim()`ed before mapping (first cohort member to apply a wire-title trim — Brex's tenant pads some titles with surrounding ASCII spaces). The D-08 entity-decode-then-tag-strip description pipeline is identical to Klaviyo's and Duolingo's because Brex's `content` is also entity-encoded. |
| shipped    | `source-company-gusto`     | `company`  | **Gusto** (`gusto.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/gusto/jobs`). | Spec 048, run #258. Thin wrapper following the `source-company-affirm` pattern (third cohort member to use the new `job-boards.greenhouse.io` permalink subdomain — variant 2) with two D-08 / D-09 deviations: (a) the description-cleanup pipeline runs `decodeHtmlEntities` before `stripHtmlTags` because this tenant emits entity-encoded content (fourth cohort member after Klaviyo / Duolingo / Brex; first to combine the entity-decode pipeline with the new `job-boards.greenhouse.io` permalink subdomain), and (b) the emitted `companyName` pins the cleaned brand name `'Gusto'` rather than the wire `company_name` value `'Gusto, Inc.'` — same approach Affirm uses for `'Affirm Holdings, Inc.'`. |
| shipped    | `source-company-mercury`   | `company`  | **Mercury** (`mercury.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/mercury/jobs`). | Spec 049, run #259. Thin wrapper following the `source-company-gusto` pattern (fourth cohort member to use the new `job-boards.greenhouse.io` permalink subdomain — variant 2; second cohort member to combine variant 2 with the entity-decode-then-tag-strip description pipeline after Gusto). Sole structural deviation from the Gusto template is in D-09: Mercury's wire `company_name` is the bare brand name `'Mercury'` (no legal-entity suffix), so the brand-name pin matches the wire byte-for-byte. Discovered via the post-Spec-048 named-candidate well: a probe sweep of Stripe-adjacent fintechs (Mercury, Modern Treasury, Ramp), vertical SaaS (Notion, Linear, Loom, Front), and e-commerce (Shopify) returned HTTP 200 on `mercury` and `rampnetwork` (a different tenant: Ramp Network — Web3 fiat-to-crypto onramp, not Ramp Inc.); Mercury picked as the alphabetically-first bite, Ramp Network queued for a future run with a sixth wire-shape variant (`job-boards.eu.greenhouse.io` EU-region permalink subdomain). |
| shipped    | `source-company-buildkite` | `company`  | **Buildkite** (`buildkite.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/buildkite/jobs`). | Spec 050, run #260. Thin wrapper following the `source-company-mercury` pattern (fifth cohort member to use the new `job-boards.greenhouse.io` permalink subdomain — variant 2; third cohort member to combine variant 2 with the entity-decode-then-tag-strip description pipeline after Gusto and Mercury; sixth overall to use the entity-decode pipeline). Sole structural deviation from the Mercury template is in D-10: a `.trim()` on the wire `title` to handle a subset of Buildkite roles padded with trailing ASCII spaces (e.g. `'Staff Engineer - Compute & Agents '`, `'Staff GTM Engineer '`, `'Technical Account Manager '`). This is the **second** plugin in the cohort to apply a wire-title trim (after Brex). Discovered via a fresh probe sweep of developer-tools / SaaS candidates that returned HTTP 200 on **nine** slugs: `buildkite`, `circleci`, `hubspot`, `netlify`, `postman`, `rampnetwork` (carry-over), `toast`, `webflow`, and `zoominfo`. Buildkite picked as the alphabetically-first bite; the eight remaining candidates queue up for runs #261+. |
| shipped    | `source-company-circleci`  | `company`  | **CircleCI** (`circleci.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/circleci/jobs`). | Spec 051, run #261. Thin wrapper following the `source-company-brex` pattern (apex-www marketing-site `absolute_url` shape — variant 5 family; seventh cohort member to use the entity-decode-then-tag-strip description pipeline). Sole structural deviation from the Brex template is in D-04: the wire `absolute_url` shape is `http://www.circleci.com/careers/jobs/<id>/?gh_jid=<id>` — a **seventh** distinct wire-shape variant in the cohort, distinguished by (a) the **HTTP scheme** (not HTTPS — first plugin in the catalogue to publish on the HTTP scheme), (b) the trailing slash before the query (`/<id>/?gh_jid=`), and (c) the path-with-`jobs`-segment (`/careers/jobs/<id>/` rather than Brex's `/careers/<id>`). Class names use the `OpenAIService` precedent for embedded-acronym brand casing — `CircleCIService` / `CircleCIModule` (D-06). Selected from the carry-over named-candidate pool from Spec 050's nine-200 probe sweep as the alphabetically-first remaining bite (`cir` < `hub` < `net` < `pos` < `ram` < `toa` < `web` < `zoo`); the seven remaining candidates queue up for runs #262+. |
| shipped    | `source-company-rampnetwork` | `company` | **Ramp Network** (`ramp.network`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/rampnetwork/jobs`). | Spec 052, run #262. Thin wrapper following the `source-company-buildkite` pattern (eighth cohort member to use the entity-decode-then-tag-strip description pipeline). Sole structural deviation from the Buildkite template is in D-04: the wire `absolute_url` shape is `https://job-boards.eu.greenhouse.io/rampnetwork/jobs/<id>` — a **sixth** distinct wire-shape variant in the cohort, structurally identical to variant 2 except for the `.eu` region prefix on the subdomain. Ramp Network is the **first plugin in the cohort to publish on the EU-region permalink subdomain** (every prior plugin uses the US-region subdomain or one of the marketing-site variants). Brand-name pin `'Ramp Network'` is the **first multi-word brand-name string literal containing an inter-word ASCII space** in the cohort (D-09). Selected from the carry-over named-candidate pool: HubSpot (alphabetically next at `hub`) was re-probed live and returned an empty `jobs[]` array (HTTP 200 with `meta.total === 0`) so was deferred; Ramp Network picked as the variant-6 EU-region pivot specifically queued for a future run in run #259's Spec 049 close-out. The six remaining queued candidates (HubSpot re-probe, Netlify, Postman, Toast, Webflow, ZoomInfo) plus a fresh probe sweep pivot queue up for runs #263+. |
| shipped    | `source-company-netlify`   | `company`  | **Netlify** (`netlify.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/netlify/jobs`). | Spec 053, run #263. Thin wrapper following the `source-company-buildkite` pattern (sixth cohort member to use variant 2 — the US-region `job-boards.greenhouse.io` permalink subdomain — after Vercel, Affirm, Gusto, Mercury, and Buildkite; ninth cohort member to use the entity-decode-then-tag-strip description pipeline). **Zero structural deviations** from the Buildkite template. The one new cohort-first observation (encoded as a regression guard but not a structural deviation) is the **literal-ampersand department names** (`R&D`, `G&A`) that the unit-test fixture exercises and the happy-path test pins byte-for-byte (D-11) — Netlify is the **first plugin in the cohort to ship a fixture with an ampersand-bearing department name**. Brand-name pin `'Netlify'` matches the wire `company_name` byte-for-byte (D-09). Selected from the carry-over named-candidate pool from Spec 050's nine-200 probe sweep as the alphabetically-next remaining live bite (`net` < `pos` < `toa` < `web` < `zoo`; HubSpot re-probe still returning empty board so deferred again). The five remaining candidates (HubSpot re-probe, Postman, Toast, Webflow, ZoomInfo) plus a fresh probe sweep pivot queue up for runs #264+. |
| shipped    | `source-company-postman`   | `company`  | **Postman** (`postman.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/postman/jobs`). | Spec 054, run #264. Thin wrapper following the `source-company-netlify` pattern (seventh cohort member to use variant 2 — the US-region `job-boards.greenhouse.io` permalink subdomain — after Vercel, Affirm, Gusto, Mercury, Buildkite, and Netlify; tenth cohort member to use the entity-decode-then-tag-strip description pipeline). **Zero structural deviations** from the Netlify template. The one new cohort-first observation (encoded as a regression guard but not a structural deviation) is the **`<div class="content-intro">` recruiter-blurb wrapper** that begins every Postman wire `content` payload (after entity decoding) and which the entity-decode-then-tag-strip pipeline neutralises into clean prose without a per-source content-intro filter (D-11). Brand-name pin `'Postman'` matches the wire `company_name` byte-for-byte (D-09). Selected from the carry-over named-candidate pool from Spec 050's nine-200 probe sweep as the alphabetically-next remaining live bite (`pos` < `toa` < `web` < `zoo`; HubSpot continues to be deferred — last re-probed at run-262 start with `meta.total === 0`). The four remaining candidates (HubSpot re-probe, Toast, Webflow, ZoomInfo) plus a fresh probe sweep pivot queue up for runs #265+. |
| shipped    | `source-company-webflow`   | `company`  | **Webflow** (`webflow.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/webflow/jobs`). | Spec 056, run #266. Thin wrapper following the `source-company-postman` pattern (eighth cohort member to use variant 2 — the US-region `job-boards.greenhouse.io` permalink subdomain — after Vercel, Affirm, Gusto, Mercury, Buildkite, Netlify, and Postman; twelfth cohort member to use the entity-decode-then-tag-strip description pipeline). **Zero structural deviations** from the Postman template. The one new cohort-first observation (encoded as a regression guard but not a structural deviation) is the **semicolon-separated multi-region location names with parenthesised province/state restrictions** (e.g. `'CA Remote (BC & ON only); U.K. / Ireland Remote; U.S. Remote'`) that the unit-test fixture exercises and the happy-path test pins byte-for-byte (D-11) — Webflow is the **first plugin in the cohort to ship a fixture with semicolon-separated multi-region location names**. Brand-name pin `'Webflow'` matches the wire `company_name` byte-for-byte (D-09). Selected from the carry-over named-candidate pool from Spec 050's nine-200 probe sweep as the alphabetically-next remaining live bite (`web` < `zoo`; HubSpot continues to be deferred — last re-probed at run-262 start with `meta.total === 0`). The two remaining candidates (HubSpot re-probe, ZoomInfo) plus a fresh probe sweep pivot queue up for runs #267+. |
| shipped    | `source-company-toast`     | `company`  | **Toast** (`toast.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/toast/jobs`). | Spec 055, run #265. Thin wrapper following the `source-company-postman` pattern (eleventh cohort member to use the entity-decode-then-tag-strip description pipeline). **One structural deviation** from the Postman template — a new D-04 wire-shape variant: the wire `absolute_url` shape is `https://careers.toasttab.com/jobs?gh_jid=<id>` — an **eighth** distinct wire-shape variant in the cohort, distinguished by (a) the **sub-brand product domain** (`toasttab.com`, the operating product domain) rather than the slug-name brand domain (`toast.com`) — first plugin in the cohort to use a sub-brand product domain — and (b) the careers-subdomain marketing-site shape with `?gh_jid=<id>` query-param-only listing identification (no `<id>` in the path). The new cohort-first observation (encoded as a regression guard but not a structural deviation) is the **colon-separated nested-path department names with spaced ampersands** (`'Sales : International : Horizon 2'`, `'R & D : Engineering : Retail'`, `'G & A : Finance GL Accounting'`) that the unit-test fixture exercises and the happy-path test pins byte-for-byte (D-11) — Toast is the **first plugin in the cohort to ship a fixture with colon-separated nested-path department names**. Brand-name pin `'Toast'` matches the wire `company_name` byte-for-byte (D-09). Selected from the carry-over named-candidate pool from Spec 050's nine-200 probe sweep as the alphabetically-next remaining live bite (`toa` < `web` < `zoo`; HubSpot continues to be deferred — last re-probed at run-262 start with `meta.total === 0`). The three remaining candidates (HubSpot re-probe, Webflow, ZoomInfo) plus a fresh probe sweep pivot queue up for runs #266+. |
| shipped    | `source-company-zoominfo`  | `company`  | **ZoomInfo** (`zoominfo.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/zoominfo/jobs`). | Spec 057, run #267. Thin wrapper following the `source-company-toast` pattern (thirteenth cohort member to use the entity-decode-then-tag-strip description pipeline). **Three structural deviations** from the Toast template: (a) D-04 wire-shape variant 9 — `https://www.zoominfo.com/careers?gh_jid=<id>` — a **ninth** distinct wire-shape variant in the cohort, distinguished from Toast's variant 8 by the **apex-www brand-domain** (`www.zoominfo.com`, the corporate brand domain on the default `www` subdomain) rather than the careers-subdomain on a sub-brand product domain (`careers.toasttab.com`), with the `/careers` path and `?gh_jid=<id>` query-param-only listing identification — first plugin in the cohort to use an apex-www brand-domain `?gh_jid=<id>` shape; (b) D-09 brand-name trim — wire `company_name` `'ZoomInfo Technologies LLC'` (the registered Delaware LLC name with space-separated `Technologies LLC` suffix) is trimmed to the brand `'ZoomInfo'` via string-literal pin — third cohort plugin to apply a brand-name trim (after Affirm `Spec 044 § 10 D-06` `'Affirm, Inc.'` → `'Affirm'` and Gusto `Spec 048 § 10 D-09` `'Gusto, Inc.'` → `'Gusto'`) and **first to clean a space-separated suffix** rather than a comma-separated one; (c) D-10 wire-title `.trim()` — 5 of 82 titles in the run-267 probe carry trailing ASCII-space padding (e.g. `'Account Manager, Enterprise Growth '`) that the plugin trims before downstream filters and emit — third cohort plugin to apply a wire-title trim (after Brex `Spec 047 § 10 D-10` and Buildkite `Spec 050 § 10 D-10`). The new cohort-first observation (encoded as a regression guard but not a structural deviation) is the **numeric-code-prefixed department names with hyphen-separated organisational hierarchy** (e.g. `'801 Client Services - Support'`, `'898 Corporate Engineering - G&A - Enterprise Technologies'`, `'820 R&G - Account Managers'`) that the unit-test fixture exercises and the happy-path test pins byte-for-byte (D-11) — ZoomInfo is the **first plugin in the cohort to ship a fixture with numeric-code-prefixed department names**. Selected from the carry-over named-candidate pool from Spec 050's nine-200 probe sweep as the **last remaining live candidate** (HubSpot continues to be deferred — last re-probed at run-267 start with `meta.total === 0`, the sixth consecutive empty re-probe across runs #262–#267). With this Spec 057 close-out, the carry-over named-candidate pool is fully exhausted; runs #268+ will pivot to a fresh probe sweep of the next batch of large-employer candidates. |
| shipped    | `source-company-attentive` | `company`  | **Attentive** (`attentive.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/attentive/jobs`). | Spec 058, run #268. Thin wrapper following the `source-company-webflow` pattern (ninth cohort member to use variant 2 — the US-region `job-boards.greenhouse.io` permalink subdomain — after Vercel, Affirm, Gusto, Mercury, Buildkite, Netlify, Postman, and Webflow; fourteenth cohort member to use the entity-decode-then-tag-strip description pipeline). **One structural deviation** from the Webflow template: D-10 wire-title `.trim()` — 4 of 59 titles in the run-268 probe carry trailing ASCII-space padding (e.g. `'Director of Engineering, Intelligent Messaging '`, `'Principal Technical Program Manager '`, `'Staff Software Engineer, Streaming '`, `'Support Engineer (West) '`) that the plugin trims before downstream filters and emit — fourth cohort plugin to apply a wire-title trim (after Brex `Spec 047 § 10 D-10`, Buildkite `Spec 050 § 10 D-10`, and ZoomInfo `Spec 057 § 10 D-10`). Brand-name pin `'Attentive'` matches the wire `company_name` byte-for-byte (D-09). Department pass-through preserves Attentive's flat single-token format (`'Finance'`, `'Engineering'`, `'Sales'`, `'Customer Success'`, `'Office of CSO'`, etc.) byte-for-byte (D-11) — distinct from ZoomInfo's numeric-code-prefix and Toast's colon-separated nested-path formats. Selected from the **fresh probe sweep** that runs #268+ pivoted to per Spec 057's run-267 close-out — eighteen large-employer Greenhouse-candidate slugs probed (`snyk`, `1password`, `canva`, `chime`, `intercom`, `mixpanel`, `gong`, `attentive`, `rippling`, `hashicorp`, `elastic`, `snowflake`, `huggingface`, `replicate`, `hubspot`, `benchling`, `segment`, `automattic`) returned HTTP 200 on **six** (`attentive` 59 jobs, `chime` 72 jobs, `elastic` 193 jobs, `hubspot` `meta.total === 0` seventh-consecutive empty re-probe, `intercom` 174 jobs, `mixpanel` 51 jobs); Attentive picked as the alphabetically-first bite from the live-board set (`att` < `chi` < `ela` < `int` < `mix`); the four remaining live candidates (Chime, Elastic, Intercom, Mixpanel) plus a HubSpot re-probe pivot queue up for runs #269+. |
| shipped    | `source-company-chime`     | `company`  | **Chime** (`chime.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/chime/jobs`). | Spec 059, run #269. Thin wrapper following the `source-company-attentive` pattern (fifteenth cohort member to use the entity-decode-then-tag-strip description pipeline). **Two structural deviations** from the Attentive template: (a) D-04 wire-shape variant 10 — `https://boards.greenhouse.io/chime/jobs/<id>?gh_jid=<id>` — a **tenth** distinct wire-shape variant in the cohort, distinguished by the **legacy hosted-board apex** (`boards.greenhouse.io` without the `job-` prefix) plus a trailing `?gh_jid=<id>` query suffix — first plugin in the cohort to use the legacy `boards.greenhouse.io` apex host. (The legacy host issues an HTTP 301 to `job-boards.greenhouse.io/chime/jobs/<id>?gh_jid=<id>` confirmed via `curl -I` at run-269 start; the wire shape is the legacy form and the plugin mirrors it byte-for-byte.) (b) D-09 brand-name trim — wire `company_name` `'Chime Financial, Inc'` (note: no trailing `.` after `Inc`, unlike Affirm's `'Affirm, Inc.'` form) is trimmed to the brand `'Chime'` via string-literal pin — fourth cohort plugin to apply a brand-name trim (after Affirm `Spec 044 § 10 D-09` `'Affirm, Inc.'` → `'Affirm'`, Gusto `Spec 048 § 10 D-09` `'Gusto, Inc.'` → `'Gusto'`, and ZoomInfo `Spec 057 § 10 D-09` `'ZoomInfo Technologies LLC'` → `'ZoomInfo'`); the **second** to clean a comma-separated suffix (after Affirm) and the **first** to clean a comma-separated suffix where the legal-entity token (`'Inc'`) carries no trailing period. **D-10 omitted** — all 72 wire titles in the run-269 probe were already trim-clean; first cohort plugin since Webflow (Spec 056) to omit D-10. The new cohort-first observation (encoded as a regression guard but not a structural deviation) is the **literal-ampersand-bearing flat department names** (e.g. `'AI & App Experience Engineering'`, `'Spending, Saving, & Investing Engineering'`, `'Enterprise S&M'`) where the wire `&` byte flows through as a literal `&` (not entity-encoded as `&amp;`) — Chime is the second plugin in the cohort to ship a fixture with literal-ampersand-bearing department names (after Netlify). Selected from the **run-268 fresh-sweep live-board pool** as the **alphabetically-next bite after Attentive** (`chi` < `ela` < `int` < `mix`); the four remaining live-board pool members (Elastic, Intercom, Mixpanel — plus a HubSpot re-probe pivot) queue up for runs #270+. |
| shipped    | `source-company-elastic`   | `company`  | **Elastic** (`elastic.co`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/elastic/jobs`). | Spec 060, run #270. Thin wrapper following the `source-company-attentive` pattern (sixteenth cohort member to use the entity-decode-then-tag-strip description pipeline). **One structural deviation** from the Attentive template: D-04 wire-shape variant 11 — `https://jobs.elastic.co/jobs?gh_jid=<id>&gh_jid=<id>` — an **eleventh** distinct wire-shape variant in the cohort, distinguished by the **vanity-domain** (`jobs.elastic.co`, the custom careers-subdomain on the brand domain hosting the rendered Greenhouse iframe) plus the **duplicate `gh_jid=<id>&gh_jid=<id>` query parameter** the second of which reflects the same listing id as the first, repeated literally on the wire — first plugin in the cohort to use a vanity-domain wire shape and first plugin in the cohort with a duplicate query parameter. **D-10 wire-title `.trim()` applied** — 16 of 193 wire titles in the run-270 probe (8.3 %) carry trailing ASCII-space padding (e.g. `'Account Executive '`, `'Consulting Architect, Public Sector - Netherlands '`, `'Customer Architect - EMEA Central '`, `'Enterprise Account Executive '`) that the plugin trims before downstream filters and emit — fifth cohort plugin to apply a wire-title trim (after Brex `Spec 047 § 10 D-10`, Buildkite `Spec 050 § 10 D-10`, ZoomInfo `Spec 057 § 10 D-10`, and Attentive `Spec 058 § 10 D-10`). **D-09 omitted** — wire `company_name === 'Elastic'` byte-for-byte (no legal-entity suffix on the wire); the plugin reads `listing.company_name` directly. The new cohort-first observation (encoded as a regression guard but not a structural deviation) is the **compound `' - '`-separated department names that scope region within line-of-business** (e.g. `'Sales - EMEA - UKI'`, `'Sales - APJ - India'`, `'Sales - EMEA - North'`, `'Sales - EMEA - Central'`, `'Sales - AMER - Management and Support'`, `'Customer Architects - EMEA'`, `'Consulting - EMEA'`, `'SA - Global'`) — Elastic is the **first plugin in the cohort to ship a fixture with `' - '`-separated compound department names that scope region within line-of-business**. Selected from the **run-268 fresh-sweep live-board pool** as the **alphabetically-next bite after Chime** (`ela` < `int` < `mix`); the two remaining live-board pool members (Intercom, Mixpanel — plus a HubSpot re-probe pivot) queue up for runs #271+. The HubSpot re-probe at run-270 start returned HTTP 200 with `meta.total === 0` — eighth-consecutive empty re-probe across runs #262–#270; HubSpot remains deferred. |
| shipped    | `source-company-intercom`  | `company`  | **Intercom** (`intercom.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/intercom/jobs`). | Spec 061, run #271. Thin wrapper following the `source-company-attentive` pattern — Intercom is a **near-pure Attentive twin**: same wire-shape variant 2 (`job-boards.greenhouse.io/intercom/jobs/<id>` — tenth cohort member to use variant 2 after Vercel, Affirm, Gusto, Mercury, Buildkite, Netlify, Postman, Webflow, and Attentive), same D-08 entity-decode-then-tag-strip description pipeline (seventeenth cohort member), same D-09 omission (wire `company_name === 'Intercom'` byte-for-byte; no legal-entity suffix on the wire — the plugin reads `listing.company_name` directly), and same D-10 application — **zero structural deviations** from the Attentive template. **D-10 wire-title `.trim()` applied** — 25 of 174 wire titles in the run-271 probe (14.4 %, the **highest pad-rate of any cohort plugin to date** — beating Elastic's 8.3 %, Attentive's 6.8 %, Buildkite's 7.4 %, ZoomInfo's 6.1 %) carry trailing ASCII-space padding (e.g. `'Account Executive, Commercial '`, `'Account Executive, Commercial - French Speaking '`, `'Account Executive (Existing Business), Commercial '`, `'Business Development Representative, Emerging AI Products '`, `'Director, Sales Strategy & Planning '`) that the plugin trims before downstream filters and emit — sixth cohort plugin to apply a wire-title trim (after Brex `Spec 047 § 10 D-10`, Buildkite `Spec 050 § 10 D-10`, ZoomInfo `Spec 057 § 10 D-10`, Attentive `Spec 058 § 10 D-10`, and Elastic `Spec 060 § 10 D-10`). The new cohort-first observation (encoded as a regression guard but not a structural deviation) is **trailing-whitespace padding on a wire department name** — the wire `'Legal '` department name carries an ASCII-space pad analogous to the wire-title pad pattern D-10 captures, but the plugin emits the wire `departments[0].name` byte-for-byte (no department-name `.trim()` — the case-insensitive `searchTerm.toLowerCase().includes(...)` filter remains semantically correct against the padded form because `'Legal '.toLowerCase().includes('legal')` is true). Department format is otherwise flat single-token (`'Sales'`, `'Engineering'`, `'Product'`, `'Marketing'`, `'Finance & Business Operations'`, `'Customer Success & Solutions'`, `'AI Group'`, `'R&D'`, `'Recruiting'`, `'Research, Analytics & Data Science'`, `'Customer Support'`, `'People'`, `'Product Design'`, `'Revenue Operations'`) — distinct from Elastic's compound `' - '`-separated regional-scoped format, ZoomInfo's numeric-code-prefix format, Toast's colon-separated nested-path format, and like Attentive's, Webflow's, Postman's, Netlify's, etc. flat single-token format. Selected from the **run-268 fresh-sweep live-board pool** as the **alphabetically-next bite after Elastic** (`int` < `mix`); the one remaining live-board pool member (Mixpanel — plus a HubSpot re-probe pivot) queues up for runs #272+. The HubSpot re-probe at run-271 start returned HTTP 200 with `meta.total === 0` — ninth-consecutive empty re-probe across runs #262–#271; HubSpot remains deferred. |
| shipped    | `source-company-mixpanel`  | `company`  | **Mixpanel** (`mixpanel.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/mixpanel/jobs`). | Spec 062, run #272. Thin wrapper following the `source-company-intercom` pattern — Mixpanel is a **near-pure Intercom twin**: same wire-shape variant 2 (`job-boards.greenhouse.io/mixpanel/jobs/<id>` — eleventh cohort member to use variant 2 after Vercel, Affirm, Gusto, Mercury, Buildkite, Netlify, Postman, Webflow, Attentive, and Intercom), same D-08 entity-decode-then-tag-strip description pipeline (eighteenth cohort member), same D-09 omission (wire `company_name === 'Mixpanel'` byte-for-byte; no legal-entity suffix on the wire — the plugin reads `listing.company_name` directly), and same D-10 application — **zero structural deviations** from the Intercom template. **D-10 wire-title `.trim()` applied** — 1 of 9 wire titles in the run-272 probe (~11.1 %) carries trailing ASCII-space padding (`'Account Manager '`) that the plugin trims before downstream filters and emit — seventh cohort plugin to apply a wire-title trim (after Brex `Spec 047 § 10 D-10`, Buildkite `Spec 050 § 10 D-10`, ZoomInfo `Spec 057 § 10 D-10`, Attentive `Spec 058 § 10 D-10`, Elastic `Spec 060 § 10 D-10`, and Intercom `Spec 061 § 10 D-10`). Department format is flat single-token (`'Sales'`, `'Engineering'`, `'Product'`, `'Marketing'`, `'Customer Success'`, etc.) — distinct from Elastic's compound `' - '`-separated regional-scoped format, ZoomInfo's numeric-code-prefix format, and Toast's colon-separated nested-path format. Selected from the **run-268 fresh-sweep live-board pool** as the **alphabetically-next bite after Intercom** (`mix` is the last live member of the pool). With Mixpanel shipped, the **run-268 fresh-sweep live-board pool is fully exhausted** — runs #273+ pivot to a third fresh probe sweep targeting the next batch of large-employer candidates (Notion, Linear, Loom, Front, Modern Treasury, Shopify, Square, Adobe, Salesforce, Atlassian, Slack, Zoom Video Communications, ServiceNow, Workday, Veeva, Toast Tab, Faire, Whatnot, Anduril, Scale AI, Glean, Perplexity, Mistral, Cohere, Together AI, Pika, Runway, Synthesia, Eleven Labs, Photoroom, Adept). The HubSpot re-probe at run-272 start returned HTTP 200 with `meta.total === 0` — tenth-consecutive empty re-probe across runs #262–#272; HubSpot remains deferred. |
| shipped    | `source-company-faire`     | `company`  | **Faire** (`faire.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/faire/jobs`). | Spec 063, run #273. Thin wrapper following the `source-company-chime` pattern — Faire shares Chime's wire-shape variant 10 (`boards.greenhouse.io/faire/jobs/<id>?gh_jid=<id>` — second cohort member to use variant 10 after Chime; legacy hosted-board apex without the `job-` prefix plus a trailing `?gh_jid=<id>` query suffix) and Chime's D-08 entity-decode-then-tag-strip description pipeline (nineteenth cohort member). **Two structural deviations** from the Chime template: (a) D-09 **omitted** — wire `company_name === 'Faire'` byte-for-byte (no legal-entity suffix on the wire — distinct from the legal-entity name "Faire Wholesale, Inc." that appears in SEC filings); the plugin reads `listing.company_name` directly. Thirteenth cohort plugin to omit D-09 against a single-word bare-brand wire `company_name`. (b) D-10 **applied** — 3 of 72 wire titles in the run-273 probe (~4.2 %) carry trailing ASCII-space padding (`'Production Designer, Brand '`, `'Senior Product Marketing Manager - Faire Pay '`, `'Staff Product Designer, Discovery Experience '`) that the plugin trims before downstream filters and emit — eighth cohort plugin to apply a wire-title trim (after Brex `Spec 047 § 10 D-10`, Buildkite `Spec 050 § 10 D-10`, ZoomInfo `Spec 057 § 10 D-10`, Attentive `Spec 058 § 10 D-10`, Elastic `Spec 060 § 10 D-10`, Intercom `Spec 061 § 10 D-10`, and Mixpanel `Spec 062 § 10 D-10`). Department format is multi-word descriptive (`'Customer Support Management'`, `'Engineering'`, `'Product Design'`, `'Marketing'`, `'Operations'`, etc.) — partly distinct from Mixpanel's strict flat single-token and Chime's single-token-with-`&`-bytes formats, structurally permissive of internal whitespace within a single department string. Selected from the **run-272 third-fresh-sweep candidate pool** — 30 candidate slugs probed at run-273 start (`notion`, `linear`, `loom`, `front`, `moderntreasury`, `shopify`, `square`, `adobe`, `salesforce`, `atlassian`, `slack`, `zoom`, `servicenow`, `workday`, `veeva`, `faire`, `whatnot`, `anduril`, `scaleai`, `glean`, `perplexity`, `mistral`, `cohere`, `together`, `pika`, `runway`, `synthesia`, `elevenlabs`, `photoroom`, `adept`); exactly **two** returned HTTP 200 (`faire` 72 jobs, `scaleai` 170 jobs); the other 28 slugs all returned HTTP 404 — those tenants are either not on Greenhouse, on authenticated-only Greenhouse boards, or use a non-public ATS. `faire` < `scaleai` lexically, so this run takes Faire; Scale AI queues for run #274. |
| shipped    | `source-company-scaleai`   | `company`  | **Scale AI** (`scale.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/scaleai/jobs`). | Spec 064, run #274. Thin wrapper following the `source-company-mixpanel` pattern — Scale AI shares Mixpanel's wire-shape variant 2 (`job-boards.greenhouse.io/scaleai/jobs/<id>` — twelfth cohort member to use variant 2 after Vercel, Affirm, Gusto, Mercury, Buildkite, Netlify, Postman, Webflow, Attentive, Intercom, and Mixpanel) and Mixpanel's D-08 entity-decode-then-tag-strip description pipeline (twentieth cohort member). **One structural deviation** from the Mixpanel template: D-10 **omitted** — 0 of 11 wire titles in the run-274 probe carry trailing ASCII-space padding (every title surveyed — `'Account Executive, Saudi Arabia'`, `'AI Applications Ops Lead, GPS'`, `'AI Deployment Strategist Intern'`, `'AI Product Manager'`, `'AI Strategy Consultant, Frontier Tech'`, `'Analytics & Data Science Manager, Finance'`, `'[Annotations] Operations Associate'`, `'[Annotations] Operations Program Manager'`, `'Applied AI Engineer, Enterprise'`, `'Applied AI Engineer, Enterprise GenAI'`, `'Applied AI Engineer, Global Public Sector'` — closes with a comma-suffix or alphanumeric byte); the plugin emits `listing.title` byte-for-byte without a `.trim()`. Structurally analogous to Chime (Spec 059 § 10 D-10 — also omitted), distinct from the trim-applied cohort: Brex, Buildkite, ZoomInfo, Attentive, Elastic, Intercom, Mixpanel, and Faire. **D-09 omitted** — wire `company_name === 'Scale AI'` byte-for-byte (the multi-token bare brand name with internal ASCII space; no legal-entity suffix on the wire — distinct from the legal-entity name "Scale AI, Inc." that appears in SEC filings); the plugin reads `listing.company_name` directly. **Fourteenth cohort plugin to omit D-09**, AND the **first to omit D-09 against a multi-token bare-brand wire `company_name`** (every prior D-09-omission plugin had a single-word bare-brand wire). The new cohort-first observation (encoded as a regression guard but not a structural deviation) is the **slug-vs-display-name whitespace-collapse asymmetry** — the Greenhouse slug is `scaleai` (no space) but the wire `company_name` is `'Scale AI'` (with internal ASCII space) and the display name `'Scale AI'` (with internal ASCII space) is preserved through the pipeline. Department format is multi-word descriptive with optional initialisms (`'GPS Sales'`, `'Engineering'`, `'Product'`, `'Operations'`, `'Annotations'`, `'Public Sector'`) — partly distinct from Mixpanel's strict flat single-token format and Faire's pure multi-word descriptive format. Selected from the **run-272 / run-273 third-fresh-sweep candidate pool** as the **alphabetically-second live-board hit after Faire** (`scaleai` is the only remaining live member). With Scale AI shipped, the **run-272 third-fresh-sweep live-board pool is fully exhausted** — runs #275+ pivot to a fourth fresh probe sweep targeting the next batch of large-employer candidates. The HubSpot re-probe at run-274 start returned HTTP 200 with `meta.total === 0` — twelfth-consecutive empty re-probe across runs #262–#274; HubSpot remains deferred. |
| shipped    | `source-company-cameo`     | `company`  | **Cameo** (`cameo.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/cameo/jobs`). | Spec 065, run #275. Thin wrapper following the `source-company-scaleai` pattern — Cameo shares Scale AI's wire-shape variant 2 (`job-boards.greenhouse.io/cameo/jobs/<id>` — thirteenth cohort member to use variant 2 after Vercel, Affirm, Gusto, Mercury, Buildkite, Netlify, Postman, Webflow, Attentive, Intercom, Mixpanel, and Scale AI), Scale AI's D-08 entity-decode-then-tag-strip description pipeline (twenty-first cohort member), Scale AI's D-09 omission (wire `company_name === 'Cameo'` byte-for-byte; no legal-entity suffix on the wire — distinct from the legal-entity name "Cameo, Inc." that appears in SEC filings), and Scale AI's D-10 omission (0 of 3 wire titles in the run-275 probe carry trailing pad bytes). **One structural deviation** from the Scale AI template: **D-11 partial-pad department pass-through** — 1 of 3 wire `departments[0].name` values in the run-275 probe (~33.3 %) carries a trailing ASCII-space pad byte (`'Cameo for Business '` — the second-listing department; the other two `'Engineering'` and `'Talent'` are clean); the plugin emits the wire `departments[0].name` byte-for-byte without a `.trim()`, locking the partial-pad pass-through as an observable regression guard. **First cohort plugin to ship a wire department-name with trailing ASCII-space padding pass-through observability**. **Fifteenth cohort plugin to omit D-09**, returning to the single-word bare-brand wire form (Mixpanel `'Mixpanel'`, Faire `'Faire'`, Intercom `'Intercom'`, Elastic `'Elastic'`, Webflow `'Webflow'`, Attentive `'Attentive'`, Postman `'Postman'`, Netlify `'Netlify'`, Mercury `'Mercury'`, Buildkite `'Buildkite'`, CircleCI `'CircleCI'`, Toast `'Toast'`, plus the Ramp Network slug-collapse case) — distinct from Scale AI's first-of-its-kind multi-token bare-brand wire `company_name === 'Scale AI'`. Department format is mixed clean and pad-suffixed multi-word descriptive (`'Engineering'`, `'Cameo for Business '`, `'Talent'`) — the highest single-listing pad rate in the cohort to date. Cameo at 3 roles is **below** the prior cohort minimum of 9 roles (Mixpanel shipped at 9; ZoomInfo at 14; Scale AI at 11), making this the **first cohort plugin to redefine the floor downward to 3** — a defensible move because the plugin is a thin wrapper that emits whatever the live board returns. Selected from the **fourth fresh probe sweep** as the **alphabetically-first live-board hit** across 36 candidate slugs (`carta`, `brightwheel`, `mavenclinic`, `glossier`, `casper`, `chewy`, `wayfair`, `flexport`, `epicgames`, `zendesk`, `stitchfix`, `classpass`, `plex`, `cameo`, `hims`, `compass`, `bumble`, `hinge`, `masterclass`, `skillshare`, `coursera`, `udemy`, `honeycomb`, `blameless`, `opslevel`, `lattice`, `workrise`, `niantic`, `tubi`, `fubotv`, `rover`, `fanatics`, `nylas`, `sanity`, `vidio`, plus the rolling `hubspot` re-probe); fourteen returned HTTP 200 (`cameo` 3 jobs, `carta` 52, `classpass` 70, `coursera` 8, `epicgames` 74, `flexport` 113, `fubotv` 11, `glossier` 17, `honeycomb` 10, `lattice` 11, `masterclass` 6, `mavenclinic` 24, `stitchfix` 22, `udemy` 17); the other 21 returned HTTP 404. The remaining thirteen live hits queue for runs #276+ in alphabetical order (`carta` next at run #276 with 52 roles). The HubSpot re-probe at run-275 start returned HTTP 200 with `meta.total === 0` — thirteenth-consecutive empty re-probe across runs #262–#275; HubSpot remains deferred. |
| shipped    | `source-company-carta`     | `company`  | **Carta** (`carta.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/carta/jobs`). | Spec 066, run #276. Thin wrapper following the `source-company-cameo` pattern — Carta shares Cameo's wire-shape variant 2 (`job-boards.greenhouse.io/carta/jobs/<id>` — fourteenth cohort member to use variant 2 after Vercel, Affirm, Gusto, Mercury, Buildkite, Netlify, Postman, Webflow, Attentive, Intercom, Mixpanel, Scale AI, and Cameo), Cameo's D-08 entity-decode-then-tag-strip description pipeline (twenty-second cohort member), and Cameo's D-09 omission (wire `company_name === 'Carta'` byte-for-byte; no legal-entity suffix on the wire — distinct from the legal-entity name "Carta, Inc." that appears in current SEC filings and the prior "eShares, Inc." legal name from before the 2017 rebrand). **Two structural deviations** from the Cameo template: (a) **D-10 applied** — at least 1 of 10 wire titles in the run-276 probe carries trailing ASCII-space padding (`'Business Development Manager, Private Equity '`); the plugin applies `.trim()` to the wire `title` before downstream filters and emit. **Ninth cohort plugin to apply D-10** (after Brex `Spec 047 § 10 D-10`, Buildkite `Spec 050 § 10 D-10`, ZoomInfo `Spec 057 § 10 D-10`, Attentive `Spec 058 § 10 D-10`, Elastic `Spec 060 § 10 D-10`, Intercom `Spec 061 § 10 D-10`, Mixpanel `Spec 062 § 10 D-10`, and Faire `Spec 063 § 10 D-10`). (b) **D-11 fully-clean** — Carta's wire department names are 0 of 10 padded (0 % pad-rate), distinct from Cameo's partial-pad pass-through; the plugin emits the wire `departments[0].name` byte-for-byte without a `.trim()` (the pass-through is a no-op on the clean wire data; if Carta adds padding upstream in the future, the pass-through observability lock catches the diff in the unit tests). **Sixteenth cohort plugin to omit D-09**, returning to the single-word bare-brand wire form. Department format is multi-word descriptive with single-token forms (`'Account Executive'`, `'Marketing'`, `'Tax'`, `'Engineering'`, `'Sales'`, `'Strategic Finance'`) — structurally permissive of internal whitespace within a single department string, similar to Faire's pure descriptive format. Selected from the **fourth fresh probe sweep** as the **alphabetically-second live-board hit** after Cameo (52 roles confirmed at run-275 close, sampled at run-276 start). The remaining twelve live hits queue for runs #277+ in alphabetical order (`classpass` next at run #277 with 70 roles). The HubSpot re-probe at run-276 start returned HTTP 200 with `meta.total === 0` — fourteenth-consecutive empty re-probe across runs #262–#276; HubSpot remains deferred. |
| shipped    | `source-company-classpass` | `company`  | **ClassPass** (`classpass.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/classpass/jobs`). | Spec 067, run #277. Thin wrapper following the `source-company-carta` pattern — ClassPass shares Carta's D-08 entity-decode-then-tag-strip description pipeline (twenty-third cohort member), Carta's D-09 omission (wire `company_name === 'ClassPass'` byte-for-byte; no parent-company suffix on the wire — distinct from the parent-company name "Mindbody" that owns ClassPass since 2021 and the legal-entity name "Classpass Inc." that may appear in older SEC filings), Carta's D-10 application (at least 10 of 70 wire titles in the run-277 probe carry trailing ASCII-space padding — `'Director, Product Management, ClassPass Consumer '`, `'Engineering Manager - Consumer & Merchandising '`, `'Field Account Executive - Portland, OR '`, plus 7 others; the plugin applies `.trim()` to the wire `title` before downstream filters and emit — **tenth cohort plugin to apply D-10**), and Carta's D-11 fully-clean department pass-through (0 of 70 wire department names in the run-277 probe carry trailing ASCII-space padding; the plugin emits `listing.departments[0].name` byte-for-byte without a `.trim()`). **One structural deviation** from the Carta template: **D-04 wire-shape variant 12 (vanity-domain).** ClassPass publishes `absolute_url` on a previously-unobserved **vanity-domain shape** `https://www.playlist.com/careers/opportunities/<id>?gh_jid=<id>` (parent-domain `www.playlist.com` rather than ClassPass's own `classpass.com`; `careers/opportunities` path; single `gh_jid` query parameter — distinct from Elastic's variant-11 duplicate-`gh_jid` shape). This is the **first** plugin in the cohort to use **wire-shape variant 12** — the **fifteenth distinct wire-shape variant** in the company-direct cohort. The plugin emits `listing.absolute_url` byte-for-byte to preserve the canonical destination; the **fallback** `jobUrl` constructor (when Greenhouse omits `absolute_url`) defaults to the canonical Greenhouse variant-2 form `https://job-boards.greenhouse.io/classpass/jobs/<id>` rather than reconstructing the vanity-domain shape, because the vanity-domain shape requires `playlist.com`-side proxying that may not be guaranteed for all listing IDs. **Seventeenth cohort plugin to omit D-09**, returning to the single-word bare-brand wire form (with internal capital P). Department format is fully-clean single-token (`'Sales'`, `'Marketing'`, `'Engineering'`) — similar to Carta's pure descriptive format. Selected from the **fourth fresh probe sweep** as the **alphabetically-third live-board hit** after Cameo and Carta (70 roles confirmed at run-277 start). The remaining eleven live hits queue for runs #278+ in alphabetical order (`coursera` next at run #278 with 8 roles). The HubSpot re-probe at run-277 start returned HTTP 200 with `meta.total === 0` — fifteenth-consecutive empty re-probe across runs #262–#277; HubSpot remains deferred. |
| shipped    | `source-company-coursera`  | `company`  | **Coursera** (`coursera.org`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/coursera/jobs`). | Spec 068, run #278. Thin wrapper following the `source-company-chime` pattern — Coursera shares Chime's wire-shape variant 2 (`job-boards.greenhouse.io/coursera/jobs/<id>` — fourteenth cohort member to use variant 2), Chime's D-08 entity-decode-then-tag-strip description pipeline (twenty-fourth cohort member), Chime's D-09 omission (wire `company_name === 'Coursera'` byte-for-byte; no legal-entity suffix on the wire — distinct from the legal-entity name "Coursera, Inc." that may appear in SEC filings or the NYSE ticker `COUR` since 2021), Chime's D-10 omission (0 of 8 wire titles in the run-278 probe carry trailing pad bytes — sixth cohort plugin to omit D-10), and Chime's D-11 fully-clean department pass-through (0 of 8 wire department names carry trailing pad bytes; the plugin emits `listing.departments[0].name` byte-for-byte without a `.trim()`). **Zero structural deviations** from the Chime template — making this the **first** Greenhouse-only company-direct plugin in run-history to ship as a clean re-spin of a prior cohort plugin with no per-axis deviations. **Eighteenth cohort plugin to omit D-09**, returning to the single-word bare-brand wire form. Department format is multi-word descriptive (`'Chief of Staff'`, `'Industry Partnerships'`, `'Degrees Marketing'`, `'People'`, `'Product Marketing'`, `'Accounting'`, `'Corporate Strategy'`) — structurally permissive of internal whitespace within a single department string. Selected from the **fourth fresh probe sweep** as the **alphabetically-fourth live-board hit** after Cameo, Carta, and ClassPass (8 roles confirmed at run-278 start). The remaining ten live hits queue for runs #279+ in alphabetical order (`epicgames` next at run #279 with 74 roles). The HubSpot re-probe at run-278 start returned HTTP 200 with `meta.total === 0` — sixteenth-consecutive empty re-probe across runs #262–#278; HubSpot remains deferred. |
| shipped    | `source-company-epicgames` | `company`  | **Epic Games** (`epicgames.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/epicgames/jobs`). | Spec 069, run #279. Thin wrapper following the `source-company-classpass` pattern — Epic Games shares ClassPass's D-08 entity-decode-then-tag-strip description pipeline (twenty-fifth cohort member), ClassPass's D-09 omission (wire `company_name === 'Epic Games'` byte-for-byte; the multi-token bare brand name with internal whitespace; no legal-entity suffix on the wire — distinct from the legal-entity name "Epic Games, Inc." that appears in current SEC filings and the prior "Epic MegaGames, Inc." legal name from before the 1999 rebrand), ClassPass's D-10 application (at least 2 of 74 wire titles in the run-279 probe carry trailing ASCII-space padding — `'Partnerships Director - Sports & Talent '` twice across two listing IDs targeting the Cary, North Carolina office; the plugin applies `.trim()` to the wire `title` before downstream filters and emit — **eleventh cohort plugin to apply D-10**), and ClassPass's D-11 fully-clean department pass-through (0 of 74 wire department names in the run-279 probe carry trailing ASCII-space padding; the plugin emits `listing.departments[0].name` byte-for-byte without a `.trim()`). **One structural deviation** from the ClassPass template: **D-04 wire-shape variant 13 (vanity-domain bare-brand).** Epic Games publishes `absolute_url` on a previously-unobserved **vanity-domain shape** `https://epicgames.com/careers/jobs/<id>?gh_jid=<id>` (bare brand-domain `epicgames.com` rather than the `www.epicgames.com` parent-domain or any `jobs.epicgames.com` subdomain; `careers/jobs` path; single `gh_jid` query parameter — distinct from ClassPass's variant-12 `www.playlist.com/careers/opportunities/<id>?gh_jid=<id>` parent-domain shape and Elastic's variant-11 duplicate-`gh_jid` shape). This is the **second** vanity-domain variant in the cohort and the **sixteenth distinct wire-shape variant** in the company-direct cohort. The plugin emits `listing.absolute_url` byte-for-byte to preserve the canonical destination; the **fallback** `jobUrl` constructor (when Greenhouse omits `absolute_url`) defaults to the canonical Greenhouse variant-2 form `https://job-boards.greenhouse.io/epicgames/jobs/<id>` rather than reconstructing the vanity-domain shape, because the fallback can only produce a guaranteed-resolvable URL using the Greenhouse subdomain (same fallback strategy as ClassPass — Spec 067 § 10 D-04). **Nineteenth cohort plugin to omit D-09**, but the **second** cohort plugin (after Scale AI `'Scale AI'`) to ship with a multi-token bare-brand wire `company_name`. Department format is fully-clean single-token / multi-word descriptive (`'Art'`, `'Data Science'`, `'General Design'`, `'Sales/Licensing'`, `'Counsel'`, `'Product Support'`, `'Production'`, `'Product Mgmt'`, `'Engineering Specialist'`, `'Partnerships'`) — structurally permissive of internal whitespace, slashes, and abbreviations within a single department string. Selected from the **fourth fresh probe sweep** as the **alphabetically-fifth live-board hit** after Cameo, Carta, ClassPass, and Coursera (74 roles confirmed at run-279 start). The remaining nine live hits queue for runs #280+ in alphabetical order (`flexport` next at run #280 with 113 roles). The HubSpot re-probe at run-279 start returned HTTP 200 with `meta.total === 0` — seventeenth-consecutive empty re-probe across runs #262–#279; HubSpot remains deferred. |
| shipped    | `source-company-flexport`  | `company`  | **Flexport** (`flexport.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/flexport/jobs`). | Spec 070, run #280. Thin wrapper following the `source-company-faire` pattern — Flexport shares Faire's wire-shape variant 10 (legacy hosted-board apex `boards.greenhouse.io/flexport/jobs/<id>?gh_jid=<id>` — **third** plugin in the cohort to use variant 10 after Chime and Faire), Faire's D-08 entity-decode-then-tag-strip description pipeline (twenty-sixth cohort member), Faire's D-09 omission (wire `company_name === 'Flexport'` byte-for-byte; no legal-entity suffix on the wire — distinct from the legal-entity name "Flexport, Inc." that may appear in corporate filings), Faire's D-10 application (at least 11 of 113 wire titles in the run-280 probe carry trailing ASCII-space padding — `'Area Manager '`, `'Country Manager, Mexico '`, `'Manager, Air Operations '`, plus 8 others, ~9.7 % pad rate — the **highest pad rate observed in the cohort to date**; the plugin applies `.trim()` to the wire `title` before downstream filters and emit — **twelfth cohort plugin to apply D-10**), and Faire's D-11 fully-clean department pass-through (0 of 113 wire department names in the run-280 probe carry trailing ASCII-space padding; the plugin emits `listing.departments[0].name` byte-for-byte without a `.trim()`). **Zero structural deviations** from the Faire template — making this the **second** Greenhouse-only company-direct plugin in run-history to ship as a clean re-spin of a prior cohort plugin with no per-axis deviations (after Coursera off Chime at run #278). **Twentieth cohort plugin to omit D-09**, returning to the single-word bare-brand wire form. Department format is mixed clean single-token / multi-word descriptive (`'Sales'`, `'Sales, Enterprise'`, `'Air & Ocean Freight Management'`, `'Fulfillment'`, `'Finance'`, `'Capital'`, `'Partnerships'`, `'Customs'`, `'Engineering'`, `'Global Operations'`, `'Ocean Gateway Operations'`, `'Product Management'`, `'Air Gateway Operations'`, `'Trade Advisory'`) — structurally permissive of internal whitespace, ampersands, and commas within a single department string. Selected from the **fourth fresh probe sweep** as the **alphabetically-sixth live-board hit** after Cameo, Carta, ClassPass, Coursera, and Epic Games (113 roles confirmed at run-280 start). The remaining eight live hits queue for runs #281+ in alphabetical order (`fubotv` next at run #281 with 11 roles). The HubSpot re-probe at run-280 start returned HTTP 200 with `meta.total === 0` — eighteenth-consecutive empty re-probe across runs #262–#280; HubSpot remains deferred. |
| shipped    | `source-company-adaptivebiotechnologies` | `company` | **Adaptive Biotechnologies** — Adaptive Biotechnologies Corporation (`adaptivebiotech.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/adaptivebiotechnologies/jobs`). | Spec 187, run #397. **Thirteenth plugin in the eleventh fresh probe sweep** (after AccuWeather at run #385, ACI Learning at run #386, Ackermann Group at run #387, ACLU at run #388, ACOG at run #389, aCommerce at run #390, ACP at run #391, Acquia at run #392, Acrisure Innovation at run #393, Acryl Data at run #394, Acumen at run #395, and Acurus Solutions at run #396). Thin wrapper following the `source-company-acurussolutions` pattern with **one structural deviation**. **D-04 sub-axis:** Acurus Solutions's variant 2 (canonical Greenhouse host `https://job-boards.greenhouse.io/<slug>/jobs/<id>`) → **NEW wire-shape variant 47** (first cohort observation): `https://www.adaptivebiotech.com/career-listings/listing?gh_jid=<id>` — HTTPS + `www.`-prefixed truncated-bare-brand `.com` (drop `nologies` from `biotechnologies` → 19-byte domain `adaptivebiotech.com`) + 2-segment `/career-listings/listing` apply-page path **without a trailing slash** + **single-id query** `?gh_jid=<id>`. The **fiftieth distinct wire-shape variant** in the company-direct cohort (after Textio variant 46 at Spec 174). **First cohort observation of brand-domain-token-truncation** (slug retains full `biotechnologies` token while the public-facing domain truncates to `biotech`, dropping the 9-byte `nologies` suffix). **First cohort observation of no-trailing-slash 2-segment apply-page path within a NEW-variant D-04 observation** (Textio variant 46 carried a *trailing-slash* `/careers/apply/` path). **First cohort observation of single-id `?gh_jid=`-only query within a NEW-variant D-04 observation** (Textio variant 46 carried dual-id `?job=<id>&gh_jid=<id>`). The plugin emits `listing.absolute_url` byte-for-byte to preserve the canonical destination; the **fallback** `jobUrl` constructor (when Greenhouse omits `absolute_url`) defaults to the canonical Greenhouse variant-2 form `https://job-boards.greenhouse.io/adaptivebiotechnologies/jobs/<id>` (same fallback strategy as Textio / Symphony / Samsara). All other axes share with Acurus Solutions: D-08 (**143rd member**), D-09 omitted at runtime (wire `'Adaptive Biotechnologies'` 24 bytes case-symmetric 2-token PascalCase + 1 ASCII space — `'Adaptive'` 8 bytes PascalCase cap-at-byte-0-only + `'Biotechnologies'` 15 bytes PascalCase cap-at-byte-0-only; slug `adaptivebiotechnologies` 23 bytes is byte-for-byte the space-strip + lowercase of the wire — canonical case-symmetric 2-token PascalCase sub-form; **134th cohort plugin to omit D-09**), D-10 applied (1 of 13 wire titles padded — `'Clinical Lab Technologist II '` trailing-pad form, ~7.7 %; **87th cohort plugin to apply D-10**), D-11 omitted (0 of 10 unique padded across `'Commercial Operations'`, `'Diagnostics Clinical Services'`, `'Diagnostics Sales'`, `'Digital Health'`, `'Executive'`, `'IT Systems & Infrastructure'`, `'Laboratory Operations'`, `'Legal'`, `'Research and Innovation'`, `'Sales & Business Development'`; **114th cohort plugin with fully-clean department pass-through**). The run-397 probe sampled **13 visible roles** via direct curl probe of `https://api.greenhouse.io/v1/boards/adaptivebiotechnologies/jobs?content=true`. **176th** Greenhouse-backed company-direct plugin in the catalogue. Sourced from the upstream `OTHERS/Ats-scrapers/ats-companies/greenhouse.csv` corpus as the **alphabetically-next** clean non-numeric-slug entry not already represented in `packages/plugins/source-company-*` after Acurus Solutions, with a confirmed live board. Adaptive Biotechnologies Corporation — Seattle, WA HQ commercial immunosequencing platform (NASDAQ: ADPT; founded by Harlan & Chad Robins in 2009; built around the immunoSEQ T-cell / B-cell receptor repertoire assay, the FDA-cleared clonoSEQ minimal-residual-disease blood test for lymphoid malignancies, and the T-Detect COVID/Lyme/CMV antigen-mapping diagnostic line; competes with Natera, Guardant Health, Exact Sciences, and Veracyte across the blood-cancer MRD segment). |
| shipped    | `source-company-acurussolutions` | `company` | **Acurus Solutions** — Acurus Solutions Private Limited (`acurus.in`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/acurussolutions/jobs`). | Spec 186, run #396. **Twelfth plugin in the eleventh fresh probe sweep** (after AccuWeather at run #385, ACI Learning at run #386, Ackermann Group at run #387, ACLU at run #388, ACOG at run #389, aCommerce at run #390, ACP at run #391, Acquia at run #392, Acrisure Innovation at run #393, Acryl Data at run #394, and Acumen at run #395). Thin wrapper following the `source-company-acumen` pattern with **one structural deviation**. **D-09 sub-axis:** Acumen's case-symmetric bare-brand single-token PascalCase 6-byte form (`'Acumen'` 6 bytes, slug `acumen` is byte-for-byte lowercase of wire) → **first cohort observation of 2-token-prefix PascalCase slug-truncation from 4-token all-PascalCase wire form with corporate-legal-suffix-drop**: wire `company_name === 'Acurus Solutions Private Limited'` 32 bytes (4 wire tokens, every token PascalCase cap-at-byte-0-only: `'Acurus'` 6 bytes + `'Solutions'` 9 bytes + `'Private'` 7 bytes + `'Limited'` 7 bytes); slug `acurussolutions` 15 bytes derives from first-2-tokens `'Acurus Solutions'` 16 bytes (drop last-2-tokens `'Private Limited'` Indian-private-limited-company legal-entity suffix), then space-strip + lowercase → 15-byte slug. **First cohort observation of 4-token all-PascalCase wire form with slug-truncation D-09 sub-form**. **First cohort observation of drop-2-tokens slug-truncation factor** (between AccuWeather drop-1 and ACLU drop-3). **First cohort observation of corporate-legal-suffix-drop slug-truncation** (drop `'Private Limited'` legal-entity suffix). **Fourth cohort observation of slug-truncation D-09 sub-form overall** (after AccuWeather drop-1, ACLU drop-3, ACOG initials-derivation). All other axes share with Acumen: D-04 variant 2 (**85th plugin**; `https://job-boards.greenhouse.io/acurussolutions/jobs/<id>`), D-08 (**142nd member**), D-09 omitted at runtime (wire `'Acurus Solutions Private Limited'` 32 bytes flows through byte-for-byte; **133rd cohort plugin to omit D-09**), D-10 applied (1 of 12 wire titles padded — trailing-pad form, ~8.3 %; **86th cohort plugin to apply D-10**), D-11 omitted (0 of 5 unique padded across `'Central Billing Office'`, `'Health Information Management'`, `'People'`, `'Revenue Cycle Management'`, `'Scribe'`; **113th cohort plugin with fully-clean department pass-through**). The run-396 probe sampled **12 visible roles** via direct curl probe of `https://api.greenhouse.io/v1/boards/acurussolutions/jobs?content=true`. **175th** Greenhouse-backed company-direct plugin in the catalogue. Sourced from the upstream `OTHERS/Ats-scrapers/ats-companies/greenhouse.csv` corpus as the **alphabetically-next** clean non-numeric-slug entry not already represented in `packages/plugins/source-company-*` after Acumen, with a confirmed live board. Acurus Solutions Private Limited — Bengaluru-HQ (Karnataka, India) full-service healthcare revenue-cycle-management (RCM) outsourcing vendor serving U.S. hospital and physician-group clients across the AR-analysis / charge-posting / coding / denial-management / payment-posting / pre-authorization / scribe-services verticals; operates the Central Billing Office, Health Information Management, People, Revenue Cycle Management, and Scribe delivery divisions. |
| shipped    | `source-company-acumen`    | `company`  | **Acumen** — Acumen, Inc. (`acumen.org`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/acumen/jobs`). | Spec 185, run #395. **Eleventh plugin in the eleventh fresh probe sweep** (after AccuWeather at run #385, ACI Learning at run #386, Ackermann Group at run #387, ACLU at run #388, ACOG at run #389, aCommerce at run #390, ACP at run #391, Acquia at run #392, Acrisure Innovation at run #393, and Acryl Data at run #394). Thin wrapper following the `source-company-tatari` pattern with **zero structural deviations** — clean re-spin of the canonical variant-2 + D-08 + D-09 omitted (case-symmetric bare-brand single-token PascalCase 6-byte sub-form) + D-10 applied (trailing-pad form) + D-11 omitted profile. **D-04 variant 2** (**84th plugin**; `https://job-boards.greenhouse.io/acumen/jobs/<id>`), **D-08** (**141st member**), **D-09 omitted at runtime** (wire `'Acumen'` 6 bytes case-symmetric PascalCase single-token, cap at byte 0 only; slug `acumen` is byte-for-byte lowercase of wire — third near-clean re-spin on this exact D-09 sub-form after Tatari and Acquia; **132nd cohort plugin to omit D-09**), **D-10 applied** (1 of 9 wire titles padded — `'Compensation Consultant '` 23-byte trailing-pad form, ~11.1 %; **85th cohort plugin to apply D-10**), **D-11 omitted** (0 of 4 unique padded across `'Acumen Academy'`, `'Executive Office'`, `'Investing'`, `'Talent'`; **112th cohort plugin with fully-clean department pass-through**). The run-395 probe sampled **9 visible roles** via direct curl probe of `https://api.greenhouse.io/v1/boards/acumen/jobs?content=true`. **174th** Greenhouse-backed company-direct plugin in the catalogue. Sourced from the upstream `OTHERS/Ats-scrapers/ats-companies/greenhouse.csv` corpus as the **alphabetically-next** clean non-numeric-slug entry not already represented in `packages/plugins/source-company-*` after Acryl Data, with a confirmed live board. Acumen, Inc. — 501(c)(3) patient-capital impact-investing nonprofit founded by Jacqueline Novogratz in 2001 in New York City; operates the Acumen Fund (catalytic equity/debt vehicle), Acumen Academy (leadership-development arm with Foundry incubator), and regional Acumen East Africa / West Africa / Latin America / South Asia investment teams across emerging markets in agriculture, clean-energy, education, financial-inclusion, healthcare, and workforce-development verticals. |
| shipped    | `source-company-acryldata` | `company`  | **Acryl Data** — Acryl Data, Inc. (`acryl.io`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/acryldata/jobs`). | Spec 184, run #394. **Tenth plugin in the eleventh fresh probe sweep** (after AccuWeather at run #385, ACI Learning at run #386, Ackermann Group at run #387, ACLU at run #388, ACOG at run #389, aCommerce at run #390, ACP at run #391, Acquia at run #392, and Acrisure Innovation at run #393). Thin wrapper following the `source-company-acquia` pattern (closest variant-2 + D-08 + D-09/D-10/D-11 all-omitted cousin) with **one structural deviation**. **D-09 sub-axis:** Acquia's case-symmetric bare-brand single-token PascalCase with slug = lowercase-of-wire (`'Acquia'` → `acquia`) → **slug-not-derived-from-wire-company_name sub-form**: wire `company_name === 'DataHub'` 7 bytes TWO-cap PascalCase single-token (caps at byte 0 and byte 4), but slug `acryldata` 9 bytes is NOT lowercase-of-wire — the slug derives from the corporate name `'Acryl Data'` (10 bytes, two-token PascalCase + ASCII-space + space-strip + lowercase → 9-byte slug), while the wire emits the product-line brand `'DataHub'`. **First cohort observation of slug-not-derived-from-wire-company_name sub-form** — the slug is sourced from the corporate name; the wire `company_name` flows through as the product brand. Precedent for slug ≠ wire `company_name`: source-company-beam (slug `beam`, wire `'Bridge to Enter Advanced Mathematics (BEAM)'`) — but BEAM extracted the slug from a parenthesised acronym inside the wire, whereas Acryl Data's slug is fully external to the wire `company_name`. All other axes share with Acquia: D-04 variant 2 (**83rd plugin**; `https://job-boards.greenhouse.io/acryldata/jobs/<id>`), D-08 (**140th member**), D-09 omitted at runtime (wire `'DataHub'` 7 bytes flows through byte-for-byte; **131st cohort plugin to omit D-09**), D-10 omitted (0 of 9 wire titles padded — defensive `.trim()` safe no-op; **44th cohort plugin to omit D-10**), D-11 omitted (0 of 3 unique padded across `'Marketing & Sales'`, `'Engineering'`, `'Customer Success'`; **111th cohort plugin with fully-clean department pass-through**). The run-394 probe sampled **9 visible roles** via direct curl probe of `https://api.greenhouse.io/v1/boards/acryldata/jobs?content=true`. **173rd** Greenhouse-backed company-direct plugin in the catalogue. Sourced from the upstream `OTHERS/Ats-scrapers/ats-companies/greenhouse.csv` corpus as the **alphabetically-next** clean non-numeric-slug entry not already represented in `packages/plugins/source-company-*` after Acrisure Innovation, with a confirmed live board. Acryl Data is the corporate steward of the open-source DataHub metadata-platform project (founded 2020, Palo Alto–HQ, backed by 8VC / LinkedIn / Insight Partners / DBC Venture Partners). |
| shipped    | `source-company-acrisureinnovation` | `company`  | **Acrisure Innovation** — Acrisure LLC innovation unit (`acrisure.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/acrisureinnovation/jobs`). | Spec 183, run #393. **Ninth plugin in the eleventh fresh probe sweep** (after AccuWeather at run #385, ACI Learning at run #386, Ackermann Group at run #387, ACLU at run #388, ACOG at run #389, aCommerce at run #390, ACP at run #391, and Acquia at run #392). Thin wrapper following the `source-company-accuweather` pattern with **one structural deviation**. **D-11 sub-axis:** AccuWeather's trailing-pad applied (2 of 15 unique departments padded — `'Facilities '`, `'Information Systems '`) → **omitted (clean pass-through)** (0 of 4 unique departments padded — `'Data'`, `'Engineering'`, `'Marketing'`, `'Product'`; wire `departments[0].name` flows through byte-for-byte without `.trim()` overlay; **110th cohort plugin with fully-clean department pass-through**). The **D-10 sub-axis** further differs in **pad-position sub-form**: AccuWeather's was **trailing-only** (7 of 30 titles, ~23.3 %); Acrisure Innovation's is the new **leading-AND-trailing-mixed** sub-form — 1 **leading-only-pad** observation (`' Forward Deployed (Echo)- Atlanta, GA'`) AND 1 **leading-AND-trailing-pad** observation (`' Forward Deployed (Echo) - Austin, TX '`) on the same fixture. **First cohort observation of the leading-AND-trailing (both-sides) ASCII-space pad sub-form on a single listing.** `.trim()` uniformly strips both leading and trailing pad bytes. All other axes share with AccuWeather: D-04 variant 2 (**82nd plugin**; `https://job-boards.greenhouse.io/acrisureinnovation/jobs/<id>`), D-08 (**139th member**), D-09 omitted at runtime (wire `'Acrisure Innovation'` 19 bytes case-symmetric two-token PascalCase + ASCII-space + space-strip — first token `Acrisure` 8 bytes PascalCase cap-at-0 + 1 internal ASCII space + second token `Innovation` 10 bytes PascalCase cap-at-0 → 18-byte lowercase slug `acrisureinnovation`; **130th cohort plugin to omit D-09**), D-10 applied with leading-AND-trailing-mixed pad form (2 of 15 padded ~13.3 %; **84th cohort plugin to apply D-10**). The run-393 probe sampled **15 visible roles** via direct curl probe of `https://api.greenhouse.io/v1/boards/acrisureinnovation/jobs?content=true`. **172nd** Greenhouse-backed company-direct plugin in the catalogue. Sourced from the upstream `OTHERS/Ats-scrapers/ats-companies/greenhouse.csv` corpus as the **alphabetically-next** clean non-numeric-slug entry not already represented in `packages/plugins/source-company-*` after Acquia, with a confirmed live board. Innovation unit of Acrisure LLC (Grand Rapids–HQ global insurance-brokerage and fintech platform; founded 2005; ~$4.3B FY2024 revenue; ~17 000 global employees; backed by Blackstone / BDT Capital / Guggenheim / Fidelity National Financial). |
| shipped    | `source-company-acquia`    | `company`  | **Acquia** — Acquia, Inc. (`acquia.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/acquia/jobs`). | Spec 182, run #392. **Eighth plugin in the eleventh fresh probe sweep** (after AccuWeather at run #385, ACI Learning at run #386, Ackermann Group at run #387, ACLU at run #388, ACOG at run #389, aCommerce at run #390, and ACP at run #391). Thin wrapper following the `source-company-coursera` pattern with **zero structural deviations** — clean re-spin of the canonical variant-2 + D-08 + D-09/D-10/D-11 all-omitted profile. **D-04 variant 2** (**81st plugin**; `https://job-boards.greenhouse.io/acquia/jobs/<id>`), **D-08** (**138th member**), **D-09 omitted at runtime** (wire `'Acquia'` 6 bytes case-symmetric PascalCase single-token; **129th cohort plugin to omit D-09**), **D-10 omitted** (0/17 padded — defensive `.trim()` safe no-op; **43rd cohort plugin to omit D-10**), **D-11 omitted** (0/5 unique padded across `'Customer Success'`, `'Engineering'`, `'Marketing'`, `'Products'`, `'Sales'`; **109th cohort plugin with fully-clean department pass-through**). The run-392 probe sampled **17 visible roles** via direct curl probe of `https://api.greenhouse.io/v1/boards/acquia/jobs?content=true`. **171st** Greenhouse-backed company-direct plugin in the catalogue. Sourced from the upstream `OTHERS/Ats-scrapers/ats-companies/greenhouse.csv` corpus as the **alphabetically-next** clean non-numeric-slug entry not already represented in `packages/plugins/source-company-*` after ACP, with a confirmed live board. Drupal-based enterprise DXP vendor founded in 2007 by Drupal creator Dries Buytaert; Vista Equity Partners portfolio since 2019. |
| shipped    | `source-company-acp`       | `company`  | **ACP** — Academy with Community Partners (`acpaz.org`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/acp/jobs`). | Spec 181, run #391. **Seventh plugin in the eleventh fresh probe sweep** (after AccuWeather at run #385, ACI Learning at run #386, Ackermann Group at run #387, ACLU at run #388, ACOG at run #389, and aCommerce at run #390). Thin wrapper following the `source-company-acog` pattern (closest D-09 acronym-by-initials cousin) with **two structural deviations**. **D-10 sub-axis:** ACOG's trailing-pad applied (1 of 6 wire titles padded ~16.7 %) → **omitted (clean pass-through)** (0 of 3 wire titles padded — defensive `.trim()` safe no-op; **42nd cohort plugin to omit D-10**). **D-09 sub-axis (cardinality variant):** ACOG's 4 PascalCase + 2 connectors (`'American College of Obstetricians and Gynecologists'` 51 bytes → 4-byte slug `acog`) → **3 PascalCase + 1 connector** (`'Academy with Community Partners'` 31 bytes — 4 wire-tokens, 3 PascalCase + 1 lowercase-connector; slug `acp` formed by sampling first letter of each PascalCase wire-token with connector-skip; A from Academy, C from Community, P from Partners; 1 connector `with` skipped). **Second cohort observation of acronym-by-initials slug derivation with connector-skip** — validates the ACOG sub-pattern at lower cardinality (3 PascalCase + 1 connector vs ACOG's 4 PascalCase + 2 connectors); confirms the sub-pattern is **not a one-off** observation but a recurring D-09 transformation pattern. All other axes share with ACOG: D-04 variant 2 (**80th plugin**; `https://job-boards.greenhouse.io/acp/jobs/<id>`), D-08 (**137th member**), D-09 omitted at runtime (wire `'Academy with Community Partners'` 31 bytes flows through byte-for-byte; **128th cohort plugin to omit D-09**), D-11 omitted (0 of 2 unique padded — wire flows through byte-for-byte; **108th cohort plugin with fully-clean department pass-through**). The run-391 probe sampled **3 visible roles** via direct curl probe of `https://api.greenhouse.io/v1/boards/acp/jobs?content=true`. **170th** Greenhouse-backed company-direct plugin in the catalogue. Sourced from the upstream `OTHERS/Ats-scrapers/ats-companies/greenhouse.csv` corpus (5 004 verified Greenhouse tenants on `71d9a56`) as the **alphabetically-next** clean non-numeric-slug entry not already represented in `packages/plugins/source-company-*` after aCommerce, with a confirmed live board. |
| shipped    | `source-company-acommerce` | `company`  | **aCommerce** — aCommerce Group (`acommerce.asia`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/acommerce/jobs`). | Spec 180, run #390. **Sixth plugin in the eleventh fresh probe sweep** (after AccuWeather at run #385, ACI Learning at run #386, Ackermann Group at run #387, ACLU at run #388, and ACOG at run #389). Thin wrapper following the `source-company-acog` pattern (closest variant-2 + D-08 + D-10-applied + D-11-omitted cousin) with **one structural deviation**. **D-09 sub-axis:** ACOG's acronym-by-initials slug derivation from a multi-token PascalCase + lowercase-connector wire form (`'American College of Obstetricians and Gynecologists'` 51 bytes — 6 wire-tokens, 4 PascalCase + 2 lowercase-connector; slug `acog` formed by sampling first letter of each PascalCase wire-token with connector-skip) → **single-token camelCase ONE-cap-at-byte-1 wire form** (`'aCommerce'` 9 bytes — single wire-token; byte 0 `'a'` lowercase, byte 1 `'C'` UPPERCASE (sole cap), bytes 2-8 `'ommerce'` all lowercase; slug `acommerce` is byte-for-byte lowercase of wire). **First cohort observation of (a) single-token camelCase classical wire form (lowercase-prefix + single-cap + lowercase-tail)** — prior ONE-cap observations all carried the cap at byte 0 (PascalCase classical). **AND (b) cap-at-byte-1-only D-09 sub-pattern** — prior lowercase-first observation at xAI (Spec 103) carried TWO caps at bytes 1 and 2 (`'xAI'`), not a single cap with lowercase tail. All other axes share with ACOG: D-04 variant 2 (**79th plugin**; `https://job-boards.greenhouse.io/acommerce/jobs/<id>`), D-08 (**136th member**), D-09 omitted at runtime (wire `'aCommerce'` 9 bytes flows through byte-for-byte; **127th cohort plugin to omit D-09**), D-10 applied with trailing-pad form (9 of 60 padded ~15.0 %; trailing-only — `'Key Account Manager '`, `'Intern - Live Streaming (Content & Creative) '`, `'Manager - Accounting '`, `'Manager - Key Account Management '`, `'Media Planner - Senior Specialist '`, `'Media Planner - Specialist '`, `'Senior Specialist - Co-Producer '`, `'Video Production Coordinator (Freelancer 6 months) '`; **83rd cohort plugin to apply D-10**), D-11 omitted (0 of 13 unique padded — wire flows through byte-for-byte; **107th cohort plugin with fully-clean department pass-through**). The run-390 probe sampled **60 visible roles** via direct curl probe of `https://api.greenhouse.io/v1/boards/acommerce/jobs?content=true`. **169th** Greenhouse-backed company-direct plugin in the catalogue. Sourced from the upstream `OTHERS/Ats-scrapers/ats-companies/greenhouse.csv` corpus (5 004 verified Greenhouse tenants on `71d9a56` after a 2-commit churn-pulse since run #389 confined to EURES / Workday scrapers + pipeline guards — zero corpus churn on the `ats-companies/` directory) as the **alphabetically-next** clean non-numeric-slug entry not already represented in `packages/plugins/source-company-*` after ACOG, with a confirmed live board. |
| shipped    | `source-company-acog`      | `company`  | **ACOG** — American College of Obstetricians and Gynecologists (`acog.org`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/acog/jobs`). | Spec 179, run #389. **Fifth plugin in the eleventh fresh probe sweep** (after AccuWeather at run #385, ACI Learning at run #386, Ackermann Group at run #387, and ACLU at run #388). Thin wrapper following the `source-company-aclu` pattern (closest variant-2 + D-10-applied cousin) with **two structural deviations**. **D-09 sub-axis:** ACLU's all-caps acronym + space-hyphen-space separator + multi-token suffix → first-token-only-lowercase slug-truncation (`'ACLU - National Office'` 22 bytes — 4 wire-tokens, first-token-only-lowercase, 3 trailing wire-tokens dropped) → **acronym-by-initials slug derivation from a multi-token PascalCase + lowercase-connector wire form** (`'American College of Obstetricians and Gynecologists'` 51 bytes — 6 wire-tokens split by ASCII spaces: `American` 8-byte PascalCase cap-at-0, `College` 7-byte PascalCase cap-at-0, `of` 2-byte all-lowercase connector, `Obstetricians` 13-byte PascalCase cap-at-0, `and` 3-byte all-lowercase connector, `Gynecologists` 13-byte PascalCase cap-at-0; slug `acog` formed by sampling the first letter of each PascalCase wire-token in order, skipping the 2 lowercase-connector tokens, and lowercasing the result). **First cohort observation of (a) acronym-by-initials slug derivation from a multi-token wire form (no single wire-token contains the slug as a substring)** — prior multi-token slug-truncation observations (AccuWeather / Tatari / ACLU) preserved the full first wire-token byte-for-byte and merely dropped trailing tokens; ACOG's slug requires multi-token initial-letter sampling. **AND (b) all-lowercase connector-token skip in slug derivation** — first cohort plugin where slug derivation explicitly excludes all-lowercase connector tokens (`of`, `and`). **D-11 sub-axis:** ACLU's trailing-pad applied (1 of 14 unique departments padded) → **clean pass-through** (0 of 6 unique departments padded; wire `departments[0].name` flows through byte-for-byte without `.trim()` overlay). All other axes share with ACLU: D-04 variant 2 (**78th plugin**; `https://job-boards.greenhouse.io/acog/jobs/<id>`), D-08 (**135th member**), D-09 omitted at runtime (wire `'American College of Obstetricians and Gynecologists'` 51 bytes flows through byte-for-byte; **126th cohort plugin to omit D-09**), D-10 applied with trailing-pad form (1 of 6 padded ~16.7 %; trailing-only — `'Director, Clinical Guidance Methodology '` (40 bytes); **82nd cohort plugin to apply D-10**), D-11 omitted (0 of 6 unique padded — wire flows through byte-for-byte; **106th cohort plugin with fully-clean department pass-through**). The run-389 probe sampled **6 visible roles** via direct curl probe of `https://api.greenhouse.io/v1/boards/acog/jobs?content=true`. **168th** Greenhouse-backed company-direct plugin in the catalogue. Sourced from the upstream `OTHERS/Ats-scrapers/ats-companies/greenhouse.csv` corpus (5 004 verified Greenhouse tenants, unchanged on `6dbb622` since run #385 — **4 consecutive zero-churn runs**) as the **alphabetically-next** clean non-numeric-slug entry not already represented in `packages/plugins/source-company-*` after ACLU, with a confirmed live board. |
| shipped    | `source-company-aclu`      | `company`  | **ACLU** — American Civil Liberties Union, Inc. (`aclu.org`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/aclu/jobs`). | Spec 178, run #388. **Fourth plugin in the eleventh fresh probe sweep** (after AccuWeather at run #385, ACI Learning at run #386, and Ackermann Group at run #387). Thin wrapper following the `source-company-accuweather` pattern (closest variant-2 + D-10-applied + D-11-applied cousin) with **one structural deviation**. **D-09 sub-axis:** AccuWeather's TWO-cap PascalCase + slug-truncation (`'AccuWeather Careers'` 19 bytes — 2 tokens, caps at 0/4 of the first token, 1 trailing PascalCase token dropped) → **all-caps acronym + space-hyphen-space separator + multi-token suffix → first-token-only-lowercase slug-truncation** (`'ACLU - National Office'` 22 bytes — 4 wire-tokens split by ASCII spaces: first wire-token `ACLU` 4-byte all-caps acronym with caps at every byte index 0/1/2/3, wire-token at index 1 = `-` 1-byte ASCII hyphen separator, wire-token at index 2 = `National` 8-byte PascalCase cap-at-0, wire-token at index 3 = `Office` 6-byte PascalCase cap-at-0; 3 wire-tokens dropped including the ASCII-hyphen separator; yielding 4-byte lowercase slug `aclu`). **First cohort observation of (a) an ASCII-hyphen wire-token being dropped in a slug-truncation D-09 sub-form** (prior slug-truncation observations — Oscar / BEAM / Founders / Fox / Symphony / AccuWeather / Tatari — dropped pure PascalCase or descriptive-corp-suffix tokens only) **AND (b) an all-caps acronym as the first wire-token of a slug-truncation D-09 sub-form** (prior all-caps-acronym D-09 observation at ACI Learning — Spec 176 — was a space-strip form, not slug-truncation; the ACI prefix was preserved into the slug, not dropped). All other axes share with AccuWeather: D-04 variant 2 (**77th plugin**; `https://job-boards.greenhouse.io/aclu/jobs/<id>`), D-08 (**134th member**), D-09 omitted at runtime (wire `'ACLU - National Office'` 22 bytes flows through byte-for-byte; **125th cohort plugin to omit D-09**), D-10 applied with trailing-pad form (2 of 40 padded ~5.0 %; all trailing-only — `'DevOps Engineering Manager '` (27 bytes) and `'Technical Project Manager (Term-Limited) '` (33 bytes); **81st cohort plugin to apply D-10**), D-11 applied with trailing-pad form (1 of 14 unique padded — `'National Political & Advocacy '` (30 bytes, on 3 of 40 listings); remaining 13 clean — `'Administration and Finance'`, `'Affiliate Offices'`, `'Affiliate Support & Nationwide Initiatives'`, `'Business Operations and Support Services'`, `'Capital Punishment Project'`, `'Communications & Marketing'`, `'Criminal Law Reform Project'`, `'Development'`, `'Exec'`, `'Legal'`, `'Product & Technology'`, `'Technology'`, `'Voting Rights Project'`; **22nd cohort plugin to apply D-11**). The run-388 probe sampled **40 visible roles** via direct curl probe of `https://api.greenhouse.io/v1/boards/aclu/jobs?content=true`. **167th** Greenhouse-backed company-direct plugin in the catalogue. Sourced from the upstream `OTHERS/Ats-scrapers/ats-companies/greenhouse.csv` corpus (5 004 verified Greenhouse tenants, unchanged on `6dbb622` since run #385 — **3 consecutive zero-churn runs**) as the **alphabetically-next** clean non-numeric-slug entry not already represented in `packages/plugins/source-company-*` after Ackermann Group, with a confirmed live board. |
| shipped    | `source-company-ackermanngroup` | `company` | **Ackermann Group** — Ackermann Group, LLC (`ackermanngroup.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/ackermanngroup/jobs`). | Spec 177, run #387. **Third plugin in the eleventh fresh probe sweep** (after AccuWeather at run #385 and ACI Learning at run #386). Thin wrapper following the `source-company-shopmonkey` pattern (closest variant-10 cousin) with **two structural deviations**. **D-09 sub-axis:** case-symmetric bare-brand (`'Shopmonkey'` 10 bytes — 1 token) → **two-token PascalCase + space-strip** (`'Ackermann Group'` 15 bytes — 2 tokens, both PascalCase with caps at byte index 0 of each token, single internal ASCII space stripped to yield 14-byte slug `ackermanngroup`). The two-token PascalCase + space-strip D-09 sub-pattern is established in the cohort (Collective Health, Maven Clinic, etc.) — Ackermann is the first to combine it with the variant-10 wire-shape. **D-11 sub-axis:** clean pass-through-with-depts (0 of 6 unique populated Shopmonkey depts padded) → **completely-absent departments form** — 0 of 12 Ackermann listings carry a department; wire `departments[]` array is empty for every listing in the run-387 probe. **First cohort observation of the completely-absent-departments D-11 sub-axis** — structurally distinct from the prior fully-clean department-pass-through observations (which all carried a populated `departments[]` array byte-for-byte clean). The plugin's `.departments?.[0]?.name ?? null` chain handles this transparently; every emitted JobPostDto carries `department === null`. All other axes share with Shopmonkey: D-04 variant 10 (legacy hosted-board apex `https://boards.greenhouse.io/ackermanngroup/jobs/<id>?gh_jid=<id>`; **9th plugin** in the cohort to use variant 10 after Chime / Faire / Flexport / Braze / Descript / Justworks / Founders / Shopmonkey), D-08 (**133rd member**), D-09 omitted at runtime (wire `'Ackermann Group'` 15 bytes flows through byte-for-byte; 124th cohort plugin to omit D-09), D-10 omitted (0 of 12 padded — emit byte-for-byte without `.trim()`; **41st cohort plugin to omit D-10**). The run-387 probe sampled **12 visible roles** via direct curl probe of `https://api.greenhouse.io/v1/boards/ackermanngroup/jobs?content=true`. **166th** Greenhouse-backed company-direct plugin in the catalogue. Sourced from the upstream `OTHERS/Ats-scrapers/ats-companies/greenhouse.csv` corpus (5 004 verified Greenhouse tenants, unchanged on `6dbb622` since run #385) as the **alphabetically-next** clean non-numeric-slug entry not already represented in `packages/plugins/source-company-*` after ACI Learning, with a confirmed live board. |
| shipped    | `source-company-acilearning` | `company` | **ACI Learning** — ACI Learning, LLC (`acilearning.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/acilearning/jobs`). | Spec 176, run #386. **Second plugin in the eleventh fresh probe sweep** (after AccuWeather at run #385). Thin wrapper following the `source-company-collectivehealth` pattern with **one structural deviation**. **D-09 sub-axis:** case-symmetric two-token space-strip (`'Collective Health'` 17 bytes — both tokens PascalCase, internal-space, lowercase-concat slug `collectivehealth`) → **acronym-prefix + PascalCase-suffix + space-strip** (`'ACI Learning'` 12 bytes — first wire token `ACI` 3-byte all-caps acronym with caps at byte indices 0/1/2; second wire token `Learning` 8-byte PascalCase with cap at byte index 0; single internal ASCII-space stripped to yield lowercase 11-byte slug `acilearning`). **First cohort observation of acronym-prefix + PascalCase-suffix + space-strip D-09 sub-pattern co-occurring in the same wire `company_name`** — all prior all-caps acronym D-09 plugins (IBM `'IBM'` 3-byte single-token) carried clean single-token wire forms with no space-strip; all prior PascalCase D-09 plugins carried single-token wire forms with no acronym sub-pattern; all prior space-strip D-09 plugins (Bill.com `'Bill.com'`, Collective Health `'Collective Health'`, Maven Clinic `'Maven Clinic'`, Scale AI `'Scale AI'`, Epic Games `'Epic Games'`, etc.) carried two case-symmetric tokens or two PascalCase tokens without the acronym sub-pattern in the first token. **First cohort plugin with an all-caps acronym prefix as the leading token of a space-strip multi-token wire form.** All other axes share with Collective Health: D-04 variant 2 (**76th plugin**; `https://job-boards.greenhouse.io/acilearning/jobs/<id>`), D-08 (**132nd member**), D-09 omitted at runtime (wire `'ACI Learning'` 12 bytes flows through byte-for-byte; **123rd cohort plugin to omit D-09**), D-10 omitted (0 of 5 padded — defensive `.trim()` safe no-op; **40th cohort plugin to omit D-10**), D-11 omitted (0 of 4 unique padded across `'Tech Academy Admissions'`, `'Success - Career Mentors'`, `'Rev Ops'`, `'Tech Academy Instructors'`; **105th cohort plugin** with fully-clean department pass-through). The run-386 probe sampled **5 visible roles**. **165th** Greenhouse-backed company-direct plugin in the catalogue. Sourced from the upstream `OTHERS/Ats-scrapers/ats-companies/greenhouse.csv` corpus (5 005 verified Greenhouse tenants, unchanged on `6dbb622` since run #385) as the **alphabetically-next** clean non-numeric-slug entry not already represented in `packages/plugins/source-company-*` after AccuWeather, with a confirmed live board. |
| shipped    | `source-company-accuweather` | `company` | **AccuWeather** — AccuWeather, Inc. (`accuweather.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/accuweather/jobs`). | Spec 175, run #385. **First plugin in the eleventh fresh probe sweep — launches the eleventh fresh probe sweep.** Thin wrapper following the `source-company-tatari` pattern with **two structural deviations**. **D-09 sub-axis:** case-symmetric bare-brand `'Tatari'` 6 bytes → **simultaneous TWO-cap PascalCase + slug-truncation** `'AccuWeather Careers'` 19 bytes — first wire token `AccuWeather` 11 bytes carries PascalCase asymmetry at byte indices 0 (`A` vs `a`) and 4 (`W` vs `w`) vs lowercase 11-byte slug `accuweather`, AND wire drops 1 trailing token `Careers`. **First cohort observation of TWO-cap PascalCase + slug-truncation co-occurring in the same wire `company_name`** — all prior TWO-cap PascalCase D-09 plugins (SoFi caps 0/2, StockX caps 0/5, xAI caps 0/2 lowercase-first, LaunchDarkly caps 0/6, PagerDuty caps 0/5, ComplyAdvantage caps 0/6, GoCardless caps 0/2, SimpliSafe caps 0/6) carried clean case-asymmetric wire forms with no descriptive-suffix truncation; all prior slug-truncation D-09 plugins (Oscar +1 token, BEAM acronym expansion, Founders -4 tokens, Fox -5 tokens, Symphony -2 tokens) carried case-symmetric wire forms with no PascalCase asymmetry inside the first token. **9th cohort plugin with TWO-cap PascalCase D-09 sub-axis** and **6th cohort observation of slug-truncation D-09 sub-axis** — AccuWeather drops 1 trailing token, the **new shortest non-zero token-truncation factor in the cohort**, displacing Symphony's prior record (2 tokens dropped). **D-11 sub-axis:** clean department pass-through → **APPLIED with trailing-pad form** — 2 of 15 unique wire department names padded (`'Facilities '`, `'Information Systems '`); the plugin applies `.trim()` to wire `departments[0].name`. Remaining 13 departments clean (`'AFB Business Development'`, `'Ad Sales'`, `'Data Development'`, `'Digital Media Managers'`, `'Enterprise Solutions'`, `'Executive'`, `'Finance'`, `'Forecasting'`, `'Forensics'`, `'Human Resources'`, `'Integrated Marketing'`, `'Marketing'`, `'Media Content Group - Content'`). **21st cohort plugin to apply D-11**. All other axes share with Tatari: D-04 variant 2 (**75th plugin**), D-08 (**131st member**), D-09 omitted at runtime (wire pass-through; **122nd cohort plugin to omit D-09**), D-10 applied with trailing-pad form (7 of 30 padded ~23.3 % — **new cohort-wide high-watermark pad rate** since SimpliSafe ~14.3 %; all trailing-only — `'Account Executive '`, `'Business Development Leader, Forensic Services '`, `'Chief of Staff, Founder & Executive Chairman's Office '`, `'Creative Writer Intern (Summer 2026) '`, `'Director, Advertising Sales '`, `'Human Resources Intern '`, `'National Multimedia Journalist '`; **80th cohort plugin to apply D-10**). The run-385 probe sampled **30 visible roles**. **164th** Greenhouse-backed company-direct plugin in the catalogue. Sourced from the upstream `OTHERS/Ats-scrapers/ats-companies/greenhouse.csv` corpus (5 004 verified Greenhouse tenants post-PR-#34 cleanup) as the **alphabetically-first** clean non-numeric-slug entry not already represented in `packages/plugins/source-company-*`. |
| shipped    | `source-company-textio`    | `company`  | **Textio** — Textio, Inc. (`textio.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/textio/jobs`). | Spec 174, run #384. **Final (twenty-second) plugin in the tenth fresh probe sweep — sweep closes at run #384.** Thin wrapper following the `source-company-recharge` pattern for trim-axis profile and `source-company-symphony` for the vanity-domain wire URL pass-through pattern. **One structural deviation** off Recharge — D-04 sub-axis: variant 2 → **NEW variant 46 (first cohort observation)** — HTTPS + `www.`-prefixed bare brand-domain `.com` + 2-segment `/careers/apply/` apply-page path **with a trailing slash** + **dual-id query** `?job=<id>&gh_jid=<id>` (vendor-side hand-off shim — Textio's careers front-end reads `job` while Greenhouse forwards `gh_jid`). The **forty-ninth distinct wire-shape variant** in the company-direct cohort (after Symphony variant 45 at Spec 172). Distinct from variant 32 (Betterment) by absence of `careers/apply/` path leaf and presence of dual-id query; distinct from variant 41 (Bird) by `.com` TLD (not `.co`) and 2-segment apply-page path with trailing slash and dual-id query; distinct from variant 44 (Samsara) by `www.`-prefixed apex domain (not subdomain) and 2-segment apply-page path with trailing slash and dual-id query; distinct from variant 45 (Symphony) by `www.` prefix and `careers/apply/` path with trailing slash and dual-id query. All other axes share with Recharge: D-08 (**130th member**), D-09 omitted with case-symmetric bare brand `'Textio'` 6 bytes (**121st cohort plugin to omit D-09**), D-10 omitted (0 of 2 padded — defensive `.trim()` safe no-op; **39th cohort plugin to omit D-10**), D-11 omitted (0 of 2 unique padded across `'Marketing'`, `'General Application'`; **104th cohort plugin** with fully-clean department pass-through). The run-384 probe sampled **2 visible roles** (closely matched the tenth-sweep estimate of ~3 — 1-key under-count, 0.667× ratio). **163rd** Greenhouse-backed company-direct plugin in the catalogue. Selected from the **tenth fresh probe sweep** as the **alphabetically-twenty-second (final) live-board hit** after Alma, Bird, BitGo, Collective Health, DeepMind, Indigo, Instabase, Iterable, Labelbox, Markforged, Maven, Netskope, Postscript, Quanata, Recharge, Samsara, Sezzle, Shopmonkey, SimpliSafe, Symphony, and Tatari. **Tenth-sweep candidate pool exhausted with run #384** — next run launches the eleventh fresh probe sweep. |
| shipped    | `source-company-tatari`    | `company`  | **Tatari** — Tatari, Inc. (`tatari.tv`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/tatari/jobs`). | Spec 173, run #383. **Twenty-first plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-simplisafe` pattern. **One D-09 sub-axis deviation** off SimpliSafe — SimpliSafe D-09 carries TWO-cap PascalCase caps-at-0/6 (`'SimpliSafe'` 10 bytes), whereas Tatari D-09 carries case-symmetric bare brand (`'Tatari'` 6 bytes — fully clean, case-symmetric with the lowercase 6-byte slug `tatari`). All other axes share with SimpliSafe: D-04 variant 2 (**74th plugin**), D-08 (**129th member**), D-09 omitted (**one-hundred-and-twentieth cohort plugin to omit D-09**), D-10 applied with trailing-pad form (3 of 52 padded ~5.8 %; all trailing-only — `'Data Science Analyst '` repeated across three listings — **same wire-title trailing-padded three times** within a single board, a rare repeated-title pad pattern; **seventy-ninth cohort plugin to apply D-10**), D-11 omitted (0 of 7 unique padded across `'Client Development'`, `'Client Services'`, `'Data Science'`, `'Engineering'`, `'Marketing'`, `'Media Buying'`, `'Product Management'`; **one-hundred-and-third cohort plugin** with fully-clean department pass-through). The run-383 probe sampled **52 visible roles** (closely matched the tenth-sweep estimate of ~55 — 3-key under-count, 0.945× ratio). **162nd** Greenhouse-backed company-direct plugin in the catalogue. **Fifty-first near-clean re-spin** in run-history. Selected from the **tenth fresh probe sweep** as the **alphabetically-twenty-first live-board hit**. The remaining 1 live hit queues for run #384 in alphabetical order (`textio` next at run #384 with ~3 keys). |
| shipped    | `source-company-symphony`  | `company`  | **Symphony** — Symphony Communication Services LLC (`symphony.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/symphony/jobs`). | Spec 172, run #382. **Twentieth plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-descope` pattern for the trim-axis profile and `source-company-samsara` for the vanity-domain wire URL pass-through pattern. **Two structural deviations** off Descope: (a) **D-04 wire-shape variant 2 → NEW variant 45 (first cohort observation)** — HTTPS + bare brand-domain `.com` (no `www.`) + 2-segment `/company/apply` apply-page path + query-only `?gh_jid=<id>` form. **Forty-eighth distinct wire-shape variant** in the company-direct cohort. Distinct from variant 13 (ComplyAdvantage) by absence of path-id leaf and absence of duplicating query-id; distinct from variant 32 (Betterment) by absence of `www.` prefix and absence of duplicating query-id; distinct from variant 43 (Netskope) by absence of `www.` prefix and the apply-page path leaf; distinct from variant 44 (Samsara) by absence of `www.` prefix and absence of path-id leaf. (b) **D-09 sub-axis: case-symmetric bare brand → fifth-cohort slug-truncation multi-token corp-suffix descriptive entity wire form** (after Oscar / BEAM / Founders / Fox). Wire `company_name === 'Symphony Communication Services'` (31 bytes — three-token corp-suffix descriptive entity name with two internal ASCII whitespace bytes); slug `symphony` (8 bytes lowercase) matches first wire token only — drops 2 trailing tokens (`Communication Services`). **Symphony drops 2 trailing tokens — the shortest non-zero token-truncation factor in the cohort to date** (Oscar +1, BEAM acronym-expansion, Founders -4, Fox -5). All other axes share with Descope: D-08 (**128th member**), D-09 omitted (**119th cohort plugin to omit D-09**), D-10 omitted (0 of 16 padded — **38th cohort plugin to omit D-10**), D-11 applied with trailing-pad form (1 of 6 unique padded — `'Customer Experience '`; remaining 5 clean — `'Business Operations'`, `'Engineering'`, `'Human Resources'`, `'Product Management'`, `'Sales and Account Management'`; **20th cohort plugin to apply D-11**). The run-382 probe sampled **16 visible roles** (closely matched the tenth-sweep estimate of ~18 — 2-key under-count, 0.89× ratio). **161st** Greenhouse-backed company-direct plugin in the catalogue. Selected from the **tenth fresh probe sweep** as the **alphabetically-twentieth live-board hit**. The remaining 2 live hits queue for runs #383+ in alphabetical order (`tatari` next at run #383 with ~55 keys). |
| shipped    | `source-company-simplisafe`| `company`  | **SimpliSafe** — SimpliSafe Inc. (`simplisafe.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/simplisafe/jobs`). | Spec 171, run #381. **Nineteenth plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-gocardless` pattern. **One D-09 sub-axis deviation** off GoCardless — GoCardless D-09 carries TWO-cap PascalCase caps-at-0/2 (`'GoCardless'` 10 bytes), whereas SimpliSafe D-09 carries TWO-cap PascalCase **caps-at-0/6** (`'SimpliSafe'` 10 bytes — caps at byte indices 0 (`S` vs `s`) and 6 (`S` vs `s`); third cohort plugin with caps-at-0/6 sub-pattern after LaunchDarkly Spec 102 and ComplyAdvantage Spec 141). All other axes share with GoCardless: D-04 variant 2 (**73rd plugin**), D-08 (**127th member**), D-09 omitted (**8th cohort plugin with TWO-cap PascalCase D-09 sub-axis** after SoFi (caps 0/2), StockX (caps 0/5), xAI (caps 0/2 lowercase first), LaunchDarkly (caps 0/6), PagerDuty (caps 0/5), ComplyAdvantage (caps 0/6), and GoCardless (caps 0/2); **one-hundred-and-eighteenth cohort plugin to omit D-09**), D-10 applied with trailing-pad form (6 of 42 padded ~14.3 %; all trailing-only — `'Customer Analytics Manager, Activations & Adoption '` (twice), `'ECommerce Growth Manager '`, `'Product Compliance Specialist '`, `'Senior Automation Engineer (Firmware) '`, `'Staff IAM Engineer '`; **seventy-eighth cohort plugin to apply D-10**), D-11 omitted (0 of 42 padded across 8 unique departments — `'Customer Experience & Monitoring'`, `'Engineering'`, `'Enterprise Info Systems'`, `'Finance'`, `'IT & InfoSec'`, `'Manufacturing Operations & Logistics'`, `'Marketing'`, `'Product'`; **one-hundred-and-second cohort plugin** with fully-clean department pass-through). The run-381 probe sampled **42 visible roles** (closely matched the tenth-sweep estimate of ~38 — 4-key inflation, 1.105× ratio). **160th** Greenhouse-backed company-direct plugin in the catalogue. **Fiftieth near-clean re-spin** in run-history. Selected from the **tenth fresh probe sweep** as the **alphabetically-nineteenth live-board hit**. The remaining 3 live hits queue for runs #382+ in alphabetical order (`symphony` next at run #382 with ~18 keys). |
| shipped    | `source-company-shopmonkey`| `company`  | **Shopmonkey** — Shopmonkey, Inc. (`shopmonkey.io`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/shopmonkey/jobs`). | Spec 170, run #380. **Eighteenth plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-justworks` pattern. **One D-10 sub-axis deviation** off Justworks — Justworks D-10 was applied (5 of 82 padded with first-cohort double-trailing-space pad form), whereas Shopmonkey D-10 is **OMITTED** (0 of 9 wire titles padded — fully-clean title set; the plugin emits `listing.title` byte-for-byte without a `.trim()`). All other axes share with Justworks: D-04 variant 10 — legacy hosted-board apex `boards.greenhouse.io/shopmonkey/jobs/<id>?gh_jid=<id>` (**eighth** plugin in the cohort to use variant 10 after Chime / Faire / Flexport / Braze / Descript / Justworks / Founders), D-08 (126th member), D-09 omitted with case-symmetric `'Shopmonkey'` (10 bytes; **one-hundred-and-seventeenth cohort plugin to omit D-09**), D-10 omitted (**thirty-seventh cohort plugin to omit D-10**), D-11 omitted (0 of 6 unique padded across `'Business Development'`, `'Engineering'`, `'General- DNU'`, `'Implementation'`, `'Product Management'`, `'Sales Development'`; **one-hundred-and-first cohort plugin** with fully-clean department pass-through — **crosses the 101-plugin D-11-omission threshold at this run**). The run-380 probe sampled **9 visible roles** (closely matched the tenth-sweep estimate of ~12 — 3-key under-count, 0.75× ratio). **159th** Greenhouse-backed company-direct plugin in the catalogue. **Forty-ninth near-clean re-spin** in run-history. Selected from the **tenth fresh probe sweep** as the **alphabetically-eighteenth live-board hit**. The remaining 4 live hits queue for runs #381+ in alphabetical order. |
| shipped    | `source-company-sezzle`    | `company`  | **Sezzle** — Sezzle, Inc. (`sezzle.com`; Nasdaq `SEZL`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/sezzle/jobs`). | Spec 169, run #379. **Seventeenth plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-instabase` pattern. **One D-11 sub-axis deviation** off Instabase — Instabase observes a trailing-pad-only D-11 form, whereas Sezzle observes a **mixed form** (3 both-end + 1 leading-only + 1 trailing-only of 11 unique departments) with **three NEW first-cohort sub-observations**: (a) **D-11 both-end pad sub-axis** (`'  CS-Customer Support '`, `'  EX-Executive '`, `'  PR-Product '` — first cohort observation of a wire department name carrying whitespace pads on BOTH ends simultaneously), (b) **D-11 leading-only pad sub-axis** (`'  PO-People Ops'` — first cohort observation of a wire department name carrying leading-only whitespace pad), (c) **multi-character (2-character) leading whitespace pad** (all 3 both-end + 1 leading-only sub-axis observations carry **2-character** leading whitespace — first cohort observation of multi-character leading whitespace pad on a wire department name). The wire-implementation is byte-for-byte identical to Instabase at the `.trim()` boundary because `.trim()` is symmetric over both ends and over multi-character whitespace runs. All other axes share with Instabase: D-04 variant 2 (72nd plugin), D-08 (125th member), D-09 omitted with case-symmetric `'Sezzle'` (6 bytes; **one-hundred-and-sixteenth cohort plugin to omit D-09**), D-10 applied with **mixed pad form (leading + trailing)** (38 of 181 padded ~21.0 %; 6 leading-only + 32 trailing-only; **tenth cohort observation of leading-pad sub-axis** after Chainguard / Oscar / Celonis / Formlabs / GoFundMe / BitGo / Instabase / Iterable / Quanata; **seventy-eighth cohort plugin to apply D-10**), D-11 applied with **NEW MIXED form** (5 of 11 unique padded; **nineteenth cohort plugin to apply D-11**). The run-379 probe sampled **181 visible roles** (closely matches the tenth-sweep estimate of ~180 — 1-key inflation, 1.006× ratio). **158th** Greenhouse-backed company-direct plugin in the catalogue. **Forty-eighth near-clean re-spin** in run-history. Selected from the **tenth fresh probe sweep** as the **alphabetically-seventeenth live-board hit**. The remaining 5 live hits queue for runs #380+ in alphabetical order (`shopmonkey` next at run #380 with ~12 keys). |
| shipped    | `source-company-samsara`   | `company`  | **Samsara** — Samsara, Inc. (`samsara.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/samsara/jobs`). | Spec 168, run #378. **Sixteenth plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-netskope` pattern. **Two structural deviations** from Netskope: (a) **D-04 wire-shape variant 43 → NEW variant 44 (HTTPS `www.samsara.com/company/careers/roles/<id>?gh_jid=<id>` — first cohort observation; forty-seventh distinct wire-shape variant)**. Variant 44 = HTTPS + `www.`-prefix brand-domain on `.com` TLD + 3-segment `/company/careers/roles/<id>` path with **path-id leaf** + duplicating `?gh_jid=<id>` query (dual-id form); distinct from variant 43 (Netskope) by the path-id leaf + duplicating-query-id form (variant 43 is careers-list-page trailing-slash + query-only-id), distinct from all earlier variants by the dual-id form on a `www.`-prefix `.com`-TLD vanity-domain. (b) **D-10 NEW same-title both-pad sub-axis observation** (first cohort observation of a SINGLE title carrying ASCII-space pads on BOTH ends simultaneously). All other axes share with Netskope: D-08 (124th member), D-09 omitted with case-symmetric `'Samsara'` (7 bytes; **one-hundred-and-fifteenth cohort plugin to omit D-09**), D-10 applied with **NEW same-title both-pad + lead-only + trail-only mixed form** (17 of 367 padded ~4.6 %; 3 same-title both-pad — `' Commercial Account Executive '` x3 — **first cohort observation** + 4 leading-only-pad + 10 trailing-only-pad; **seventy-seventh cohort plugin to apply D-10**), D-11 omitted (0 of 367 padded across 44 unique departments — high-cardinality dept set with internal whitespace, ampersands, and slashes; **one-hundredth cohort plugin** with fully-clean department pass-through — **crosses the 100-plugin D-11-omission threshold at this run**). The run-378 probe sampled **367 visible roles** (matched the tenth-sweep estimate exactly — 1× match; **highest-volume tenant in the tenth-sweep pool**, exceeding Netskope's 130 by 2.82×). **157th** Greenhouse-backed company-direct plugin in the catalogue. Selected from the **tenth fresh probe sweep** as the **alphabetically-sixteenth live-board hit**. The remaining 6 live hits queue for runs #379+ in alphabetical order (`sezzle` next at run #379 with ~180 keys). |
| shipped    | `source-company-recharge`  | `company`  | **Recharge** — Recharge Payments, Inc. (`rechargepayments.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/recharge/jobs`). | Spec 167, run #377. **Fifteenth plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-maven` pattern. **Zero structural deviations** — making this the **forty-ninth** clean re-spin. All axes share with Maven: D-04 variant 2 (71st plugin), D-08 (123rd member), D-09 omitted with case-symmetric `'Recharge'` (8 bytes; **one-hundred-and-fourteenth cohort plugin to omit D-09**), D-10 omitted (0 of 4 padded; **thirty-sixth cohort plugin to omit D-10**), D-11 omitted (0 of 4 padded across 4 unique departments — `'Corporate - IT'`, `'Data'`, `'Engineering General'`, `'FP&A'`; **ninety-ninth cohort plugin** with fully-clean department pass-through). The run-377 probe sampled **4 visible roles** (matched the tenth-sweep estimate exactly — 1× match). **156th** Greenhouse-backed company-direct plugin in the catalogue. Selected from the **tenth fresh probe sweep** as the **alphabetically-fifteenth live-board hit**. The remaining 7 live hits queue for runs #378+ in alphabetical order (`samsara` next at run #378 with ~367 keys). |
| shipped    | `source-company-quanata`   | `company`  | **Quanata** — Quanata, LLC (`quanata.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/quanata/jobs`). | Spec 166, run #376. **Fourteenth plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-iterable` pattern. **Zero structural deviations** — making this the **forty-eighth** clean re-spin. All axes share with Iterable: D-04 variant 2 (70th plugin), D-08 (122nd member), D-09 omitted with case-symmetric `'Quanata'` (7 bytes; **one-hundred-and-thirteenth cohort plugin to omit D-09**), D-10 applied with **leading-pad form** (1 of 10 padded ~10 %; leading-only — `' Staff Accountant [Remote-US]'`; **9th cohort observation of leading-pad sub-axis** after Chainguard / Oscar / Celonis / Formlabs / GoFundMe / BitGo / Instabase / Iterable; **seventy-sixth cohort plugin to apply D-10**), D-11 omitted (0 of 10 padded across 6 unique departments — `'Actuarial Operations and Risk Management'`, `'Engineering'`, `'Finance'`, `'Operations'`, `'Security'`, `'Talent Community'`; **ninety-eighth cohort plugin** with fully-clean department pass-through). The run-376 probe sampled **10 visible roles** (matched the tenth-sweep estimate exactly — 1× match). **155th** Greenhouse-backed company-direct plugin in the catalogue. Selected from the **tenth fresh probe sweep** as the **alphabetically-fourteenth live-board hit**. The remaining 8 live hits queue for runs #377+ in alphabetical order (`recharge` next at run #377 with ~4 keys). |
| shipped    | `source-company-cresta`    | `company`  | **Cresta** — Cresta Inc. (`cresta.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/cresta/jobs`). | Spec 165, run #375. Fresh probe-sweep hit (post-tenth-sweep). Thin wrapper following the `source-company-postscript` pattern. **One D-10 sub-axis observation** off Postscript — making this the **forty-seventh near-clean re-spin** in run-history. All other axes share with Postscript: D-04 variant 2 (69th plugin), D-08 (121st member), D-09 omitted with case-symmetric `'Cresta'` (6 bytes; **one-hundred-and-twelfth cohort plugin to omit D-09**), D-10 applied with **trailing-pad-dominant + leading-pad sub-axis** form (30 of 114 trailing-only ~26.3 % + 1 of 114 leading-only ~0.88 %; `.trim()` symmetric — covers both forms identically; **seventy-fifth cohort plugin to apply D-10**), D-11 omitted (0 of 114 padded across 8 unique departments — `'Customer Success'`, `'Engineering'`, `'Finance'`, `'HR'`, `'Marketing'`, `'Product'`, `'Sales'`, `'Technical Operations'`; **ninety-seventh cohort plugin** with fully-clean department pass-through). The run-375 probe sampled **114 visible roles** via direct curl probe. **154th** Greenhouse-backed company-direct plugin in the catalogue. |
| shipped    | `source-company-postscript`| `company`  | **Postscript** — Postscript Inc. (`postscript.io`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/postscript/jobs`). | Spec 164, run #374. **Thirteenth plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-alma` pattern. **Zero structural deviations** — making this the **forty-sixth** clean re-spin. All axes share with Alma: D-04 variant 2 (68th plugin), D-08 (120th member — **crosses the 120-plugin D-08-application threshold at this run**), D-09 omitted with case-symmetric `'Postscript'` (10 bytes; **one-hundred-and-eleventh cohort plugin to omit D-09**), D-10 applied with trailing-pad form (2 of 9 padded ~22.2 %; all trailing-only — `'Senior Customer Success Manager '`, `'Senior Engineering Manager, AI '`; **seventy-fourth cohort plugin to apply D-10**), D-11 omitted (0 of 9 padded across 4 unique departments — `'Business Development'`, `'Customer Success'`, `'Engineering'`, `'Product'`; **ninety-sixth cohort plugin** with fully-clean department pass-through). The run-374 probe sampled **9 visible roles** (matched the tenth-sweep estimate exactly — 1× match). Selected from the **tenth fresh probe sweep** as the **alphabetically-thirteenth live-board hit**. The remaining 9 live hits queue for runs #375+ in alphabetical order (`quanata` next at run #375 with ~10 keys). |
| shipped    | `source-company-netskope`  | `company`  | **Netskope** — Netskope, Inc. (`netskope.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/netskope/jobs`). | Spec 163, run #373. **Twelfth plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-alma` pattern. **One structural deviation** from Alma: **D-04 wire-shape variant 43 (HTTPS `www.netskope.com/company/careers/open-positions/?gh_jid=<id>` — first cohort observation; forty-sixth distinct wire-shape variant)**. Variant 43 = HTTPS + `www.`-prefixed brand-domain on `.com` TLD + 3-segment `/company/careers/open-positions/` path with trailing slash + query-only-id; distinct from Klaviyo variant 19 (1-segment careers path), Bird variant 41 (`.co` TLD), and Collective Health variant 42 (`jobs.` subdomain). All other axes share with Alma: D-08 (119th member), D-09 omitted with case-symmetric `'Netskope'` (8 bytes; **one-hundred-and-tenth cohort plugin to omit D-09 — crosses the 110-plugin D-09-omission threshold at this run**), D-10 applied with trailing-pad form (14 of 130 padded ~10.8 %; all trailing-only — `'Channel Sales Manager '`, `'Director, Regional Sales '`, `'Solutions Architect '`, plus 11 others; **seventy-third cohort plugin to apply D-10**), D-11 omitted (0 of 130 padded across 21 unique departments; **ninety-fifth cohort plugin** with fully-clean department pass-through). The run-373 probe sampled **130 visible roles** (matched the tenth-sweep estimate of ~129 — 1.008× ratio, near-exact 1× match; highest-volume tenant in the tenth-sweep pool). Selected from the **tenth fresh probe sweep** as the **alphabetically-twelfth live-board hit**. The remaining 10 live hits queue for runs #374+ in alphabetical order (`postscript` next at run #374 with ~9 keys). |
| shipped    | `source-company-maven`     | `company`  | **Maven** — Maven Learning, Inc. (`maven.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/maven/jobs`). | Spec 162, run #372. **Eleventh plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-markforged` pattern. **Zero structural deviations** — making this the **forty-fifth** clean re-spin. Distinct from existing Maven Clinic plugin (Spec 076 / `mavenclinic`) — Maven Learning publishes a separate `maven` Greenhouse board. All axes share with Markforged: D-04 variant 2 (67th plugin), D-08 (118th member), D-09 omitted with case-symmetric `'Maven'` (5 bytes; **one-hundred-and-ninth cohort plugin to omit D-09**), D-10 omitted (0 of 5 padded; **thirty-fifth cohort plugin to omit D-10**), D-11 omitted (0 of 5 padded across 4 unique departments — `'Engineering'`, `'Growth'`, `'Operations'`, `'Product'`; **ninety-fourth cohort plugin** with fully-clean department pass-through). The run-372 probe sampled **5 visible roles** (matched the tenth-sweep estimate exactly — 1× match). Selected from the **tenth fresh probe sweep** as the **alphabetically-eleventh live-board hit**. The remaining 11 live hits queue for runs #373+ in alphabetical order (`netskope` next at run #373 with ~129 keys). |
| shipped    | `source-company-markforged`| `company`  | **Markforged** — Markforged Holding Corporation (`markforged.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/markforged/jobs`). | Spec 161, run #371. **Tenth plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-labelbox` pattern. **Zero structural deviations** — making this the **forty-fourth** clean re-spin AND the **150th** Greenhouse-backed company-direct plugin in the catalogue (**crosses the 150-plugin company-direct threshold at this run**). All axes share with Labelbox: D-04 variant 2 (66th plugin), D-08 (117th member), D-09 omitted with case-symmetric `'Markforged'` (10 bytes; **one-hundred-and-eighth cohort plugin to omit D-09**), D-10 omitted (0 of 6 padded; **thirty-fourth cohort plugin to omit D-10**), D-11 omitted (0 of 6 padded across 4 unique departments — `'Application Engineering'`, `'Engineering'`, `'Operations Management'`, `'Sales'`; **ninety-third cohort plugin** with fully-clean department pass-through). The run-371 probe sampled **6 visible roles** (matched the tenth-sweep estimate exactly — 1× match). Selected from the **tenth fresh probe sweep** as the **alphabetically-tenth live-board hit**. The remaining 12 live hits queue for runs #372+ in alphabetical order (`maven` next at run #372 with ~5 keys). |
| shipped    | `source-company-labelbox`  | `company`  | **Labelbox** — Labelbox, Inc. (`labelbox.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/labelbox/jobs`). | Spec 160, run #370. **Ninth plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-indigo` pattern. **Zero structural deviations** — making this the **forty-third** clean re-spin. All axes share with Indigo: D-04 variant 2 (65th plugin), D-08 (116th member), D-09 omitted with case-symmetric `'Labelbox'` (8 bytes; **one-hundred-and-seventh cohort plugin to omit D-09**), D-10 omitted (0 of 10 padded; **thirty-third cohort plugin to omit D-10**), D-11 omitted (0 of 10 padded across 3 unique departments — `'Alignerr Services'`, `'Engineering'`, `'Sales'`; **ninety-second cohort plugin** with fully-clean department pass-through). The run-370 probe sampled **10 visible roles** (matched the tenth-sweep estimate exactly — 1× match). Selected from the **tenth fresh probe sweep** as the **alphabetically-ninth live-board hit**. The remaining 13 live hits queue for runs #371+ in alphabetical order (`markforged` next at run #371 with ~6 keys). |
| shipped    | `source-company-iterable`  | `company`  | **Iterable** — Iterable, Inc. (`iterable.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/iterable/jobs`). | Spec 159, run #369. **Eighth plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-alma` pattern. **One structural deviation** from Alma: D-10 sub-axis (Alma 1/9 trailing-only → Iterable 4/40 mixed-pad with 8th cohort leading-pad observation). All other axes share with Alma: D-04 variant 2 (64th plugin), D-08 (115th member), D-09 omitted with case-symmetric `'Iterable'` (8 bytes; **one-hundred-and-sixth cohort plugin to omit D-09**), D-10 applied with **mixed-pad form + 8th-cohort leading-pad observation** (4 of 40 padded ~10 %; 2 trailing — `'Senior Site Reliability Engineer (Cloud Platform) '`, `'Software Engineer II (Infrastructure) '` + 2 leading — `' FP&A Manager'`, `' Solutions Consultant'`; **8th cohort observation of leading-pad** after Chainguard / Oscar / Celonis / Formlabs / GoFundMe / BitGo / Instabase; **seventy-second cohort plugin to apply D-10**), D-11 omitted (0 of 40 padded across 6 unique departments — `'Engineering'`, `'Finance'`, `'Marketing'`, `'Product'`, `'Sales'`, `'Security & IT'`; **ninety-first cohort plugin** with fully-clean department pass-through — **crosses the 91-plugin D-11-omission threshold at this run**). The run-369 probe sampled **40 visible roles** (matched the tenth-sweep estimate exactly — 1× match). Selected from the **tenth fresh probe sweep** as the **alphabetically-eighth live-board hit**. The remaining 14 live hits queue for runs #370+ in alphabetical order (`labelbox` next at run #370 with ~10 keys). |
| shipped    | `source-company-instabase` | `company`  | **Instabase** — Instabase, Inc. (`instabase.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/instabase/jobs`). | Spec 158, run #368. **Seventh plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-melio` pattern. **Zero structural deviations** — making this the **forty-second** clean re-spin. All axes share with Melio: D-04 variant 2 (63rd plugin), D-08 (114th member), D-09 omitted with case-symmetric `'Instabase'` (9 bytes; **one-hundred-and-fifth cohort plugin to omit D-09**), D-10 applied with **mixed pad form + 7th-cohort leading-pad observation** (2 of 12 padded ~16.7 %; 1 trailing + 1 leading-pad — `' Senior Technical Recruiter'`; **7th cohort observation of leading-pad** after Chainguard / Oscar / Celonis / Formlabs / GoFundMe / BitGo; **seventy-first cohort plugin to apply D-10**), D-11 applied (2 of 8 unique departments padded — `'Finance/Accounting '`, `'Recruiting '`; listing-level 2 of 12 ~16.7 %; **eighteenth cohort plugin to apply D-11**). Notable: `'Finance/Accounting '` dept name contains `/` separator — plugin emits `listing.departments[0].name.trim()` byte-for-byte; downstream rendering / parsing semantics out-of-scope. The run-368 probe sampled **12 visible roles** (matched the tenth-sweep estimate exactly — 1× match). Selected from the **tenth fresh probe sweep** as the **alphabetically-seventh live-board hit**. The remaining 15 live hits queue for runs #369+ in alphabetical order (`iterable` next at run #369 with ~40 keys). |
| shipped    | `source-company-indigo`    | `company`  | **Indigo** — Indigo Agriculture, Inc. (`indigoag.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/indigo/jobs`). | Spec 157, run #367. **Sixth plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-lookout` pattern. **One structural deviation** from Lookout: D-04 sub-axis (variant 20 → variant 2 canonical Greenhouse host). All other axes share with Lookout: D-08 (113th member), D-09 omitted with case-symmetric `'Indigo'` (6 bytes; **one-hundred-and-fourth cohort plugin to omit D-09**), D-10 omitted (0 of 1 padded; **thirty-second cohort plugin to omit D-10**; low-volume sample), D-11 omitted (0 of 1 padded across 1 unique department — `'People'`; **ninetieth cohort plugin** with fully-clean department pass-through — **crosses the 90-plugin D-11-omission threshold at this run**). The run-367 probe sampled **1 visible role** (matched the tenth-sweep estimate exactly — 1× match — lowest-volume tenant in the tenth-sweep pool). Selected from the **tenth fresh probe sweep** as the **alphabetically-sixth live-board hit**. The remaining 16 live hits queue for runs #368+ in alphabetical order (`instabase` next at run #368 with ~12 keys). |
| shipped    | `source-company-deepmind`  | `company`  | **DeepMind** — Google DeepMind (`deepmind.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/deepmind/jobs`). | Spec 156, run #366. **Fifth plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-assemblyai` pattern. **One structural deviation** from AssemblyAI: **D-09 sub-axis** — AssemblyAI THREE-cap PascalCase consecutive-at-tail acronym caps (`AI` at 0/8/9) → DeepMind TWO-cap PascalCase non-consecutive segment-boundary caps (`Deep \| Mind` at 0/4); **9th cohort plugin with TWO-cap PascalCase D-09 sub-axis** after SoFi (caps 0/2), StockX (caps 0/5), xAI (caps 0/2 lowercase first), LaunchDarkly (caps 0/6), PagerDuty (caps 0/5), ComplyAdvantage (caps 0/6), GoCardless (caps 0/2), and BitGo (caps 0/3); **NEW caps-at-0/4 sub-pattern** distinct from all prior TWO-cap PascalCase caps positions; **one-hundred-and-third cohort plugin to omit D-09**. All other axes share with AssemblyAI: D-04 variant 2 (62nd plugin), D-08 (112th member), D-10 applied (9 of 73 padded ~12.3 % trailing-pad form; **seventieth cohort plugin to apply D-10 — crosses the 70-plugin D-10-application threshold at this run**), D-11 applied (1 of 5 unique departments padded — `'Frontier AI '`; listing-level 14 of 73 ~19.2 %; **seventeenth cohort plugin to apply D-11**). **First cohort observation of TWO-cap PascalCase plugin with both D-10 and D-11 applied** — all prior TWO-cap PascalCase plugins applied at most one of D-10 / D-11. The run-366 probe sampled **73 visible roles** (matched the tenth-sweep estimate exactly — 1× match, fifth 1× match in the tenth-sweep). Selected from the **tenth fresh probe sweep** as the **alphabetically-fifth live-board hit** after Alma, Bird, BitGo, and Collective Health. The remaining 17 live hits queue for runs #367+ in alphabetical order (`indigo` next at run #367 with ~1 key). |
| shipped    | `source-company-collectivehealth` | `company` | **Collective Health** — Collective Health, Inc. (`collectivehealth.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/collectivehealth/jobs`). | Spec 155, run #365. **Fourth plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-cribl` pattern. **Two structural deviations** from Cribl: (a) **D-04 wire-shape variant 42 (HTTPS `jobs.collectivehealth.com/apply/?gh_jid=<id>` — first cohort observation; forty-fifth distinct wire-shape variant)**. **First cohort observation of `jobs.` subdomain prefix** — distinct from HelloFresh/Formlabs `careers.` subdomain (variants 26/40) and from `www.` brand-domain variants. (b) **D-09 internal-whitespace asymmetric wire form** (wire `'Collective Health'` 17 bytes — two-token brand with internal ASCII space at byte index 10; case-AND-length-asymmetric vs lowercase 16-byte concatenated slug `collectivehealth`; **9th internal-whitespace D-09 asymmetry case** in the cohort after Scale AI / Maven Clinic / Stitch Fix / New Relic / Dollar Shave Club / Misfits Market / Constant Contact / Modern Health; **one-hundred-and-second cohort plugin to omit D-09**). All other axes share with Cribl: D-08 (111th member), D-10 omitted (0 of 14 padded; **thirty-first cohort plugin to omit D-10**), D-11 omitted (0 of 14 padded across 7 unique departments — `'Client Success'`, `'Contractors'`, `'Engineering'`, `'Financial Planning & Analysis'`, `'Health Plan Operations'`, `'Legal'`, `'Marketing'`; **eighty-ninth cohort plugin** with fully-clean department pass-through). The run-365 probe sampled **14 visible roles** (matched the tenth-sweep estimate exactly — 1× match, fourth 1× match in the tenth-sweep). Selected from the **tenth fresh probe sweep** as the **alphabetically-fourth live-board hit** after Alma, Bird, and BitGo. The remaining 18 live hits queue for runs #366+ in alphabetical order (`deepmind` next at run #366 with ~73 keys). |
| shipped    | `source-company-bitgo`     | `company`  | **BitGo** — BitGo Holdings, Inc. (`bitgo.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/bitgo/jobs`). | Spec 154, run #364. **Third plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-pagerduty` pattern. **Zero structural deviations** — making this the **forty-first** clean re-spin. All axes share with PagerDuty: D-04 variant 2 (61st plugin), D-08 (110th member — **crosses the 110-plugin D-08-application threshold at this run**), D-09 omitted with **TWO-cap PascalCase case-asymmetric wire form** (wire `'BitGo'` 5 bytes; caps at byte indices 0/3 — `B` at 0, `G` at 3; **8th cohort plugin with TWO-cap PascalCase D-09 sub-axis** after SoFi (caps 0/2), StockX (caps 0/5), xAI (caps 0/2), LaunchDarkly (caps 0/6), PagerDuty (caps 0/5), ComplyAdvantage (caps 0/6), and GoCardless (caps 0/2); **NEW caps-at-0/3 sub-pattern** distinct from all prior TWO-cap PascalCase plugins; **one-hundred-and-first cohort plugin to omit D-09**), D-10 applied with **mixed pad form + 2nd-cohort triple-trailing-space + 6th-cohort leading-pad observations** (5 of 47 padded ~10.6 %; 1 trailing + 1 triple-trailing-space — `'Mobile Software Engineer E3 - (React Native)   '` (3 spaces) + 3 leading-pad — `' Senior Director Risk Management'`; **2nd cohort observation of triple-pad form** after Formlabs Spec 147; **6th cohort observation of leading-pad** after Chainguard / Oscar / Celonis / Formlabs / GoFundMe; **sixty-ninth cohort plugin to apply D-10**), D-11 omitted (0 of 47 padded across 11 unique departments — `'Compliance'`, `'Digital Technology'`, `'Engineering'`, `'Finance'`, `'Marketing'`, `'Operations'`, `'Risk'`, `'Sales'`, `'Sales Solutions'`, `'Security'`, `'Trust'`; **eighty-eighth cohort plugin** with fully-clean department pass-through). The run-364 probe sampled **47 visible roles** (matched the tenth-sweep estimate exactly — 1× match). Selected from the **tenth fresh probe sweep** as the **alphabetically-third live-board hit**. The remaining 19 live hits queue for runs #365+ in alphabetical order (`collectivehealth` next at run #365 with ~14 keys). |
| shipped    | `source-company-bird`      | `company`  | **Bird** — Bird Rides, Inc. (`bird.co`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/bird/jobs`). | Spec 153, run #363. **Second plugin in the tenth fresh probe sweep.** Thin wrapper following the `source-company-doximity` pattern. **One structural deviation** from Doximity: **D-04 wire-shape variant 41 (HTTPS `www.bird.co/careers?gh_jid=<id>` — first cohort observation; forty-fourth distinct wire-shape variant)**. Variant 41 = HTTPS + `www.`-prefixed brand-domain on `.co` TLD + `/careers` careers-list-page path (no job-leaf) + query-only-id. **First cohort observation of `.co` TLD on a vanity-domain** — sister to variant 19 (Klaviyo) by `www.`-prefix and query-only-id; distinct by `.co` TLD and serving a careers-list page. All other axes share with Doximity: D-08 (109th member), D-09 omitted with case-symmetric `'Bird'` (4 bytes; **one-hundredth cohort plugin to omit D-09 — crosses the 100-plugin D-09-omission threshold at this run**), D-10 applied (1 of 39 padded ~2.6 % trailing-pad form — `'Vehicle Mechanic - Bronx, NY '`; **sixty-eighth cohort plugin to apply D-10**), D-11 omitted (0 of 39 padded across 2 unique departments — `'Contractor'`, `'Operations'`; **eighty-seventh cohort plugin** with fully-clean department pass-through). The run-363 probe sampled **39 visible roles** (matched the tenth-sweep estimate exactly — 1× match). Selected from the **tenth fresh probe sweep** as the **alphabetically-second live-board hit**. The remaining 20 live hits queue for runs #364+ in alphabetical order (`bitgo` next at run #364 with ~47 keys). |
| shipped    | `source-company-alma`      | `company`  | **Alma** (`helloalma.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/alma/jobs`). | Spec 152, run #362. **First plugin in the TENTH fresh probe sweep.** Thin wrapper following the `source-company-doximity` pattern. **Zero structural deviations** — making this the **fortieth** clean re-spin. All axes share with Doximity: D-04 variant 2 (60th plugin — **crosses the 60-plugin variant-2 threshold at this run**), D-08 (108th member), D-09 omitted with case-symmetric `'Alma'` (4 bytes; **ninety-ninth cohort plugin to omit D-09**), D-10 applied (1 of 9 padded ~11.1 % trailing-pad form — `'Senior Data Scientist '`; **sixty-seventh cohort plugin to apply D-10**), D-11 omitted (0 of 9 padded across 6 unique departments — `'Business Intelligence'`, `'Clinical Operations'`, `'Core Ops'`, `'Customer Experience'`, `'Data Science'`, `'Engineering'`; **eighty-sixth cohort plugin** with fully-clean department pass-through). The run-362 probe sampled **9 visible roles** (matched the tenth-sweep estimate exactly — 1× match). **Run #362 launches the TENTH fresh probe sweep** — probed ~150 candidate slugs and found **22 fresh non-empty live hits**: `alma` (9, this row), `bird` (~39), `bitgo` (~47), `collectivehealth` (~14), `deepmind` (~73), `indigo` (~1), `instabase` (~12), `iterable` (~40), `labelbox` (~10), `markforged` (~6), `maven` (~5), `netskope` (~129), `postscript` (~9), `quanata` (~10), `recharge` (~4), `samsara` (~367), `sezzle` (~180), `shopmonkey` (~12), `simplisafe` (~38), `symphony` (~18), `tatari` (~55), `textio` (~3). The remaining 21 live hits queue for runs #363+ in alphabetical order (`bird` next at run #363 with ~39 keys). |
| shipped    | `source-company-gofundme`  | `company`  | **GoFundMe** — GoFundMe, Inc. (`gofundme.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/gofundme/jobs`). | Spec 151, run #361. **Seventeenth and last plugin in the ninth fresh probe sweep — sweep fully exhausted at this run.** Thin wrapper following the `source-company-assemblyai` pattern. **One structural deviation** from AssemblyAI: **D-09 omitted with FIRST-COHORT NON-consecutive segment-boundary THREE-cap PascalCase case-asymmetric wire form** (wire `'GoFundMe'` 8 bytes; caps at byte indices 0/2/6 — `Go ǀ Fund ǀ Me` segment boundaries; **first cohort observation of NON-consecutive segment-boundary THREE-cap PascalCase D-09 sub-axis** — distinct from prior consecutive-at-tail acronym forms in AssemblyAI (caps 0/8/9 forming `AI`) and BigID (caps 0/3/4 forming `ID`); **3rd THREE-cap PascalCase plugin overall** in the cohort; **ninety-eighth cohort plugin to omit D-09**). All other axes share with AssemblyAI: D-04 variant 2 (59th plugin), D-08 (107th member), D-10 applied with **fifth-cohort leading-pad observation** (5 of 47 padded ~10.6 %; 3 trailing-pad + 2 leading-pad — `' Privacy Program Manager'`, `' Staff Software Engineer'`; fifth observation after Chainguard / Oscar / Celonis / Formlabs; **sixty-sixth cohort plugin to apply D-10**), D-11 applied (1 of 13 unique departments padded — `'Technical Solutions & Partnerships '`; listing-level 1 of 47 ~2.1 %; **sixteenth cohort plugin to apply D-11**). The run-361 probe sampled **47 visible roles** (estimate ~36 keys; ~1.31× ratio — near-1× match). **Run #361 closes out the ninth fresh probe sweep** — the run-345 candidate pool of 17 has now produced 17 shipped plugins. **The tenth fresh probe sweep launches at run #362+.** Selected from the **ninth fresh probe sweep** as the **alphabetically-seventeenth and final live-board hit**. |
| shipped    | `source-company-gocardless`| `company`  | **GoCardless** — GoCardless Ltd. (`gocardless.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/gocardless/jobs`). | Spec 150, run #360. **Sixteenth plugin in the ninth fresh probe sweep.** Thin wrapper following the `source-company-pagerduty` pattern. **Zero structural deviations** — making this the **thirty-ninth** clean re-spin. All axes share with PagerDuty: D-04 variant 2 (58th plugin), D-08 (106th member), D-09 omitted with **TWO-cap PascalCase case-asymmetric wire form** (wire `'GoCardless'` 10 bytes; caps at byte indices 0/2; **7th cohort plugin with TWO-cap PascalCase D-09 sub-axis** after SoFi / StockX / xAI / LaunchDarkly / PagerDuty / ComplyAdvantage; **caps-at-0/2 matches SoFi and xAI exactly** — third cohort plugin with caps-at-0/2 sub-pattern, distinct from xAI by being uppercase-first; **ninety-seventh cohort plugin to omit D-09**), D-10 applied (1 of 41 padded ~2.4 % trailing-pad form — `'Site Reliability Engineer '`; **sixty-fifth cohort plugin to apply D-10**), D-11 omitted (0 of 41 padded across 6 unique departments — `'Customer'`, `'Marketing'`, `'People'`, `'Product Development'`, `'Risk'`, `'Sales'`; **eighty-fifth cohort plugin** with fully-clean department pass-through). The run-360 probe sampled **41 visible roles** (estimate ~36 keys; ~1.14× ratio — near-1× match). Selected from the **ninth fresh probe sweep** as the **alphabetically-sixteenth live-board hit**. The remaining 1 live hit queues for run #361 (`gofundme` — **last ninth-sweep candidate**; **tenth fresh probe sweep** launches at run #362 after GoFundMe closes the ninth-sweep). |
| shipped    | `source-company-fox`       | `company`  | **Fox Creek Veterinary Hospital - Wildwood** (`foxcreekvet.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/fox/jobs`). | Spec 149, run #359. **Fifteenth plugin in the ninth fresh probe sweep.** Thin wrapper following the `source-company-beam` pattern. **Zero structural deviations** — making this the **thirty-eighth** clean re-spin. All axes share with BEAM: D-04 variant 2 (57th plugin), D-08 (105th member), D-09 omitted with **FOURTH-COHORT slug-truncation asymmetric wire form** (wire `'Fox Creek Veterinary Hospital - Wildwood'` 40 bytes — 6-token legal entity name; slug `fox` 3 bytes — matches first token only, truncates 5 trailing tokens; **fourth cohort observation** after Oscar / BEAM / Founders; **Fox has the NEW largest slug-token-truncation factor in cohort to date** — 5 tokens dropped beyond slug, exceeding Founders's prior record of 4; **ninety-sixth cohort plugin to omit D-09**), D-10 omitted (0 of 5 padded; **thirty-seventh cohort plugin to omit D-10**), D-11 omitted (0 of 5 padded across 4 unique departments — `'Externships'`, `'Reception'`, `'Technicians'`, `'Veterinary Doctors'`; **eighty-fourth cohort plugin** with fully-clean department pass-through). The run-359 probe sampled **5 visible roles** (estimate ~10 keys; ~0.5× ratio under-count). Selected from the **ninth fresh probe sweep** as the **alphabetically-fifteenth live-board hit**. The remaining 2 live hits queue for runs #360+ in alphabetical order (`gocardless` next at run #360 with ~36 keys). |
| shipped    | `source-company-founders`  | `company`  | **Founders Green Animal Hospital** (`foundersgreenanimalhospital.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/founders/jobs`). | Spec 148, run #358. **Fourteenth plugin in the ninth fresh probe sweep.** Thin wrapper following the `source-company-beam` pattern. **One structural deviation** from BEAM: **D-04 sub-axis** (variant 2 canonical Greenhouse host → variant 10 legacy hosted-board apex `boards.greenhouse.io/founders/jobs/<id>?gh_jid=<id>`; **seventh** variant-10 plugin in the cohort after Chime, Faire, Flexport, Braze, Descript, and Justworks). All other axes share with BEAM: D-08 (104th member), D-09 omitted with **THIRD-COHORT slug-truncation asymmetric wire form** — wire `company_name === 'Founders Green Animal Hospital'` (30 bytes — FOUR-token legal-entity name) vs slug `founders` (8 bytes — matches first token only; truncates 3 trailing tokens `Green Animal Hospital`); third cohort observation of slug-truncation sub-axis after Oscar (Spec 133 — slug-extra-word, 1 token added) and BEAM (Spec 136 — slug-acronym-expansion); Founders has the **largest slug-token-truncation factor in cohort to date** (4 tokens dropped beyond slug); **ninety-fifth cohort plugin to omit D-09**), D-10 omitted (0 of 3 padded; **twenty-ninth cohort plugin to omit D-10**), D-11 omitted (0 of 3 padded across 3 unique departments — `'Assistants'`, `'Reception'`, `'Technicians'`; **eighty-third cohort plugin** with fully-clean department pass-through). The run-358 probe sampled **3 visible roles** (estimate ~6 keys; ~0.5× ratio under-count). Selected from the **ninth fresh probe sweep** as the **alphabetically-fourteenth live-board hit**. The remaining 3 live hits queue for runs #359+ in alphabetical order (`fox` next at run #359 with ~10 keys). |
| shipped    | `source-company-formlabs`  | `company`  | **Formlabs** — Formlabs, Inc. (`formlabs.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/formlabs/jobs`). | Spec 147, run #357. **Thirteenth plugin in the ninth fresh probe sweep.** Thin wrapper following the `source-company-doximity` pattern. **One structural deviation** from Doximity: **D-04 wire-shape variant 40 (HTTPS `careers.formlabs.com/job/<id>/apply/?gh_jid=<id>` — first cohort observation; forty-third distinct wire-shape variant)**. Variant 40 = HTTPS + careers-subdomain (`careers.formlabs.com`) + `/job/<id>/apply/` (singular leaf with `/apply/` action segment trailing the path-id, with trailing slash) + dual-id (path-id + query-id). Sister to variant 26 (HelloFresh) by careers-subdomain prefix and to variant 28 (SoFi) by dual-id form, distinct from both by the `/apply/` action-segment trailing the path-id. All other axes share with Doximity: D-08 (103rd member), D-09 omitted with case-symmetric `'Formlabs'` (8 bytes; **ninety-fourth cohort plugin to omit D-09**), D-10 applied with **FIRST-COHORT triple-trailing-space pad observation + fourth-cohort leading-pad observation** — 13 of 189 padded ~6.9 % (1 sample carries 3 ASCII trailing spaces — `'Robotic Systems Integration Engineer (SLA & SLS)   '` — distinct from Justworks (Spec 129) double-pad; 1 sample carries leading-pad — `' 3D Print Optimization Engineer'` — fourth observation after Chainguard / Oscar / Celonis); `.trim()` byte-count agnostic; **sixty-fourth cohort plugin to apply D-10**; D-11 omitted (0 of 189 padded across 16 unique departments — `'Customer Strategy & Operations'`, `'Finance'`, `'Form Now'`, `'Global Marketing'`, `'Global Sales'`, `'Global Services'`, `'Hardware Engineering'`, `'Legal'`, `'Manufacturing'`, `'Materials Engineering'`, `'Operations'`, `'People & Culture'`, `'Product'`, `'Software Engineering'`, `'Spectra'`, `'Systems'`; **eighty-second cohort plugin** with fully-clean department pass-through). The run-357 probe sampled **189 visible roles** (estimate ~95 keys; ~1.99× ratio over-count). Selected from the **ninth fresh probe sweep** as the **alphabetically-thirteenth live-board hit**. The remaining 4 live hits queue for runs #358+ in alphabetical order (`founders` next at run #358 with ~6 keys). |
| shipped    | `source-company-fairmarkit`| `company`  | **Fairmarkit** — Fairmarkit, Inc. (`fairmarkit.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/fairmarkit/jobs`). | Spec 146, run #356. **Twelfth plugin in the ninth fresh probe sweep.** Thin wrapper following the `source-company-melio` pattern. **Zero structural deviations** — making this the **thirty-seventh** clean re-spin. All axes share with Melio: D-04 variant 2 (56th plugin), D-08 (102nd member), D-09 omitted with case-symmetric `'Fairmarkit'` (10 bytes; **ninety-third cohort plugin to omit D-09**), D-10 applied with **FIRST-COHORT mojibake-Cyrillic-Es leading-residue observation** — 5 of 12 padded ~41.7 % trailing-pad form; 1 of the 5 carries leading byte sequence `c3 90 c2 a1` (`'Сore Software Architect '` — wire-side double-UTF-8-encoded U+0421 Cyrillic Capital Es `С`, visual homograph of Latin `C`); distinct from prior mojibake-NBSP trailing form (Bloomreach Spec 139, ExpressVPN Spec 145); `.trim()` strips trailing ASCII space, leading mojibake bytes preserved by-design (not whitespace) — wire-faithful; **sixty-third cohort plugin to apply D-10**; D-11 applied with trailing-pad form (2 of 7 unique departments padded — `'Customer Success - Services '`, `'International Operations '`; listing-level 2 of 12 ~16.7 %; **fifteenth cohort plugin to apply D-11**). The run-356 probe sampled **12 visible roles** (matched the ninth-sweep estimate exactly — **1× exact match**, fifth 1× match in ninth-sweep after BEAM, BigID, ComplyAdvantage, and Conviva). Selected from the **ninth fresh probe sweep** as the **alphabetically-twelfth live-board hit**. The remaining 5 live hits queue for runs #357+ in alphabetical order (`formlabs` next at run #357 with ~95 keys). |
| shipped    | `source-company-expressvpn`| `company`  | **ExpressVPN** — Express Technologies Ltd. (`expressvpn.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/expressvpn/jobs`). | Spec 145, run #355. **Eleventh plugin in the ninth fresh probe sweep.** Thin wrapper following the `source-company-pagerduty` pattern. **One structural deviation** from PagerDuty: **D-09 omitted with FIRST-COHORT FOUR-cap PascalCase case-asymmetric wire form** — wire `'ExpressVPN'` byte-for-byte (10 bytes; case-asymmetric vs slug `expressvpn` at FOUR byte indices 0/7/8/9 — caps at 7-8-9 form the embedded 3-letter acronym `VPN` at the tail; **first cohort observation of FOUR-cap PascalCase D-09 sub-axis** — distinct from prior TWO-cap forms (SoFi caps 0/2, StockX 0/5, xAI 0/2 lowercase first, LaunchDarkly 0/6, PagerDuty 0/5, ComplyAdvantage 0/6) and prior THREE-cap forms (AssemblyAI 0/8/9 forming `AI`, BigID 0/3/4 forming `ID`); ExpressVPN's caps form a 3-letter acronym `VPN` rather than 2-letter; **ninth PascalCase case-asymmetric plugin overall** in the cohort; **ninety-second cohort plugin to omit D-09**). All other axes share with PagerDuty: D-04 variant 2 (55th plugin), D-08 (101st member), D-10 applied with **second cohort observation of mojibake-NBSP D-10 trailing-pad sub-axis** after Bloomreach (Spec 139) — 3 of 29 padded ~10.3 % trailing-pad form; 1 of the 3 carries `c3 82 c2 a0` byte sequence (wire-side double-UTF-8-encoded U+00A0 NBSP); `.trim()` strips trailing NBSP, residual mojibake `Â` byte preserved by-design — wire-faithful; **sixty-second cohort plugin to apply D-10**; D-11 omitted (0 of 29 padded across 7 unique departments — `'Business Operations'`, `'Data Engineering & Insights'`, `'Design'`, `'Engineering'`, `'Marketing'`, `'Product Marketing'`, `'Security'`; **eighty-first cohort plugin** with fully-clean department pass-through). The run-355 probe sampled **29 visible roles** (estimate ~32 keys; ~0.91× ratio — near-1× match). Selected from the **ninth fresh probe sweep** as the **alphabetically-eleventh live-board hit**. The remaining 6 live hits queue for runs #356+ in alphabetical order (`fairmarkit` next at run #356 with ~12 keys). |
| shipped    | `source-company-earnest`   | `company`  | **Earnest** — Earnest Operations LLC (`earnest.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/earnest/jobs`). | Spec 144, run #354. **Tenth plugin in the ninth fresh probe sweep.** Thin wrapper following the `source-company-melio` pattern. **One structural deviation** from Melio: **D-04 wire-shape variant 39 (HTTPS `app.careerpuck.com/job-board/earnest/job/<id>?gh_jid=<id>` — first cohort observation; forty-second distinct wire-shape variant)**. **First cohort observation of third-party careers-proxy host as a wire-shape variant** — every prior 41 variants used either Greenhouse-controlled hosts or brand-vanity-domains; CareerPuck (`app.careerpuck.com`) is a careers-proxy SaaS distinct from Greenhouse / brand-vanity-domains / `boards.greenhouse.io` legacy apex. All other axes share with Melio: D-08 (**100th member — crosses the 100-plugin D-08-application threshold at this run**), D-09 omitted with case-symmetric `'Earnest'` (7 bytes; **ninety-first cohort plugin to omit D-09**), D-10 applied (1 of 11 padded ~9.1 % trailing-pad form — `'Director of Collections '`; **sixty-first cohort plugin to apply D-10**), D-11 applied (1 of 7 unique departments padded — `'Engineering '`; listing-level pad rate 1 of 11 ~9.1 %; **fourteenth cohort plugin to apply D-11**). The run-354 probe sampled **11 visible roles** (estimate ~33 keys; ~0.33× ratio under-count — **largest ninth-sweep under-count factor**). Selected from the **ninth fresh probe sweep** as the **alphabetically-tenth live-board hit**. The remaining 7 live hits queue for runs #355+ in alphabetical order (`expressvpn` next at run #355 with ~32 keys). |
| shipped    | `source-company-cribl`     | `company`  | **Cribl** — Cribl, Inc. (`cribl.io`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/cribl/jobs`). | Spec 143, run #353. **Ninth plugin in the ninth fresh probe sweep.** Thin wrapper following the `source-company-lookout` pattern. **One structural deviation** from Lookout: **D-04 wire-shape variant 38 (HTTPS `cribl.io/job-detail/?gh_jid=<id>` — first cohort observation; forty-first distinct wire-shape variant)**. **First cohort observation of `.io` TLD on a vanity-domain** (all 40 prior variants used `.com` or legacy `boards.greenhouse.io` apex). **First cohort observation of NO-`/careers/` ancestor on a vanity-domain** (every prior bare-brand vanity-domain variant included `/careers/` in the path). All other axes share with Lookout: D-08 (99th member), D-09 omitted with case-symmetric `'Cribl'` (5 bytes; **ninetieth cohort plugin to omit D-09 — crosses the 90-plugin D-09-omission threshold at this run**), D-10 omitted (0 of 51 padded; **twenty-eighth cohort plugin to omit D-10**), D-11 omitted (0 of 51 padded across 9 unique departments — `'Customer Experience'`, `'Engineering'`, `'Finance'`, `'IT & Security'`, `'Marketing'`, `'Operations'`, `'People'`, `'Sales'`, `'Support'`; **eightieth cohort plugin** with fully-clean department pass-through — **crosses the 80-plugin D-11-omission threshold at this run**). The run-353 probe sampled **51 visible roles** (estimate ~33 keys; ~1.55× ratio over-count). Selected from the **ninth fresh probe sweep** as the **alphabetically-ninth live-board hit**. The remaining 8 live hits queue for runs #354+ in alphabetical order (`earnest` next at run #354 with ~33 keys). |
| shipped    | `source-company-conviva`   | `company`  | **Conviva** — Conviva, Inc. (`conviva.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/conviva/jobs`). | Spec 142, run #352. **Eighth plugin in the ninth fresh probe sweep.** Thin wrapper following the `source-company-lookout` pattern. **One structural deviation** from Lookout: **D-04 wire-shape variant 37 (HTTPS `www.conviva.com/careers/job/<id>?gh_jid=<id>` — first cohort observation; fortieth distinct wire-shape variant)**. Variant 37 combines `www.`-prefix + singular-leaf + path-id + dual-id form for the first time; sister to variant 19 (Klaviyo / Fivetran / Amplitude) by `www.`-prefix and singular-leaf base, sister to variant 28 (SoFi) by singular-leaf + id-in-path + dual-id. All other axes share with Lookout: D-08 (98th member), D-09 omitted with case-symmetric `'Conviva'` (7 bytes; **eighty-ninth cohort plugin to omit D-09**), D-10 omitted (0 of 9 padded; **twenty-seventh cohort plugin to omit D-10**), D-11 omitted (0 of 9 padded across 5 unique departments — `'Customer Support'`, `'Finance'`, `'Product Management'`, `'Sales'`, `'Technical Solutions'`; **seventy-ninth cohort plugin** with fully-clean department pass-through). The run-352 probe sampled **9 visible roles** (matched the ninth-sweep estimate exactly — 1× match, **fourth 1× match in ninth-sweep** after BEAM, BigID, and ComplyAdvantage). Selected from the **ninth fresh probe sweep** as the **alphabetically-eighth live-board hit**. The remaining 9 live hits queue for runs #353+ in alphabetical order (`cribl` next at run #353 with ~33 keys). |
| shipped    | `source-company-complyadvantage` | `company` | **ComplyAdvantage** — ComplyAdvantage Ltd. (`complyadvantage.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/complyadvantage/jobs`). | Spec 141, run #351. **Seventh plugin in the ninth fresh probe sweep.** Thin wrapper following the `source-company-epicgames` pattern. **One structural deviation** from Epic Games: **D-09 omitted with TWO-cap PascalCase case-asymmetric wire form** (Epic Games multi-token bare-brand `'Epic Games'` with internal whitespace → ComplyAdvantage TWO-cap PascalCase concatenated `'ComplyAdvantage'` 15 bytes; case-asymmetric vs slug `complyadvantage` at byte indices 0/6 — caps form embedded `Advantage` word boundary; **sixth cohort observation of TWO-cap PascalCase D-09 sub-axis** after SoFi (caps 0/2), StockX (caps 0/5), xAI (caps 0/2 with lowercase first letter), LaunchDarkly (caps 0/6), and PagerDuty (caps 0/5); **caps-at-0/6 matches LaunchDarkly exactly** — second cohort plugin with this caps-position sub-pattern; **eighty-eighth cohort plugin to omit D-09**). All other axes share with Epic Games: D-04 variant 13 (sixth plugin — second-direct cohort re-use of Epic Games's `<brand-domain>.com/careers/jobs/<id>?gh_jid=<id>` bare-domain dual-id form), D-08 (97th member), D-10 applied (8 of 33 padded ~24.2 % trailing-pad form — `'Data Scientist '`, `'Senior Director, Revenue Operations '` (twice across two listings), plus 6 others; **sixtieth cohort plugin to apply D-10**), D-11 omitted (0 of 33 padded across 5 unique departments — `'Commercial'`, `'Technology'`, `'Finance'`, `'Marketing'`, `'Product'`; **seventy-eighth cohort plugin** with fully-clean department pass-through). The run-351 probe sampled **33 visible roles** (matched the ninth-sweep estimate of ~33 keys exactly — **1× exact match**, third 1× match in ninth-sweep after BEAM and BigID). Selected from the **ninth fresh probe sweep** as the **alphabetically-seventh live-board hit**. The remaining 10 live hits queue for runs #352+ in alphabetical order (`conviva` next at run #352 with ~9 keys). |
| shipped    | `source-company-celonis`   | `company`  | **Celonis** — Celonis SE (`celonis.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/celonis/jobs`). | Spec 140, run #350. **Sixth plugin in the ninth fresh probe sweep.** Thin wrapper following the `source-company-doximity` pattern. **Zero structural deviations** — making this the **thirty-sixth** clean re-spin. All axes share with Doximity: D-04 variant 2 (54th plugin), D-08 (96th member), D-09 omitted with case-symmetric `'Celonis'` (7 bytes; **eighty-seventh cohort plugin to omit D-09**), D-10 applied with **THIRD-COHORT leading-pad observation + FIRST-COHORT mixed-leading-trailing ratio observation** — 26 of 188 padded ~13.8 % (23 trailing + 3 leading in same plugin); third cohort observation of leading-pad sub-axis after Chainguard (Spec 122 — leading-only, first observation) and Oscar (Spec 133 — 1 leading + 1 trailing at ~0.81 %); **first cohort plugin to observe leading-pad at meaningful volume** (3 leading samples — `' Field CTO'`, `' Principal Enterprise Architect (public sector)'`, `' Salesforce Software Engineer '` — last is dual-padded leading + trailing); `.trim()` covers both directions transparently; **fifty-ninth cohort plugin to apply D-10**; D-11 omitted (0 of 188 padded across 12 unique departments — `'Sales'`, `'Sales & Partner Mgmt'`, `'Value Engineering'`, `'Business Development'`, `'Services'`, `'Engineering'`, `'Corporate'`, `'IT & Systems'`, `'Product'`, `'Ecosystem'`, `'Product Business Apps'`, `'Legal'`; **seventy-seventh cohort plugin** with fully-clean department pass-through). The run-350 probe sampled **188 visible roles** (estimate ~154 keys; ~1.22× ratio — over-count). Selected from the **ninth fresh probe sweep** as the **alphabetically-sixth live-board hit**. The remaining 11 live hits queue for runs #351+ in alphabetical order (`complyadvantage` next at run #351 with ~33 keys). |
| shipped    | `source-company-bloomreach`| `company`  | **Bloomreach** (`bloomreach.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/bloomreach/jobs`). | Spec 139, run #349. **Fifth plugin in the ninth fresh probe sweep.** Thin wrapper following the `source-company-doximity` pattern. **Zero structural deviations** — making this the **thirty-fifth** clean re-spin. All axes share with Doximity: D-04 variant 2 (53rd plugin), D-08 (95th member), D-09 omitted with case-symmetric `'Bloomreach'` (10 bytes; **eighty-sixth cohort plugin to omit D-09**), D-10 applied with **FIRST-COHORT mojibake-NBSP trailing-pad sub-axis observation** — 10 of 78 padded ~12.8 % trailing-pad form; 1 of the 10 carries a mojibake-double-encoded NBSP byte sequence (`c3 82 c2 a0` — wire-side double-UTF-8-encoded U+00A0 NBSP); JavaScript `.trim()` strips the trailing NBSP (U+00A0 is in `WhiteSpace`), the residual mojibake `Â` (U+00C2) byte remains by-design — wire-faithful; **first cohort observation of mojibake-NBSP pad form** across 57 prior D-10-applying plugins; **fifty-eighth cohort plugin to apply D-10**; D-11 omitted (0 of 76 padded across 8 unique departments — `'Engineering'`, `'G&A - FLS'`, `'G&A - GIST'`, `'G&A - People'`, `'Marketing'`, `'Operations'`, `'Product'`, `'Revenue'`; **seventy-sixth cohort plugin** with fully-clean department pass-through). The run-349 probe sampled **78 visible roles** (estimate ~62 keys; ~0.79× ratio — under-count). Selected from the **ninth fresh probe sweep** as the **alphabetically-fifth live-board hit**. The remaining 12 live hits queue for runs #350+ in alphabetical order (`celonis` next at run #350 with ~154 keys). |
| shipped    | `source-company-blend`     | `company`  | **Blend** — Blend Labs, Inc. (`blend.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/blend/jobs`). | Spec 138, run #348. **Fourth plugin in the ninth fresh probe sweep.** Thin wrapper following the `source-company-doximity` pattern. **Zero structural deviations** — making this the **thirty-fourth** clean re-spin. All axes share with Doximity: D-04 variant 2 (52nd plugin), D-08 (94th member), D-09 omitted with case-symmetric `'Blend'` (5 bytes; **eighty-fifth cohort plugin to omit D-09**), D-10 applied (1 of 7 padded ~14.3 % trailing-pad form `'Customer Success Manager '`; **fifty-seventh cohort plugin to apply D-10**), D-11 omitted with **FIRST-COHORT company-suffix dept naming sub-axis observation** — 0 of 7 padded across 4 unique department names (`'Customer Success- Blend Labs'`, `'Growth Team- Blend Labs'`, `'Relationship Management - Blend Labs'`, `'Sales Engineering- Blend Labs'`) — but **all 4 follow a `<dept-name>- Blend Labs` company-suffix convention**; every dept name carries the trailing `- Blend Labs` legal-entity suffix; **first cohort observation of company-suffix dept naming** across 74 prior D-11-omitting plugins; preserves byte-for-byte; **seventy-fifth cohort plugin** with fully-clean department pass-through. The run-348 probe sampled **7 visible roles** (matched the ninth-sweep estimate of ~14 keys at ~0.5× ratio — under-count). Selected from the **ninth fresh probe sweep** as the **alphabetically-fourth live-board hit**. The remaining 13 live hits queue for runs #349+ in alphabetical order (`bloomreach` next at run #349 with ~62 keys). |
| shipped    | `source-company-bigid`     | `company`  | **BigID** (`bigid.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/bigid/jobs`). | Spec 137, run #347. **Third plugin in the ninth fresh probe sweep.** Thin wrapper following the `source-company-assemblyai` pattern. **One structural deviation** from AssemblyAI: **D-04 wire-shape variant 36 (HTTPS bare brand-domain `bigid.com/company/careers/job-details/<id>?gh_jid=<id>` — first cohort observation; thirty-ninth distinct wire-shape variant)**. Variant 36 is sister to Okta's variant 31 (HTTPS `www.okta.com/company/careers/opportunity/<id>?gh_jid=<id>`) but with bare brand-domain (no `www.` prefix) and `/job-details/<id>` leaf (vs Okta's `opportunity/<id>`); **second cohort observation of `/company/careers/<leaf>/<id>` path-prefix** after Okta. All other axes share with AssemblyAI: D-08 (93rd member), D-09 omitted with **PascalCase THREE-cap case-asymmetric wire form** (wire `'BigID'` 5 bytes; case-asymmetric vs slug `bigid` at THREE byte indices 0/3/4 — caps form embedded `ID` initialism; **second cohort observation of THREE-cap PascalCase D-09 sub-axis** after AssemblyAI's caps 0/8/9 forming embedded `AI`; **eighty-fourth cohort plugin to omit D-09**), D-10 applied (2 of 30 padded ~6.7 %; **fifty-sixth cohort plugin to apply D-10**), D-11 applied (2 of 10 unique dept names padded — `'Sales Development '`, `'Solutions Engineering '`; listing-level pad rate 9 of 30 ~30 %; **thirteenth cohort plugin to apply D-11**). The run-347 probe sampled **30 visible roles** (matched the ninth-sweep estimate exactly — 1× inflation, second 1× match in ninth-sweep after BEAM). Selected from the **ninth fresh probe sweep** as the **alphabetically-third live-board hit**. The remaining 14 live hits queue for runs #348+ in alphabetical order (`blend` next at run #348 with ~14 keys). |
| shipped    | `source-company-beam`      | `company`  | **BEAM** — Bridge to Enter Advanced Mathematics (`beammath.org`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/beam/jobs`). | Spec 136, run #346. **Second plugin in the ninth fresh probe sweep.** Thin wrapper following the `source-company-branch` pattern. **One structural deviation** from Branch: **D-09 first-cohort slug-acronym-expansion asymmetric wire form** — slug `beam` (4 bytes — the acronym only) vs wire `'Bridge to Enter Advanced Mathematics (BEAM)'` (43 bytes — full org name + acronym in parens); distinct from Oscar's slug-extra-word asymmetry (Oscar appended ONE word; BEAM fully expands the acronym); **second cohort observation of slug-truncation D-09 sub-axis** after Oscar; **eighty-third cohort plugin to omit D-09**. All other axes share with Branch: D-04 variant 2 (51st plugin), D-08 (92nd member), D-10 omitted (0 of 14 padded; **twenty-sixth cohort plugin to omit D-10**), D-11 omitted (0 of 14 padded across 4 unique departments — `'HQ Central Programs'`, `'HQ Fundraising'`, `'Summer Programs'`, `'Yearround LA'`; **seventy-fourth cohort plugin** with fully-clean department pass-through). The run-346 probe sampled **14 visible roles** (matched the ninth-sweep estimate of ~14 keys exactly — 1× inflation, **first 1× match in ninth-sweep**). Selected from the **ninth fresh probe sweep** as the **alphabetically-second live-board hit**. The remaining 15 live hits queue for runs #347+ in alphabetical order (`bigid` next at run #347 with ~30 keys). |
| shipped    | `source-company-axon`      | `company`  | **Axon** (`axon.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/axon/jobs`). | Spec 135, run #345. **First plugin in the NINTH fresh probe sweep.** Thin wrapper following the `source-company-doximity` pattern. **Zero structural deviations** — making this the **thirty-third** clean re-spin. All axes share with Doximity: D-04 variant 2 (50th plugin — **crosses the 50-plugin variant-2 threshold at this run**), D-08 (91st member), D-09 omitted with case-symmetric `'Axon'` (4 bytes; **eighty-second cohort plugin to omit D-09**), D-10 applied (90 of 539 padded ~16.7 %; **fifty-fifth cohort plugin to apply D-10**), D-11 omitted (0 of 539 padded across 85 unique departments — all `<numeric_code> <name>` numeric-prefix-with-space convention; **second cohort observation** of this convention after Constant Contact (Spec 111); **first-cohort observation of internal-double-whitespace anomaly in dept-name field** — `'1105 SCM - Distribution &  Warehousing - Skybridge'` carries 2 consecutive spaces preserved byte-for-byte; **seventy-third cohort plugin** with fully-clean department pass-through). The run-345 probe sampled **539 visible roles** — **NEW LARGEST probe-counter UNDER-count factor across all sweeps at ~6.8×** (estimate ~79 keys vs actual 539) AND **largest single-board sample observed in run-history**. **Run #345 launches the ninth fresh probe sweep** — probed 95 candidate slugs and found **17 fresh non-empty live hits** (excluding Asana and Calendly which are already shipped): `axon` (539, this row), `beam` (~14), `bigid` (~30), `blend` (~14), `bloomreach` (~62), `celonis` (~154), `complyadvantage` (~33), `conviva` (~9), `cribl` (~33), `earnest` (~33), `expressvpn` (~32), `fairmarkit` (~12), `formlabs` (~95), `founders` (~6), `fox` (~10), `gocardless` (~36), `gofundme` (~36). The remaining 16 live hits queue for runs #346+ in alphabetical order (`beam` next at run #346 with ~14 keys). |
| shipped    | `source-company-starburst` | `company`  | **Starburst** (`starburst.io`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/starburst/jobs`). | Spec 134, run #344. **Fifteenth and last plugin in the eighth fresh probe sweep — sweep fully exhausted at this run.** Thin wrapper following the `source-company-doximity` pattern. **Zero structural deviations** — making this the **thirty-second** clean re-spin. All axes share with Doximity: D-04 variant 2 (49th plugin), D-08 (90th member — **crosses the 90-plugin D-08-application threshold at this run**), D-09 omitted with case-symmetric `'Starburst'` (9 bytes; **eighty-first cohort plugin to omit D-09**), D-10 applied (6 of 26 padded ~23.1 %; **fifty-fourth cohort plugin to apply D-10**), D-11 omitted (0 of 26 padded across 11 unique departments — `'Engineering'`, `'Enterprise Sales'`, `'Executive Operations'`, `'GTM'`, `'IT'`, `'Marketing'`, `'Presales'`, `'Product'`, `'Professional Services'`, `'Sales Development'`, `'Support'`; **seventy-second cohort plugin** with fully-clean department pass-through). The run-344 probe sampled **26 visible roles** (matched the eighth-sweep estimate exactly — 1× inflation; **seventh 1× match in eighth-sweep**). **Run #344 closes out the eighth fresh probe sweep** — the run-330 candidate pool of 15 has now produced 15 shipped plugins (Betterment, Branch, Chainguard, Checkr, Contentful, Descope, Dialpad, Doximity, Dremio, Justworks, Melio, Modern Health, Opendoor, Oscar, Starburst). **The ninth fresh probe sweep launches at run #345+.** |
| shipped    | `source-company-oscar`     | `company`  | **Oscar Health** (`hioscar.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/oscar/jobs`). | Spec 133, run #343. **Fourteenth plugin in the eighth fresh probe sweep (second-to-last).** Thin wrapper following the `source-company-squarespace` pattern. **Two structural deviations** from Squarespace: (a) **D-04 wire-shape variant 35 (HTTP `www.hioscar.com/careers/<id>?gh_jid=<id>` — first cohort observation; thirty-eighth distinct wire-shape variant; second cohort plugin with HTTP scheme after Squarespace)**. Variant 35 is sister to Squarespace's variant 22 (HTTP + www + query-only id) but with id-in-path + gh_jid (dual-id form) and parent-domain `hioscar.com` (not slug-matching). (b) **D-09 first-cohort slug-extra-word asymmetric wire form** — slug `oscar` (5 bytes) vs wire `'Oscar Health'` (12 bytes — wire adds entire second token `' Health'` beyond slug); distinct from prior internal-whitespace asymmetry cases (Scale AI etc.) which had same letters split by space; **eightieth cohort plugin to omit D-09 — crosses the 80-plugin D-09-omission threshold at this run**. All other axes share with Squarespace: D-08 (89th member), D-10 applied with **second-cohort leading-pad observation** after Chainguard (2 of 247 padded ~0.81 % — **lowest D-10 pad rate in cohort**; 1 trailing + 1 leading; **fifty-third cohort plugin to apply D-10**), D-11 omitted (0 of 247 padded across 23 unique departments; **seventy-first cohort plugin** with fully-clean department pass-through). The run-343 probe sampled **247 visible roles** — **largest probe-counter UNDER-count in eighth-sweep at ~0.33× ratio** (estimate ~81 keys vs actual 247) and **largest single-board sample in eighth-sweep**. Selected from the **eighth fresh probe sweep** as the **alphabetically-fourteenth live-board hit**. The remaining 1 live hit queues for run #344 (`starburst` — last eighth-sweep candidate). |
| shipped    | `source-company-opendoor`  | `company`  | **Opendoor** (`opendoor.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/opendoor/jobs`). | Spec 132, run #342. **Thirteenth plugin in the eighth fresh probe sweep.** Thin wrapper following the `source-company-dremio` pattern. **One structural deviation** from Dremio: **D-04 wire-shape variant 34 (HTTPS `www.opendoor.com/careers/open-positions?gh_jid=<id>` — first cohort observation; thirty-seventh distinct wire-shape variant)**. Variant 34 is sister to Dremio's variant 33 (different leaf-segment: `/careers/open-positions` vs `/careers/job-postings/`). All other axes share with Dremio: D-08 (88th member), D-09 omitted with case-symmetric `'Opendoor'` (8 bytes; **seventy-ninth cohort plugin to omit D-09**), D-10 applied with **first-cohort internal-double-whitespace observation** (4 of 66 padded ~6.1 %; one carries internal double-space — `'Customer Experience  Specialist '`; preserved byte-for-byte after `.trim()`; **fifty-second cohort plugin to apply D-10**), D-11 omitted (0 of 66 padded across 11 unique departments; **seventieth cohort plugin** with fully-clean department pass-through — **crosses the 70-plugin D-11-omission threshold at this run**). The run-342 probe sampled **66 visible roles** — **fifth eighth-sweep candidate where probe-counter UNDER-counted** (estimate ~34 keys vs actual 66; ~0.52× ratio). Selected from the **eighth fresh probe sweep** as the **alphabetically-thirteenth live-board hit**. The remaining 2 live hits queue for runs #343+ in alphabetical order (`oscar` next at run #343 with ~81 keys). |
| shipped    | `source-company-modernhealth` | `company`  | **Modern Health** (`modernhealth.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/modernhealth/jobs`). | Spec 131, run #341. **Twelfth plugin in the eighth fresh probe sweep.** Thin wrapper following the `source-company-constantcontact` pattern. **Zero structural deviations** — making this the **thirty-first** clean re-spin and the **eighth** internal-whitespace asymmetry case in the cohort. All axes share with Constant Contact: D-04 variant 2 (48th plugin), D-08 (87th member), D-09 omitted with **internal-whitespace asymmetric** wire form (wire `'Modern Health'` 13 bytes — two-token brand with internal space at byte index 6; case-AND-length-asymmetric vs concatenated 12-byte slug `modernhealth`; **eighth internal-whitespace asymmetry case** after Scale AI/Maven Clinic/Stitch Fix/New Relic/Dollar Shave Club/Misfits Market/Constant Contact; **seventy-eighth cohort plugin to omit D-09**), D-10 applied (1 of 14 padded ~7.1 %; **fifty-first cohort plugin to apply D-10**), D-11 omitted (0 of 14 padded across 8 unique departments — `'Customer Success'`, `'Engineering'`, `'Legal'`, `'Marketing'`, `'Operations'`, `'Partnerships'`, `'People'`, `'Sales'`; **sixty-ninth cohort plugin** with fully-clean department pass-through). The run-341 probe sampled **14 visible roles** (matched the eighth-sweep estimate exactly — 1× inflation; **sixth 1× match in eighth-sweep** after Branch, Descope, Doximity, Dremio, Melio). Selected from the **eighth fresh probe sweep** as the **alphabetically-twelfth live-board hit**. The remaining 3 live hits queue for runs #342+ in alphabetical order (`opendoor` next at run #342 with ~34 keys). |
| shipped    | `source-company-melio`     | `company`  | **Melio** (`melio.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/melio/jobs`). | Spec 130, run #340. **Eleventh plugin in the eighth fresh probe sweep.** Thin wrapper following the `source-company-descope` pattern. **One structural deviation** from Descope: **D-10 APPLIED** (Descope D-10 omitted at 0/8 padded; Melio 2 of 20 padded ~10 %; **fiftieth cohort plugin to apply D-10 — crosses the 50-plugin D-10-application threshold at this run**). All other axes share with Descope: D-04 variant 2 (47th plugin), D-08 (86th member), D-09 omitted with case-symmetric `'Melio'` (5 bytes; **seventy-seventh cohort plugin to omit D-09**), D-11 applied (1 of 8 unique dept names padded — `'Design '`; **twelfth cohort plugin to apply D-11**). The run-340 probe sampled **20 visible roles** (matched the eighth-sweep estimate exactly — 1× inflation; **fifth 1× match in eighth-sweep** after Branch, Descope, Doximity, Dremio). Selected from the **eighth fresh probe sweep** as the **alphabetically-eleventh live-board hit**. The remaining 4 live hits queue for runs #341+ in alphabetical order (`modernhealth` next at run #341 with ~14 keys). |
| shipped    | `source-company-justworks` | `company`  | **Justworks** (`justworks.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/justworks/jobs`). | Spec 129, run #339. **Tenth plugin in the eighth fresh probe sweep.** Thin wrapper following the `source-company-descript` pattern. **Zero structural deviations** with **first-cohort D-10 sub-axis observation** (double-trailing-space pad form) — making this the **thirtieth** clean re-spin and the **sixth** plugin to use **wire-shape variant 10** (legacy hosted-board apex `boards.greenhouse.io/justworks/jobs/<id>?gh_jid=<id>` — after Chime/Faire/Flexport/Braze/Descript). All axes share with Descript: D-04 variant 10, D-08 (85th member), D-09 omitted with case-symmetric `'Justworks'` (9 bytes; **seventy-sixth cohort plugin to omit D-09**), D-10 applied with **FIRST-COHORT double-trailing-space pad form** (5 of 82 padded ~6.1 %; one carries 2 trailing spaces — `'Overnight Customer Support Advocate (Remote)  '`; **forty-ninth cohort plugin to apply D-10**), D-11 omitted (0 of 82 padded across 17 unique departments — `'Corporate & Finance'`, `'Customer Success'`, `'Engineering'`, `'Operations, Benefits'`, `'Operations, Payments & Tax'`, plus 12 others; **sixty-eighth cohort plugin** with fully-clean department pass-through). The run-339 probe sampled **82 visible roles** — **fourth eighth-sweep candidate where probe-counter UNDER-counted** (estimate ~60 keys vs actual 82; ~0.73× ratio). Selected from the **eighth fresh probe sweep** as the **alphabetically-tenth live-board hit**. The remaining 5 live hits queue for runs #340+ in alphabetical order (`melio` next at run #340 with ~20 keys). |
| shipped    | `source-company-dremio`    | `company`  | **Dremio** (`dremio.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/dremio/jobs`). | Spec 128, run #338. **Ninth plugin in the eighth fresh probe sweep.** Thin wrapper following the `source-company-fastly` pattern. **One structural deviation** from Fastly: **D-04 wire-shape variant 33 (HTTPS `www.dremio.com/careers/job-postings/?gh_jid=<id>` — first cohort observation; thirty-sixth distinct wire-shape variant)**. Variant 33 is sister to Fastly's variant 30 (HTTPS + www + query-only-id, distinct path: `/careers/job-postings/` with trailing slash vs `/about/jobs/apply` without trailing slash). All other axes share with Fastly: D-08 (84th member), D-09 omitted with case-symmetric `'Dremio'` (6 bytes; **seventy-fifth cohort plugin to omit D-09**), D-10 applied (2 of 12 padded ~16.7 %; **forty-eighth cohort plugin to apply D-10**), D-11 omitted (0 of 12 padded across 7 unique departments — `'Engineering'`, `'Marketing'`, `'Presales'`, `'Product'`, `'Sales'`, `'Security'`, plus 1 **first-cohort sentence-style catchall dept**: `'Unsure what to apply for? No worries! Submit your resume here.'`; **sixty-seventh cohort plugin** with fully-clean department pass-through). The run-338 probe sampled **12 visible roles** (matched the eighth-sweep estimate exactly — 1× inflation; **fourth 1× match in eighth-sweep** after Branch, Descope, Doximity). Selected from the **eighth fresh probe sweep** as the **alphabetically-ninth live-board hit**. The remaining 6 live hits queue for runs #339+ in alphabetical order (`justworks` next at run #339 with ~60 keys). |
| shipped    | `source-company-doximity`  | `company`  | **Doximity** (`doximity.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/doximity/jobs`). | Spec 127, run #337. **Eighth plugin in the eighth fresh probe sweep.** Thin wrapper following the `source-company-contentful` pattern. **Zero structural deviations** — making this the **twenty-ninth** clean re-spin. All axes share with Contentful: D-04 variant 2 (46th plugin), D-08 (83rd member), D-09 omitted with case-symmetric `'Doximity'` (8 bytes; **seventy-fourth cohort plugin to omit D-09**), D-10 applied (2 of 15 padded ~13.3 %; **forty-seventh cohort plugin to apply D-10**), D-11 omitted (0 of 15 padded across 7 unique departments — `'Data'`, `'Engineering'`, `'Finance & Accounting'`, `'Marketing'`, `'Mobile Engineering'`, `'Sales & Client Success'`, `'Summer Internships'`; **sixty-sixth cohort plugin** with fully-clean department pass-through). The run-337 probe sampled **15 visible roles** (matched the eighth-sweep estimate exactly — 1× inflation; **third 1× match in eighth-sweep** after Branch and Descope). Selected from the **eighth fresh probe sweep** as the **alphabetically-eighth live-board hit**. The remaining 7 live hits queue for runs #338+ in alphabetical order (`dremio` next at run #338 with ~12 keys). |
| shipped    | `source-company-dialpad`   | `company`  | **Dialpad** (`dialpad.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/dialpad/jobs`). | Spec 126, run #336. **Seventh plugin in the eighth fresh probe sweep.** Thin wrapper following the `source-company-branch` pattern. **Zero structural deviations** with **first-cohort D-11 sub-axis observation** (numeric-prefix-with-hyphen-separator dept naming) — making this the **twenty-eighth** clean re-spin. All axes share with Branch: D-04 variant 2 (45th plugin), D-08 (82nd member), D-09 omitted with case-symmetric `'Dialpad'` (7 bytes; **seventy-third cohort plugin to omit D-09**), D-10 omitted (0 of 86 padded; **twenty-fifth cohort plugin to omit D-10**), D-11 omitted with **FIRST-COHORT numeric-prefix-with-hyphen-separator** dept naming convention — all 33 unique dept names follow `<numeric_code> - <name>` pattern (`'120 - Product Operations'`, `'214 - AI Engineering'`, `'515 - Enterprise Sales'`, plus 30 others); **distinct from Constant Contact's space-only-separator** (`'100 Engineering'`); **sixty-fifth cohort plugin** with fully-clean department pass-through. The run-336 probe sampled **86 visible roles** — **third eighth-sweep candidate where probe-counter UNDER-counted** (estimate ~35 keys vs actual 86; ~0.4× ratio — even tighter under-count than Checkr at ~0.5×). Selected from the **eighth fresh probe sweep** as the **alphabetically-seventh live-board hit**. The remaining 8 live hits queue for runs #337+ in alphabetical order (`doximity` next at run #337 with ~15 keys). |
| shipped    | `source-company-descope`   | `company`  | **Descope** (`descope.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/descope/jobs`). | Spec 125, run #335. **Sixth plugin in the eighth fresh probe sweep.** Thin wrapper following the `source-company-branch` pattern. **One structural deviation** from Branch: **D-11 APPLIED with trailing-pad form** (Branch 0/11 padded omitted; Descope 2 of 3 unique dept names padded — `'Customer Success '` and `'Engineering '`; **listing-level pad rate 6 of 8 ~75 % — the highest D-11 listing-level pad rate observed in the cohort to date**, surpassing AssemblyAI's ~43 %; **eleventh cohort plugin to apply D-11**). All other axes share with Branch: D-04 variant 2 (44th plugin), D-08 (81st member), D-09 omitted with case-symmetric `'Descope'` (7 bytes; **seventy-second cohort plugin to omit D-09**), D-10 omitted (0 of 8 padded; **twenty-fourth cohort plugin to omit D-10**). The run-335 probe sampled **8 visible roles** (matched the eighth-sweep estimate exactly — 1× inflation; second 1× match after Branch). Selected from the **eighth fresh probe sweep** as the **alphabetically-sixth live-board hit**. The remaining 9 live hits queue for runs #336+ in alphabetical order (`dialpad` next at run #336 with ~35 keys). |
| shipped    | `source-company-contentful` | `company`  | **Contentful** (`contentful.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/contentful/jobs`). | Spec 124, run #334. **Fifth plugin in the eighth fresh probe sweep.** Thin wrapper following the `source-company-checkr` pattern. **Zero structural deviations** — making this the **twenty-seventh** clean re-spin. All axes share with Checkr: D-04 variant 2 (43rd plugin), D-08 (80th member — **crosses the 80-plugin D-08-application threshold at this run**), D-09 omitted with case-symmetric `'Contentful'` (10 bytes; **seventy-first cohort plugin to omit D-09**), D-10 applied (8 of 108 padded ~7.4 %; **forty-sixth cohort plugin to apply D-10**), D-11 omitted (0 of 108 padded across 9 unique departments — `'Customer Experience'`, `'Engineering'`, `'Finance'`, `'IT'`, `'Marketing'`, `'Partnerships'`, `'Product'`, `'Sales'`, `'Security'`; **sixty-fourth cohort plugin** with fully-clean department pass-through). The run-334 probe sampled **108 visible roles** — **second eighth-sweep candidate where the probe-counter UNDER-counted** (estimate ~92 keys vs actual 108; ~0.85× ratio; after Checkr's ~0.5×). Selected from the **eighth fresh probe sweep** as the **alphabetically-fifth live-board hit**. The remaining 10 live hits queue for runs #335+ in alphabetical order (`descope` next at run #335 with ~8 keys → likely under 10 actual). |
| shipped    | `source-company-checkr`    | `company`  | **Checkr** (`checkr.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/checkr/jobs`). | Spec 123, run #333. **Fourth plugin in the eighth fresh probe sweep.** Thin wrapper following the `source-company-otter` pattern. **Zero structural deviations** — making this the **twenty-sixth** clean re-spin. All axes share with Otter: D-04 variant 2 (42nd plugin), D-08 (79th member), D-09 omitted with case-symmetric `'Checkr'` (6 bytes; **seventieth cohort plugin to omit D-09 — crosses the 70-plugin D-09-omission threshold at this run**), D-10 applied (2 of 65 padded ~3.1 %; **forty-fifth cohort plugin to apply D-10**), D-11 omitted (0 of 65 padded across 13 unique departments — `'CheckrX'`, `'Customer Success'`, `'Engineering'`, `'Finance'`, `'Implementations'`, `'Information Technology'`, `'Legal & Compliance'`, plus 6 others; `'CheckrX'` carries the **embedded brand-name dept naming convention** as observability sub-axis; **sixty-third cohort plugin** with fully-clean department pass-through). The run-333 probe sampled **65 visible roles** — **first eighth-sweep candidate where the probe-counter UNDER-counted** the actual job total (estimate ~33 keys vs actual 65; ~0.5× ratio; prior 13 eighth-sweep candidates over-counted 1× to ~9×). Selected from the **eighth fresh probe sweep** as the **alphabetically-fourth live-board hit**. The remaining 11 live hits queue for runs #334+ in alphabetical order (`contentful` next at run #334 with ~92 keys). |
| shipped    | `source-company-chainguard` | `company`  | **Chainguard** (`chainguard.dev`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/chainguard/jobs`). | Spec 122, run #332. **Third plugin in the eighth fresh probe sweep.** Thin wrapper following the `source-company-otter` pattern. **Zero structural deviations** with **first-cohort D-10 sub-axis observation** (mixed leading-AND-trailing pad form) — making this the **twenty-fifth** clean re-spin. All axes share with Otter: D-04 variant 2 (41st plugin), D-08 (78th member), D-09 omitted with case-symmetric `'Chainguard'` (10 bytes; **sixty-ninth cohort plugin to omit D-09**), D-10 applied with **FIRST-COHORT mixed leading-AND-trailing pad form** (7 of 60 padded ~11.7 % — 6 trailing + 1 **leading-pad** `' Senior Software Engineer (Experience)'`; **first cohort observation of leading-pad title form**; `.trim()` is symmetric so no axis change required; **forty-fourth cohort plugin to apply D-10**), D-11 omitted (0 of 60 padded across 20 unique departments; **sixty-second cohort plugin** with fully-clean department pass-through). The run-332 probe sampled **60 visible roles** (eighth-sweep estimate was ~75, ~1.25× inflation). Selected from the **eighth fresh probe sweep** as the **alphabetically-third live-board hit**. The remaining 12 live hits queue for runs #333+ in alphabetical order (`checkr` next at run #333 with ~33 keys). |
| shipped    | `source-company-branch`    | `company`  | **Branch** (`branch.io`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/branch/jobs`). | Spec 121, run #331. **Second plugin in the eighth fresh probe sweep.** Thin wrapper following the `source-company-pendo` pattern. **Zero structural deviations** — making this the **twenty-fourth** clean re-spin. All axes share with Pendo: D-04 variant 2 (40th plugin — the cohort crosses the 40-plugin variant-2 threshold at this run), D-08 (77th member), D-09 omitted with case-symmetric `'Branch'` (6 bytes; **sixty-eighth cohort plugin to omit D-09**), D-10 omitted (0 of 11 padded; **twenty-third cohort plugin to omit D-10**), D-11 omitted (0 of 11 padded across 7 unique departments — `'Customer Integration'`, `'Engineering'`, `'Finance'`, `'Marketing'`, `'Sales'`, `'Security'`, `'Support Operations'`; **sixty-first cohort plugin** with fully-clean department pass-through). The run-331 probe sampled **11 visible roles** (matched the eighth-sweep estimate exactly — 1× inflation). **Slug-vs-brand asymmetry note:** the slug `branch` matches multiple brands (Branch.io mobile deep-linking, Branch International, Branch Insurance, etc.); department names suggest the published board covers the **Branch.io mobile deep-linking** brand. Selected from the **eighth fresh probe sweep** as the **alphabetically-second live-board hit**. The remaining 13 live hits queue for runs #332+ in alphabetical order (`chainguard` next at run #332 with ~75 keys → likely ~25 actual via probe-counter inflation). |
| shipped    | `source-company-betterment` | `company`  | **Betterment** (`betterment.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/betterment/jobs`). | Spec 120, run #330. **First plugin in the EIGHTH fresh probe sweep.** Thin wrapper following the `source-company-elastic` pattern. **Two structural deviations** from Elastic: (a) **D-04 wire-shape variant 32 (HTTPS `www.betterment.com/careers/current-openings/job?gh_jid=<id>&gh_jid=<id>` — first cohort observation; thirty-fifth distinct wire-shape variant)**. Variant 32 is the **`www.<brand>.com` brand-domain** sister to Elastic's variant 11 vanity-subdomain (`jobs.elastic.co/jobs`), with a different host AND path (`/careers/current-openings/job` vs `/jobs`); both share the **duplicate `gh_jid` query parameter** wire form (Greenhouse vanity-tenant pattern — **second cohort observation of duplicate-gh_jid form** after Elastic). (b) **D-11 APPLIED with trailing-pad form** (Elastic 0/N omitted; Betterment 1 of 11 unique dept names padded — `'Customer Experience '`, listing-level pad rate 3 of 31 ~9.7 %; **tenth cohort plugin to apply D-11**). All other axes share with Elastic: D-08 (76th cohort member), D-09 omitted with case-symmetric `'Betterment'` (10 bytes; **sixty-seventh cohort plugin to omit D-09**), D-10 applied (5 of 31 padded ~16 %; **forty-third cohort plugin to apply D-10**). The run-330 probe sampled **31 visible roles** (matched the eighth-sweep estimate exactly — 1× inflation). **Run #330 launches the eighth fresh probe sweep** — probed 95 candidate slugs and found **15 fresh non-empty live hits** (excluding Brex and Stripe which are already shipped): `betterment` (31, this row), `branch` (~11), `chainguard` (~75), `checkr` (~33), `contentful` (~92), `descope` (~8), `dialpad` (~35), `doximity` (~15), `dremio` (~12), `justworks` (~60), `melio` (~20), `modernhealth` (~14), `opendoor` (~34), `oscar` (~81), `starburst` (~26). The remaining 14 live hits queue for runs #331+ in alphabetical order (`branch` next at run #331 with ~11 keys). |
| shipped    | `source-company-vonage`    | `company`  | **Vonage** (`vonage.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/vonage/jobs`). | Spec 119, run #329. **Fourteenth and last plugin in the seventh fresh probe sweep — sweep fully exhausted at this run.** Thin wrapper following the `source-company-otter` pattern. **Zero structural deviations** — making this the **twenty-third** clean re-spin. All axes share with Otter: D-04 variant 2 (39th plugin), D-08 (75th member), D-09 omitted with case-symmetric `'Vonage'` (6 bytes; **sixty-sixth cohort plugin to omit D-09**), D-10 applied (11 of 81 padded ~13.6 %; **forty-second cohort plugin to apply D-10**), D-11 omitted (0 of 81 padded across 19 unique departments — `'API BU Engineering'`, `'API BU Product'`, `'API Customer Care'`, `'API In-BU Operations'`, `'API Sales Development'`, `'Apps BU Engineering'`, `'COO - R&D'`, plus 12 others; **sixtieth cohort plugin** with fully-clean department pass-through — crosses the 60-plugin D-11-omission threshold at this run). The run-329 probe sampled **81 visible roles** (matched the run-316 seventh-sweep estimate of ~81 keys exactly — 1× inflation, second-cleanest match after Pendo). **Run #329 closes out the seventh fresh probe sweep** — the run-316 candidate pool of 14 has now produced 14 shipped plugins (Airtable, Amplitude, AssemblyAI, Bandwidth, Braze, Constant Contact, Descript, Fastly, LaunchDarkly, Okta, Otter, PagerDuty, Pendo, Vonage). **The eighth fresh probe sweep launches at run #330+.** |
| shipped    | `source-company-pendo`     | `company`  | **Pendo** (`pendo.io`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/pendo/jobs`). | Spec 118, run #328. **Thirteenth plugin in the seventh fresh probe sweep.** Thin wrapper following the `source-company-coursera` pattern. **Zero structural deviations** — making this the **twenty-second** clean re-spin. All axes share with Coursera: D-04 variant 2 (38th plugin), D-08 (74th member), D-09 omitted with case-symmetric `'Pendo'` (5 bytes; **sixty-fifth cohort plugin to omit D-09**), D-10 omitted (0 of 18 padded; **twenty-second cohort plugin to omit D-10**), D-11 omitted (0 of 18 padded across 7 unique departments — `'Brand Marketing'`, `'Commercial'`, `'Engineering Operations'`, `'Enterprise'`, `'Field Marketing'`, `'Finance'`, `'Product Marketing'`; **fifty-ninth cohort plugin** with fully-clean department pass-through). The run-328 probe sampled **18 visible roles** (matched the run-316 seventh-sweep estimate of ~18 keys exactly — **lowest probe-counter-inflation factor (1×) observed in the seventh-sweep**, the only sweep member with zero inflation). Selected from the **seventh fresh probe sweep** as the **alphabetically-thirteenth live-board hit**. The remaining 1 live hit queues for run #329 (`vonage` next at run #329 with ~81 keys → likely ~25 actual via probe-counter inflation — the **last** seventh-sweep candidate). |
| shipped    | `source-company-pagerduty` | `company`  | **PagerDuty** (`pagerduty.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/pagerduty/jobs`). | Spec 117, run #327. **Twelfth plugin in the seventh fresh probe sweep.** Thin wrapper following the `source-company-launchdarkly` pattern. **Zero structural deviations** — making this the **twenty-first** clean re-spin. All axes share with LaunchDarkly: D-04 variant 2 (37th plugin), D-08 (73rd member), D-09 omitted with **PascalCase TWO-cap case-asymmetric wire form** (wire `'PagerDuty'` 9 bytes; case-asymmetric vs slug `pagerduty` at TWO byte indices 0/5; **fifth cohort observation of TWO-cap PascalCase D-09 sub-axis** after SoFi/StockX/xAI/LaunchDarkly; PagerDuty's caps at 0/5 **tie StockX for second-deepest second-cap** in the cohort, LaunchDarkly retains the deepest at 0/6; **sixty-fourth cohort plugin to omit D-09**), D-10 applied (4 of 48 padded ~8.3 %; **forty-first cohort plugin to apply D-10**), D-11 omitted (0 of 48 padded across 14 unique departments — `'Business Operations'`, `'Commercial Sales'`, `'Customer Success'`, `'Enterprise Sales'`, `'Legal'`, `'Marketing'`, `'Product Management'`, `'Professional Services'`, `'Renewals'`, `'Sales'`, plus 4 others; **fifty-eighth cohort plugin** with fully-clean department pass-through). The run-327 probe sampled **48 visible roles** (the run-316 seventh-sweep estimate of ~96 keys was probe-counter-inflated by ~2× via dept/office IDs). Selected from the **seventh fresh probe sweep** as the **alphabetically-twelfth live-board hit**. The remaining 2 live hits queue for runs #328+ in alphabetical order (`pendo` next at run #328 with ~18 keys → likely ~5 actual via probe-counter inflation). |
| shipped    | `source-company-otter`     | `company`  | **Otter** (`tryotter.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/otter/jobs`). | Spec 116, run #326. **Eleventh plugin in the seventh fresh probe sweep.** Thin wrapper following the `source-company-airtable` pattern. **Zero structural deviations** — making this the **twentieth** clean re-spin. All axes share with Airtable: D-04 variant 2 (36th plugin), D-08 (72nd member), D-09 omitted with case-symmetric `'Otter'` (5 bytes; **sixty-third cohort plugin to omit D-09**), D-10 applied (5 of 39 padded ~12.8 %; **fortieth cohort plugin to apply D-10 — crosses the 40-plugin D-10-application threshold at this run**), D-11 omitted (0 of 39 padded across 16 unique departments — `'ENG Beam'`, `'ENG Brick & Mortar'`, `'Otter - ENT'`, `'Otter - SMB'`, `'Otter Central Ops'`, `'Hardware Engineering'`, plus 10 others; **fifty-seventh cohort plugin** with fully-clean department pass-through). The run-326 probe sampled **39 visible roles** (the run-316 seventh-sweep estimate of ~156 keys was probe-counter-inflated by ~4× via dept/office IDs). **Slug-vs-brand asymmetry note:** the slug `otter` matches both Otter.ai (AI-transcription) and Otter food-delivery infrastructure (formerly Ordermark / TryOtter); department names suggest the published board covers the **food-delivery** brand. Selected from the **seventh fresh probe sweep** as the **alphabetically-eleventh live-board hit**. The remaining 3 live hits queue for runs #327+ in alphabetical order (`pagerduty` next at run #327 with ~96 keys → likely ~30 actual via probe-counter inflation). |
| shipped    | `source-company-okta`      | `company`  | **Okta** (`okta.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/okta/jobs`). | Spec 115, run #325. **Tenth plugin in the seventh fresh probe sweep.** Thin wrapper following the `source-company-fastly` pattern. **One structural deviation** from Fastly: **D-04 wire-shape variant 31 (HTTPS `www.okta.com/company/careers/opportunity/<id>?gh_jid=<id>` — first cohort observation; thirty-fourth distinct wire-shape variant)**. Variant 31 is the **id-in-path + gh_jid query (dual-id form)** sister to Fastly's variant 30 (query-only id), with a different path (`/company/careers/opportunity/<id>` rather than `/about/jobs/apply` — first cohort observation of `/company/` prefix-segment AND singular `opportunity/<id>` leaf form). All other axes share with Fastly: D-08 (71st cohort member), D-09 omitted with case-symmetric `'Okta'` (4 bytes; **sixty-second cohort plugin to omit D-09**), D-10 applied (54 of 358 padded ~15.1 %; eighth-largest D-10 pad rate; **thirty-ninth cohort plugin to apply D-10**), D-11 omitted with **FIRST-COHORT suffix-numeric-ID dept naming sub-axis** (`<name>-<numeric ID>` form — `'Accounting Operations-121'`, `'Auth0 DevRel-494'`, `'BT Engineering Services-779'`, plus 73 others; **second cohort observation of numeric IDs in dept names** after Constant Contact's prefix convention; Okta is the **first** cohort observation of the **suffix** form; **fifty-sixth cohort plugin** with fully-clean department pass-through). The run-325 probe sampled **358 visible roles** (the run-316 seventh-sweep estimate of ~3222 keys was probe-counter-inflated by ~9× via dept/office IDs — the **largest probe-counter-inflation factor** observed in the seventh-sweep). Selected from the **seventh fresh probe sweep** as the **alphabetically-tenth live-board hit**. The remaining 4 live hits queue for runs #326+ in alphabetical order (`otter` next at run #326 with ~156 keys → likely ~50 actual via probe-counter inflation). |
| shipped    | `source-company-launchdarkly` | `company`  | **LaunchDarkly** (`launchdarkly.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/launchdarkly/jobs`). | Spec 114, run #324. **Ninth plugin in the seventh fresh probe sweep.** Thin wrapper following the `source-company-planetscale` pattern. **One structural deviation** from PlanetScale: **D-10 APPLIED with trailing-pad form** (PlanetScale 0/6 padded omitted; LaunchDarkly 3/45 padded ~6.7 % applied; **thirty-eighth cohort plugin to apply D-10**). All other axes share with PlanetScale: D-04 variant 2 (canonical Greenhouse host — **thirty-fifth** plugin), D-08 (70th cohort member), D-09 omitted with **PascalCase TWO-cap case-asymmetric wire form** (wire `'LaunchDarkly'` 12 bytes; case-asymmetric vs slug `launchdarkly` at TWO byte indices 0/6; **fourth cohort observation of TWO-cap PascalCase D-09 sub-axis** after SoFi/StockX/xAI; LaunchDarkly's caps at 0/6 are the **deepest second-cap observed** in the cohort to date — StockX previously held the deepest at index 5; **sixty-first cohort plugin to omit D-09**), D-11 omitted (0 of 45 padded across 17 clean multi-token departments — `'AI Engineering'`, `'CEO Administration'`, `'Core Engineering'`, `'Customer Success'`, `'Finance'`, `'IT'`, `'Legal'`, `'Marketing'`, `'Measure Engineering'`, `'Partnerships'`, plus 7 others; **fifty-fifth cohort plugin** with fully-clean department pass-through). The run-324 probe sampled **45 visible roles** (the run-316 seventh-sweep estimate of ~135 keys was probe-counter-inflated by ~3× via dept/office IDs). Selected from the **seventh fresh probe sweep** as the **alphabetically-ninth live-board hit**. The remaining 5 live hits queue for runs #325+ in alphabetical order (`okta` next at run #325 with ~3222 keys → likely much smaller actual via probe-counter inflation). |
| shipped    | `source-company-fastly`    | `company`  | **Fastly** (`fastly.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/fastly/jobs`). | Spec 113, run #323. **Eighth plugin in the seventh fresh probe sweep.** Thin wrapper following the `source-company-squarespace` pattern. **One structural deviation** from Squarespace: **D-04 wire-shape variant 30 (HTTPS `www.fastly.com/about/jobs/apply?gh_jid=<id>` — first cohort observation; thirty-third distinct wire-shape variant)**. Variant 30 is the HTTPS sister to Squarespace's variant 22 HTTP `/about/careers`, with a different path (`/about/jobs/apply` rather than `/about/careers` — first cohort observation of `/about/jobs/<leaf>` form with `/jobs/` mid-segment + `/apply` leaf). All other axes share with Squarespace: D-08 (69th cohort member), D-09 omitted with case-symmetric `'Fastly'` (6 bytes; **sixtieth cohort plugin to omit D-09 — crosses the 60-plugin D-09-omission threshold at this run**), D-10 applied (1 of 64 padded ~1.6 %; second-lowest D-10 pad rate observed in cohort, just behind Braze's ~1.4 %; **thirty-seventh cohort plugin to apply D-10**), D-11 omitted (0 of 64 padded across 28 clean multi-token departments with hyphens and acronym suffixes; **fifty-fourth cohort plugin** with fully-clean department pass-through). The run-323 probe sampled **64 visible roles** (the run-316 seventh-sweep estimate of ~256 keys was probe-counter-inflated by ~4× via dept/office IDs). Selected from the **seventh fresh probe sweep** as the **alphabetically-eighth live-board hit**. The remaining 6 live hits queue for runs #324+ in alphabetical order (`launchdarkly` next at run #324 with ~135 keys → likely ~40 actual via probe-counter inflation). |
| shipped    | `source-company-descript`  | `company`  | **Descript** (`descript.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/descript/jobs`). | Spec 112, run #322. **Seventh plugin in the seventh fresh probe sweep.** Thin wrapper following the `source-company-braze` pattern. **Zero structural deviations** — making this the **nineteenth** clean re-spin and the **fifth** plugin to use **wire-shape variant 10** (legacy hosted-board apex `https://boards.greenhouse.io/descript/jobs/<id>?gh_jid=<id>` — after Chime/Faire/Flexport/Braze). All axes share with Braze: D-04 variant 10 (5th cohort plugin), D-08 (68th member), D-09 omitted with case-symmetric `'Descript'` (8 bytes; **fifty-ninth cohort plugin to omit D-09**), D-10 applied (2 of 25 padded ~8 %; **thirty-sixth cohort plugin to apply D-10**), D-11 omitted (0 of 25 padded across 5 clean multi-token departments; **fifty-third cohort plugin** with fully-clean department pass-through). The run-322 probe sampled **25 visible roles** (matched probe-counter exactly). Selected from the **seventh fresh probe sweep** as the **alphabetically-seventh live-board hit**. The remaining 7 live hits queue for runs #323+ in alphabetical order (`fastly` next at run #323 with ~256 keys → likely ~80 actual via probe-counter inflation). |
| shipped    | `source-company-constantcontact` | `company`  | **Constant Contact** (`constantcontact.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/constantcontact/jobs`). | Spec 111, run #321. **Sixth plugin in the seventh fresh probe sweep.** Thin wrapper following the `source-company-misfitsmarket` pattern. **Zero structural deviations** from Misfits Market — making this the **eighteenth** clean re-spin. All axes share with Misfits Market: D-04 variant 2 (35th plugin), D-08 (67th member), D-09 omitted with **internal-whitespace wire asymmetry** (wire `'Constant Contact'` 16 bytes vs slug `constantcontact` 15 bytes; **seventh** internal-whitespace asymmetry case after Scale AI/Maven Clinic/Stitch Fix/New Relic/Dollar Shave Club/Misfits Market; **fifty-eighth cohort plugin to omit D-09**), D-10 applied (1 of 28 padded ~3.6 %; **thirty-fifth cohort plugin to apply D-10**), D-11 omitted with **FIRST-COHORT numeric-prefix department names** — all 9 unique department names carry numeric organizational-ID prefixes (`'100 Engineering'`, `'126 Design'`, `'135 Product'`, `'250 Sales'`, etc.); **fifty-second cohort plugin** with fully-clean department pass-through. The run-321 probe sampled **28 visible roles** (run-316 estimate ~168 was 6× probe-counter inflated). Selected from the **seventh fresh probe sweep** as the **alphabetically-sixth live-board hit**. The remaining 8 live hits queue for runs #322+ in alphabetical order (`descript` next at run #322 with ~25 keys). |
| shipped    | `source-company-braze`     | `company`  | **Braze** (`braze.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/braze/jobs`). | Spec 110, run #320. **Fifth plugin in the seventh fresh probe sweep.** Thin wrapper following the `source-company-flexport` pattern. **Zero structural deviations** from Flexport — making this the **seventeenth** Greenhouse-only company-direct plugin in run-history to ship as a clean re-spin. All five primary axes share with Flexport: D-04 wire-shape variant 10 (legacy hosted-board apex `https://boards.greenhouse.io/braze/jobs/<id>?gh_jid=<id>` — **fourth** plugin in the cohort to use variant 10 after Chime, Faire, Flexport), D-08 entity-decode-then-tag-strip (66th cohort member), D-09 omitted with case-symmetric bare-brand wire `'Braze'` (5 bytes; **fifty-seventh cohort plugin to omit D-09**), D-10 applied (3 of 207 ~1.5 %; **thirty-fourth cohort plugin to apply D-10**), D-11 omitted (0 of 207 padded across 11 unique departments; **fifty-first cohort plugin** with fully-clean department pass-through). The run-320 probe sampled **207 visible roles** (run-316 estimate ~414 was 2× probe-counter inflated). Selected from the **seventh fresh probe sweep** as the **alphabetically-fifth live-board hit**. The remaining 9 live hits queue for runs #321+ in alphabetical order (`constantcontact` next at run #321 with ~168 keys → likely ~50 actual via probe-counter inflation). |
| shipped    | `source-company-bandwidth` | `company`  | **Bandwidth** (`bandwidth.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/bandwidth/jobs`). | Spec 109, run #319. **Fourth plugin in the seventh fresh probe sweep.** Thin wrapper following the `source-company-airtable` pattern. **Zero structural deviations** from Airtable — making this the **fifteenth** Greenhouse-only company-direct plugin in run-history to ship as a clean re-spin. All five primary axes share with Airtable: D-04 variant 2 (canonical Greenhouse host — **thirty-fourth** plugin), D-08 (65th cohort member), D-09 omitted with case-symmetric bare-brand wire `'Bandwidth'` (9 bytes; **fifty-sixth cohort plugin to omit D-09**), D-10 applied (10 of 45 padded ~22 %; **thirty-third cohort plugin to apply D-10**), D-11 omitted (0 of 45 padded across 14 unique departments — `'IT'`, `'Network Engineering'`, `'Network Operations Center'`, `'Ohana Child Development Center'`, plus 10 others; **forty-ninth cohort plugin** with fully-clean department pass-through). The run-319 probe sampled **45 visible roles** (matched probe-counter exactly). Selected from the **seventh fresh probe sweep** as the **alphabetically-fourth live-board hit**. The remaining 10 live hits queue for runs #320+ in alphabetical order (`braze` next at run #320 with ~414 keys → likely ~120 actual via probe-counter inflation). |
| shipped    | `source-company-assemblyai` | `company`  | **AssemblyAI** (`assemblyai.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/assemblyai/jobs`). | Spec 108, run #318. **Third plugin in the seventh fresh probe sweep.** Thin wrapper following the `source-company-stockx` pattern (variant 2 + D-09 case-asym + D-11 trailing-pad apply cousin). **One structural deviation** from StockX: **D-10 applied** (1/7 ~14 %; **thirty-second cohort plugin to apply D-10**). **D-09 omitted with FIRST-COHORT PascalCase THREE-cap same-byte-count case-asymmetric wire form** — wire `company_name === 'AssemblyAI'` byte-for-byte (10 bytes; case-asymmetric at THREE byte indices — 0/8/9). **First cohort observation of THREE-cap PascalCase** — distinct from prior TWO-cap forms in SoFi/StockX/xAI. AssemblyAI's three caps mark the {first-letter-of-Assembly} + {all-letters-of-AI} two-token camelcase concat with the second token all-uppercase — first cohort observation of an `<Brand>+AI` concat where the trailing `AI` initialism is fully-capitalized. **Fifty-fifth cohort plugin to omit D-09**. **D-11 APPLIED with high-pad-rate trailing-pad form** — 3 of 7 listings carry `departments[0].name` records padded (`'Customer Experience '`, `'Product Marketing '`, `'Research '`); ~43 % listing-level pad rate, the **highest D-11 pad-rate observed** in the cohort to date. **Eighth cohort plugin to apply D-11**. **First cohort variant-2 plugin to apply BOTH D-10 and D-11 simultaneously** (HelloFresh and BILL apply both axes but on variants 26 and 24). **D-04 variant 2** (33rd plugin). **D-08** (64th cohort member). The run-318 probe sampled **7 visible roles** (matched probe-counter exactly). Selected from the **seventh fresh probe sweep** as the **alphabetically-third live-board hit**. The remaining 11 live hits queue for runs #319+ in alphabetical order (`bandwidth` next at run #319 with ~45 keys). |
| shipped    | `source-company-amplitude` | `company`  | **Amplitude** (`amplitude.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/amplitude/jobs`). | Spec 107, run #317. **Second plugin in the seventh fresh probe sweep.** Thin wrapper following the `source-company-fivetran` pattern (the run-292 first-ever D-09 trailing-pad applicant). **Two structural deviations** from Fivetran: (1) **D-04 wire-shape variant 2 (canonical Greenhouse host)** — Amplitude's `absolute_url` shape `https://job-boards.greenhouse.io/amplitude/jobs/<id>` (Fivetran was variant 19 with bare-brand-domain `/careers/job/...`). **Thirty-second** plugin in the cohort to use variant 2. (2) **D-10 applied** — Amplitude's wire shows 3 of 60 wire titles padded with single-trailing-ASCII-space form (~5.0 % pad rate); **thirty-first cohort plugin to apply D-10** (Fivetran was 0/173 omitted). **D-09 application is shared** — wire `company_name === 'Amplitude '` byte-for-byte (10 bytes; trailing-space-padded; 100 % pad rate; 60/60 listings); the plugin trims to 9-byte `'Amplitude'`. **Third cohort plugin to apply D-09** (after Fivetran's run-292 first-ever trailing-space `'Fivetran '` and sweetgreen's run-314 leading-space `' sweetgreen'`). **Second cohort observation of TRAILING-space D-09 application** — confirms trailing-pad as a recurring sub-axis (Fivetran first, Amplitude second). **D-11 omitted** — 0 of 60 wire department names padded across 23 unique departments — first cohort observation of `':'`-separator functional-sub-department naming convention (`'CFO : Accounting'`, `'CFO : Deal Desk and Sales Planning'`, `'CFO : Financial Planning'`, `'Customer Success : CSA'`, `'Customer Success : TSM'`, plus 18 others); **forty-eighth cohort plugin** with fully-clean department pass-through. **D-08 entity-decode-then-tag-strip** (63rd cohort member). The run-317 probe at start sampled **60 visible roles** (the run-316 seventh-sweep estimate of ~120 keys was probe-counter-inflated by ~2× via dept/office IDs). Selected from the **seventh fresh probe sweep** as the **alphabetically-second live-board hit** after Airtable. The remaining 12 live hits queue for runs #318+ in alphabetical order (`assemblyai` next at run #318 with ~7 roles). |
| shipped    | `source-company-airtable`  | `company`  | **Airtable** (`airtable.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/airtable/jobs`). | Spec 106, run #316. **First plugin in the seventh fresh probe sweep.** Thin wrapper following the `source-company-cerebral` pattern. **Zero structural deviations** from the Cerebral template — making this the **fourteenth** Greenhouse-only company-direct plugin in run-history to ship as a clean re-spin. All five primary axes share with Cerebral: D-04 wire-shape variant 2 (canonical Greenhouse host `job-boards.greenhouse.io/airtable/jobs/<id>` — **thirty-first** plugin to use variant 2), D-08 entity-decode-then-tag-strip (62nd cohort member), D-09 omitted with case-symmetric bare-brand wire (wire `company_name === 'Airtable'` byte-for-byte — 8 bytes; case-symmetric with the lowercase 8-byte slug `airtable`; **fifty-fourth cohort plugin to omit D-09**), D-10 applied (1 of 27 wire titles padded with single-trailing-ASCII-space form — ~3.7 % pad rate; **thirtieth cohort plugin to apply D-10**), D-11 omitted (0 of 27 wire department names padded across 11 unique departments — `'Accounting'`, `'Customer Success & Services'`, `'Customer Support'`, `'Data'`, `'Demand Generation'`, `'Design'`, `'Engineering'`, `'Legal'`, plus 3 others — clean multi-token forms; **forty-seventh cohort plugin** with fully-clean department pass-through). The run-316 probe at start sampled **27 visible roles** (matched probe-counter exactly — minimal inflation). **Run #316 launches the seventh fresh probe sweep** — probed ~80 candidate slugs and found **14 fresh non-empty live hits** forming the new candidate pool: `airtable` (27, run #316 shipped — this row), `amplitude` (~120 keys), `assemblyai` (~7), `bandwidth` (~45), `braze` (~414), `constantcontact` (~168), `descript` (~25), `fastly` (~256), `launchdarkly` (~135), `okta` (~3222 keys → likely very large via probe-counter inflation), `otter` (~156), `pagerduty` (~96), `pendo` (~18), `vonage` (~81). Plus deferred-empty: `hubspot` HTTP 200 with 0 jobs (still on the rolling re-probe schedule). The remaining 13 live hits queue for runs #317+ in alphabetical order (`amplitude` next at run #317 with ~120 keys → likely ~40 actual via probe-counter inflation). |
| shipped    | `source-company-xai`       | `company`  | **xAI** (`x.ai`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/xai/jobs`). | Spec 105, run #315. **Sixteenth plugin in the sixth fresh probe sweep.** Thin wrapper following the `source-company-adyen` pattern. **One structural deviation** from Adyen: **D-09 sub-axis** — xAI's wire `company_name === 'xAI'` byte-for-byte (3 bytes; mixed-case TWO-cap with LOWERCASE first letter and UPPERCASE last two letters — `x` at index 0, `A` at index 1, `I` at index 2; case-asymmetric vs the lowercase 3-byte slug `xai` at TWO indices). **First cohort observation of lowercase-first + uppercase-last-two TWO-cap mixed-case in a 3-byte form** — distinct from prior TWO-cap forms in SoFi (Spec 102, caps at 0/2) and StockX (Spec 103, caps at 0/5) which both have an UPPERCASE first letter. xAI inverts the first-letter cap convention, mirroring iPhone-style branding. **Fifty-third cohort plugin to omit D-09**. All other axes share with Adyen: D-04 wire-shape variant 2 (canonical Greenhouse host `job-boards.greenhouse.io/xai/jobs/<id>` — **thirtieth** plugin to use variant 2), D-08 entity-decode-then-tag-strip (61st cohort member), D-10 applied (18 of 249 wire titles padded with single-trailing-ASCII-space form — ~7.2 % pad rate, close to Adyen's ~10 % rate; **twenty-ninth cohort plugin to apply D-10**), D-11 omitted (0 of 249 wire department names padded across 24 unique departments — `'Data Center'`, `'Design'`, `'Engineering'`, `'Finance'`, `'Financial'`, `'Grok for Government'`, `'Human Data'`, `'Information Security'`, plus 16 others; **forty-sixth cohort plugin** with fully-clean department pass-through). The run-315 probe at start sampled **249 visible roles** (the run-300 sixth-sweep estimate of ~498 keys was probe-counter-inflated by ~2× via dept/office IDs). Selected from the **sixth fresh probe sweep** as the **alphabetically-sixteenth live-board hit** after Adyen, Benevity, BILL, Bobbie, Cerebral, Coalition, Dollar Shave Club, HelloFresh, Misfits Market, Monzo, N26, PlanetScale, SoFi, StockX, and sweetgreen. **Sixth-sweep effectively exhausted at this run** — the remaining live hit (`gather` and `calm` were initially deferred-empty in run #300; gather was 0 jobs, calm was 1 job — both flagged for re-probe at sweep close). With xAI shipped, the sixth-fresh-sweep candidate pool of 17 has now produced 16 shipped plugins; the next run (#316) will pivot to a **seventh fresh probe sweep**. |
| shipped    | `source-company-sweetgreen` | `company`  | **sweetgreen** (`sweetgreen.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/sweetgreen/jobs`). | Spec 104, run #314. **Fifteenth plugin in the sixth fresh probe sweep.** Thin wrapper following the `source-company-hellofresh` pattern (variant-26 careers-subdomain cousin). **Two structural deviations** from HelloFresh: (1) **D-04 wire-shape variant 29 (careers-subdomain `careers.sweetgreen.com` + root-level `/jobs/<id>` path-id + query-id, no locale prefix — first cohort observation).** sweetgreen publishes `absolute_url` on `https://careers.sweetgreen.com/jobs/<id>?gh_jid=<id>`. Three sub-axes: (a) careers-subdomain `careers.<brand-domain>` (same as variants 8/21/26); (b) **root-level `/jobs/<id>` path-id (no locale prefix and no `/careers/` parent)** — distinct from variants 21 (`/en/all-jobs/`), 26 (`/global/en/job/`), 27 (`/en-eu/careers/positions/`); plural `/jobs/` collection name; (c) path-id + query-id dual-id. **First** plugin to use **wire-shape variant 29** — the **thirty-second distinct wire-shape variant**. Fallback uses canonical Greenhouse variant-2. (2) **D-09 APPLIED with FIRST-COHORT leading-space pad sub-axis** — all 44 wire `company_name` records carry leading ASCII space (`' sweetgreen'` 11 bytes; trim → 10-byte `'sweetgreen'`; 100 % pad rate). **Second cohort plugin to apply D-09** (after Fivetran's run-292 first-ever trailing-space `'Fivetran '` application) and **first cohort observation of LEADING-space D-09 application** — lifts the trim-directionality sub-axis on D-09 from a single trailing-pad form to a recurring leading-AND-trailing pattern. **D-10 application is shared** with HelloFresh — 3 of 44 wire titles padded with single-trailing-ASCII-space form (~6.8 % pad rate); **twenty-eighth cohort plugin to apply D-10**. **D-11 omitted** with **first-cohort store-location department names** — 0 of 44 wire department names padded across 39 unique department records, but the departments are **store-location identifiers** (e.g. `'16th + Market'`, `'67th + Columbus'`, `'Back Bay'`, `'Bentonville'`, `'Berkeley'`) rather than functional categories — **first cohort observation of departments-as-store-locations under D-11**; **forty-fifth cohort plugin** with fully-clean department pass-through. **D-08 entity-decode-then-tag-strip** (60th cohort member). The run-314 probe at start sampled **44 visible roles** (the run-300 sixth-sweep estimate of ~132 keys was probe-counter-inflated by ~3× via dept/office IDs — same pattern as HelloFresh / Misfits Market). Selected from the **sixth fresh probe sweep** as the **alphabetically-fifteenth live-board hit** after Adyen, Benevity, BILL, Bobbie, Cerebral, Coalition, Dollar Shave Club, HelloFresh, Misfits Market, Monzo, N26, PlanetScale, SoFi, and StockX. The remaining 2 live hits queue for runs #315+ in alphabetical order (`xai` next at run #315 with ~498 keys → likely ~150 actual via probe-counter inflation). |
| shipped    | `source-company-stockx`    | `company`  | **StockX** (`stockx.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/stockx/jobs`). | Spec 103, run #313. **Fourteenth plugin in the sixth fresh probe sweep.** Thin wrapper following the `source-company-datacamp` pattern (D-11 application cousin). **One structural deviation** from DataCamp: **D-11 sub-axis** — StockX's wire shows 5 of 25 listings with `'Customer Service '` trailing-ASCII-space pad (1 of 7 unique department names is padded; ~20 % listing-level pad rate); the plugin trims the wire department name before emit. Distinct from DataCamp's run-291 first-ever leading-pad form. **Seventh cohort plugin to apply D-11** (after Lattice / DataCamp / Typeform / BILL / Dollar Shave Club / HelloFresh). All other axes share with DataCamp: D-04 wire-shape variant 2 (canonical Greenhouse host `job-boards.greenhouse.io/stockx/jobs/<id>` — **twenty-ninth** plugin to use variant 2), D-08 entity-decode-then-tag-strip (59th cohort member), D-09 omitted with PascalCase TWO-cap same-byte-count case-asymmetry (wire `company_name === 'StockX'` byte-for-byte — 6 bytes, PascalCase concat; case-asymmetric at TWO byte indices — `'S'` vs `'s'` at index 0 AND `'X'` vs `'x'` at index 5; **second cohort observation of TWO-cap PascalCase same-byte-count case-asymmetry** after SoFi's run-312 first-ever observation at indices 0/2 — StockX's caps at 0/5 are positionally distinct; **fifty-second cohort plugin to omit D-09**), D-10 omitted (0 of 25 wire titles padded; **twenty-first cohort plugin to omit D-10**). The run-313 probe at start sampled **25 visible roles** (the run-300 sixth-sweep estimate of ~50 keys was probe-counter-inflated by ~2× via dept/office IDs). Selected from the **sixth fresh probe sweep** as the **alphabetically-fourteenth live-board hit** after Adyen, Benevity, BILL, Bobbie, Cerebral, Coalition, Dollar Shave Club, HelloFresh, Misfits Market, Monzo, N26, PlanetScale, and SoFi. The remaining 3 live hits queue for runs #314+ in alphabetical order (`sweetgreen` next at run #314 with ~132 keys → likely ~40 actual via probe-counter inflation). |
| shipped    | `source-company-sofi`      | `company`  | **SoFi** (`sofi.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/sofi/jobs`). | Spec 102, run #312. **Thirteenth plugin in the sixth fresh probe sweep.** Thin wrapper following the `source-company-klaviyo` pattern (variant 19 cousin). **One structural deviation** from Klaviyo: **D-04 wire-shape variant 28 (bare brand-domain `sofi.com` + `/careers/job/` path + path-id + query-id — first cohort observation; sister to variant 19 with bare-brand-domain instead of `www.`-prefix).** SoFi publishes `absolute_url` on `https://sofi.com/careers/job/<id>?gh_jid=<id>`. **First** plugin to use **wire-shape variant 28** — the **thirty-first distinct wire-shape variant**. Fallback uses canonical Greenhouse variant-2. **D-09 omitted with FIRST-COHORT PascalCase TWO-internal-cap same-byte-count case-asymmetric wire form** — wire `company_name === 'SoFi'` byte-for-byte (4 bytes — fully clean; 0/204 padded). Slug `sofi` is 4 bytes lowercase; case-asymmetric at TWO byte indices — `'S'` vs `'s'` at index 0 AND `'F'` vs `'f'` at index 2. **First** cohort observation of TWO-cap PascalCase same-byte-count case-asymmetry — distinct from prior single-cap forms in DataCamp (Spec 075, cap at byte 4), HelloFresh (Spec 097, cap at byte 5), and PlanetScale (Spec 101, cap at byte 6). **Fifty-first cohort plugin to omit D-09**. **D-10 applied with trailing-pad form** — 8 of 204 wire titles padded with single-trailing-ASCII-space form (~3.9 % pad rate); **twenty-seventh cohort plugin to apply D-10**. **D-11 omitted** — 0 of 204 wire department names padded across 45 unique departments (`'Accounting'`, `'Affiliates'`, `'BUL'`, `'Banking Ops'`, `'Business Controls and Complaints'`, `'Capital Markets'`, `'Comms & Policy'`, `'Compliance'`, plus 37 others — clean multi-token forms with internal whitespace, ampersands, and acronyms); **forty-fourth cohort plugin** with fully-clean department pass-through. **D-08 entity-decode-then-tag-strip** (58th cohort member). The run-312 probe at start sampled **204 visible roles** (the run-300 sixth-sweep estimate of ~2652 keys was probe-counter-inflated by ~13× via dept/office IDs — the highest probe-counter inflation observed in run history, consistent with SoFi's 45 unique departments + many offices pushing nested ID records). Selected from the **sixth fresh probe sweep** as the **alphabetically-thirteenth live-board hit** after Adyen, Benevity, BILL, Bobbie, Cerebral, Coalition, Dollar Shave Club, HelloFresh, Misfits Market, Monzo, N26, and PlanetScale. The remaining 4 live hits queue for runs #313+ in alphabetical order (`stockx` next at run #313 with ~50 keys → likely ~15 actual via probe-counter inflation). |
| shipped    | `source-company-planetscale` | `company`  | **PlanetScale** (`planetscale.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/planetscale/jobs`). | Spec 101, run #311. **Twelfth plugin in the sixth fresh probe sweep.** Thin wrapper following the `source-company-coursera` pattern. **Zero structural deviations** from the Coursera template — making this the **thirteenth** Greenhouse-only company-direct plugin in run-history to ship as a clean re-spin (after Coursera off Chime, Flexport off Faire, Glossier off Flexport, Marqeta off Calendly, New Relic off Maven Clinic, Scopely off Marqeta, Adyen off Marqeta, Bobbie off Coursera, Cerebral off Adyen, Misfits Market off New Relic, Monzo off Adyen, plus a corrected count). All five primary axes share with Coursera: D-04 wire-shape variant 2 (canonical Greenhouse host `job-boards.greenhouse.io/planetscale/jobs/<id>` — **twenty-eighth** plugin to use variant 2), D-08 entity-decode-then-tag-strip (57th cohort member), D-09 omitted with **PascalCase same-byte-count case-asymmetric wire form** (wire `company_name === 'PlanetScale'` byte-for-byte — 11 bytes, PascalCase concat; same byte-count as the lowercase 11-byte slug `planetscale` but byte-distinct via case alone at byte index 6 — `'S'` vs `'s'`; same case-only-asymmetric same-byte-count shape as DataCamp / HelloFresh; **fiftieth cohort plugin to omit D-09**), D-10 omitted (0 of 6 wire titles padded; **twentieth cohort plugin to omit D-10**), D-11 omitted (0 of 6 wire department names padded — `'Customer Engineering'`, `'Engineering'`, `'Marketing'`, `'Sales'` — clean multi-token forms; **forty-third cohort plugin** with fully-clean department pass-through). The run-311 probe at start sampled **6 visible roles** (the run-300 sixth-sweep estimate of ~6 keys matched the actual job count exactly — minimal probe-counter inflation, similar to Adyen / BILL / Bobbie / Cerebral / Dollar Shave Club / Monzo). Selected from the **sixth fresh probe sweep** as the **alphabetically-twelfth live-board hit** after Adyen, Benevity, BILL, Bobbie, Cerebral, Coalition, Dollar Shave Club, HelloFresh, Misfits Market, Monzo, and N26. The remaining 5 live hits queue for runs #312+ in alphabetical order (`sofi` next at run #312 with ~2652 keys → likely large via probe-counter inflation). |
| shipped    | `source-company-n26`       | `company`  | **N26** (`n26.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/n26/jobs`). | Spec 100, run #310. **Eleventh plugin in the sixth fresh probe sweep**; **the 100th source-company spec** in the catalogue (counting the contiguous Spec 020..100 sequence). Thin wrapper following the `source-company-epicgames` pattern (variant 13 bare-brand-domain `/careers/jobs/<id>?gh_jid=<id>` cousin). **Two structural deviations** from Epic Games: (1) **D-04 wire-shape variant 27 (bare brand-domain `n26.com` + locale-region single-segment prefix `/en-eu/` + `/careers/positions/` path + path-id + query-id — first cohort observation).** N26 publishes `absolute_url` on `https://n26.com/en-eu/careers/positions/<id>?gh_jid=<id>` with four sub-axes: (a) bare brand-domain (same as variants 13/15/18/23); (b) **locale-region single-segment prefix `/en-eu/`** — first cohort observation of a hyphenated locale-region single-segment form (distinct from variant 21's `/en/` locale-only and variant 26's `/global/en/` two-segment form); (c) **`/careers/positions/<id>` path** — `/careers/` ancestor with `/positions/` parent — first cohort observation of `/positions/` parent (vs Epic Games' `/jobs/`, Klaviyo's `/job/`, Lookout's `/job-post/`, Benevity's `/job-posting/`); (d) path-id + query-id dual-id form (same as variants 13 / 26). **First** plugin to use **wire-shape variant 27** — the **thirtieth distinct wire-shape variant**. Fallback uses canonical Greenhouse variant-2. (2) **D-09 omitted with FIRST-COHORT all-caps-letter + digits short wire form** — wire `company_name === 'N26'` byte-for-byte (3 bytes — fully clean; 0/47 padded). Slug `n26` is 3 bytes lowercase; case-asymmetric ON THE LETTER ONLY (`'N'` vs `'n'`) — the digits `26` are byte-identical between slug and wire. **First** cohort observation of a wire `company_name` that combines an uppercase letter with digits in a compact 3-byte form (BILL was the prior all-caps short wire form at 4 bytes but contained no digits). **Forty-ninth cohort plugin to omit D-09**. **D-10 application is shared** with Epic Games — 2 of 47 wire titles padded with single-trailing-ASCII-space form (~4.3 % pad rate — close to Epic Games' ~2.7 % rate); **twenty-sixth cohort plugin to apply D-10**. **D-11 omitted** — 0 of 47 wire department names padded (`'Banking Operations'`, `'Business'`, `'Executive Office'`, `'Finance'`, `'Group Internal Audit'`, `'Legal'`, `'Marketing'`, `'Operations'`, plus 12 others — clean multi-token forms); **forty-second cohort plugin** with fully-clean department pass-through. **D-08 entity-decode-then-tag-strip** (56th cohort member). The run-310 probe at start sampled **47 visible roles** (the run-300 sixth-sweep estimate of ~94 keys was probe-counter-inflated by ~2× via dept/office IDs). Selected from the **sixth fresh probe sweep** as the **alphabetically-eleventh live-board hit** after Adyen, Benevity, BILL, Bobbie, Cerebral, Coalition, Dollar Shave Club, HelloFresh, Misfits Market, and Monzo. The remaining 6 live hits queue for runs #311+ in alphabetical order (`planetscale` next at run #311 with ~6 roles). |
| shipped    | `source-company-monzo`     | `company`  | **Monzo** (`monzo.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/monzo/jobs`). | Spec 099, run #309. **Tenth plugin in the sixth fresh probe sweep.** Thin wrapper following the `source-company-adyen` pattern. **Zero structural deviations** from the Adyen template — making this the **twelfth** Greenhouse-only company-direct plugin in run-history to ship as a clean re-spin (after Coursera off Chime, Flexport off Faire, Glossier off Flexport, Marqeta off Calendly, New Relic off Maven Clinic, Scopely off Marqeta, Adyen off Marqeta, Bobbie off Coursera, Cerebral off Adyen, Misfits Market off New Relic, plus a corrected count). All five primary axes share with Adyen: D-04 wire-shape variant 2 (canonical Greenhouse host `job-boards.greenhouse.io/monzo/jobs/<id>` — **twenty-seventh** plugin to use variant 2), D-08 entity-decode-then-tag-strip (55th cohort member), D-09 omitted with case-symmetric bare-brand wire (wire `company_name === 'Monzo'` byte-for-byte — 5 bytes; case-symmetric with the lowercase 5-byte slug `monzo`; **forty-eighth cohort plugin to omit D-09**), D-10 applied (6 of 65 wire titles padded with single-trailing-ASCII-space form — ~9.2 % pad rate, close to Adyen's ~10 % and Faire's ~9.7 %; **twenty-fifth cohort plugin to apply D-10**), D-11 omitted (0 of 65 wire department names padded — `'Engineering'`, `'Data'`, `'Design'`, `'Finance'`, `'Legal'`, `'Marketing & Community'`, `'Information Security'`, `'Borrowing'`, `'Company Operations'`, `'Customer Operations'`, plus 5 others — clean multi-token forms with internal whitespace and ampersands; **forty-first cohort plugin** with fully-clean department pass-through). The run-309 probe at start sampled **65 visible roles** (the run-300 sixth-sweep estimate of ~65 keys matched the actual job count exactly — minimal probe-counter inflation, similar to BILL/Adyen/Bobbie/Cerebral/Dollar Shave Club). Selected from the **sixth fresh probe sweep** as the **alphabetically-tenth live-board hit** after Adyen, Benevity, BILL, Bobbie, Cerebral, Coalition, Dollar Shave Club, HelloFresh, and Misfits Market. The remaining 7 live hits queue for runs #310+ in alphabetical order (`n26` next at run #310 with ~94 keys → likely ~30 actual via probe-counter inflation). |
| shipped    | `source-company-misfitsmarket` | `company`  | **Misfits Market** (`misfitsmarket.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/misfitsmarket/jobs`). | Spec 098, run #308. **Ninth plugin in the sixth fresh probe sweep.** Thin wrapper following the `source-company-newrelic` pattern. **Zero structural deviations** from the New Relic template — making this the **eleventh** Greenhouse-only company-direct plugin in run-history to ship as a clean re-spin (after Coursera off Chime, Flexport off Faire, Glossier off Flexport, Marqeta off Calendly, New Relic off Maven Clinic, Scopely off Marqeta, Adyen off Marqeta, Bobbie off Coursera, Cerebral off Adyen, plus a corrected count). All five primary axes share with New Relic: D-04 wire-shape variant 2 (canonical Greenhouse host `job-boards.greenhouse.io/misfitsmarket/jobs/<id>` — **twenty-sixth** plugin to use variant 2), D-08 entity-decode-then-tag-strip (54th cohort member), D-09 omitted with **internal-whitespace wire asymmetry** (wire `company_name === 'Misfits Market'` byte-for-byte — 14 bytes; two-word brand with internal ASCII space at index 7; case-AND length-asymmetric vs the lowercase 13-byte concatenated slug `misfitsmarket`; **sixth** internal-whitespace asymmetry case in the cohort after Scale AI / Maven Clinic / Stitch Fix / New Relic / Dollar Shave Club; **forty-seventh cohort plugin to omit D-09**), D-10 applied (6 of 44 wire titles padded with single-trailing-ASCII-space form — ~13.6 % pad rate, mid-range cohort position; **twenty-fourth cohort plugin to apply D-10**), D-11 omitted (0 of 44 wire department names padded — `'Distribution'`, `'Marketing'`, `'Merchandising'`, `'Operations'`, `'Safety'` — clean single-token forms; **fortieth cohort plugin** with fully-clean department pass-through). The run-308 probe at start sampled **44 visible roles** (the run-300 sixth-sweep estimate of ~132 keys was probe-counter-inflated by ~3× via dept/office IDs — same pattern as HelloFresh, Coalition, Peloton, New Relic, Marqeta, DataCamp, Scopely). Selected from the **sixth fresh probe sweep** as the **alphabetically-ninth live-board hit** after Adyen, Benevity, BILL, Bobbie, Cerebral, Coalition, Dollar Shave Club, and HelloFresh. The remaining 8 live hits queue for runs #309+ in alphabetical order (`monzo` next at run #309 with ~65 keys → likely ~20 actual via probe-counter inflation). |
| shipped    | `source-company-hellofresh` | `company`  | **HelloFresh** (`hellofresh.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/hellofresh/jobs`). | Spec 097, run #307. **Eighth plugin in the sixth fresh probe sweep.** Thin wrapper following the `source-company-billcom` pattern (D-10+D-11 dual-application cousin). **Three structural deviations** from BILL: (1) **D-04 wire-shape variant 26 (brand-host careers-subdomain `careers.hellofresh.com` + dual-segment locale prefix `/global/en/` + path-id `/job/<id>` + single-`gh_jid` query — first cohort observation).** Two new sub-axes: (a) **dual-segment locale prefix starting with `/global/` region-cluster ancestor** (vs Peloton's variant 21 single-segment `/en/`); (b) **path-id + query-id dual-id form** (vs BILL's variant 24 bare-query-token + gh_jid). **First** plugin on **wire-shape variant 26** — the **twenty-ninth distinct wire-shape variant**. (2) **D-09 omitted with case-asymmetric PascalCase wire form** — wire `company_name === 'HelloFresh'` byte-for-byte (10 bytes, PascalCase concat; same byte-count as the lowercase 10-byte slug `hellofresh` but byte-distinct via case at byte index 5 — `'F'` vs `'f'`); same case-only-asymmetric same-byte-count shape as DataCamp (Spec 075); 0 of 368 padded; **forty-sixth cohort plugin to omit D-09**. (3) **D-11 applied with medium-pad-rate trailing-pad form** (vs BILL's high ~39.1 %) — 43 of 368 listings carry `departments[0].name` with single-trailing-ASCII-space padding (~11.7 % listing-level pad rate); **sixth cohort plugin to apply D-11** (after Lattice / DataCamp / Typeform / BILL / Dollar Shave Club). **D-10 application is shared** with BILL — 57 of 368 wire titles padded with single-trailing-ASCII-space form (~15.5 % pad rate; mid-range vs prior cohort: Squarespace ~25 %, New Relic ~21.6 %, Adyen ~10 %, Faire ~9.7 %); **twenty-third cohort plugin to apply D-10**. **D-08 entity-decode-then-tag-strip** (53rd cohort member). The run-307 probe at start sampled **368 visible roles** (the run-300 sixth-sweep estimate of ~1104 keys was probe-counter-inflated by ~3× via dept/office IDs — same pattern observed for Coalition, Peloton, New Relic, Marqeta, DataCamp, Scopely). Fallback uses canonical Greenhouse variant-2. Selected from the **sixth fresh probe sweep** as the **alphabetically-eighth live-board hit** after Adyen, Benevity, BILL, Bobbie, Cerebral, Coalition, and Dollar Shave Club. The remaining 9 live hits queue for runs #308+ in alphabetical order (`misfitsmarket` next at run #308 with ~132 keys → likely 30–60 actual roles). |
| shipped    | `source-company-dollarshaveclub` | `company`  | **Dollar Shave Club** (`dollarshaveclub.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/dollarshaveclub/jobs`). | Spec 096, run #306. **Seventh plugin in the sixth fresh probe sweep.** Thin wrapper following the `source-company-cameo` pattern (the closest cousin under the run-275 first-ever D-11-trailing-pad-pass-through axis). **One structural deviation** from the Cameo template: **D-11 APPLIED with trailing-pad form** (vs Cameo's pass-through pad observability lock). Dollar Shave Club's wire shows 1 of 5 `departments[0].name` records padded (`'Legal '` trailing-space; ~20 % listing-level pad rate); the plugin trims the wire department name before emit — distinct from Cameo's run-275 first-ever **partial-pad pass-through** observability lock. **Fifth cohort plugin to apply D-11** (after Lattice's run-284 first-ever trailing-pad, DataCamp's run-291 first-ever leading-pad, Typeform's run-299 second trailing-pad, and BILL's run-302 high-pad-rate trailing). All other axes share with Cameo: D-04 wire-shape variant 2 (canonical Greenhouse host `job-boards.greenhouse.io/dollarshaveclub/jobs/<id>` — **twenty-fifth** plugin to use variant 2), D-08 entity-decode-then-tag-strip (52nd cohort member), D-09 omitted with **internal-whitespace wire asymmetry** (wire `company_name === 'Dollar Shave Club'` byte-for-byte — 17 bytes; three-word brand with two internal ASCII spaces; case-AND length-asymmetric vs the lowercase 15-byte concatenated slug `dollarshaveclub`; **fifth** internal-whitespace asymmetry case in the cohort after Scale AI / Maven Clinic / Stitch Fix / New Relic; **forty-fifth cohort plugin to omit D-09**), D-10 omitted (0 of 5 wire titles padded; **nineteenth cohort plugin to omit D-10**). The run-306 probe at start sampled **5 visible roles** (the run-300 sixth-sweep estimate of ~5 keys matched the actual job count exactly — minimal probe-counter inflation). Selected from the **sixth fresh probe sweep** as the **alphabetically-seventh live-board hit** after Adyen, Benevity, BILL, Bobbie, Cerebral, and Coalition. The remaining 10 live hits queue for runs #307+ in alphabetical order (`hellofresh` next at run #307 with ~1104 keys → likely large). |
| shipped    | `source-company-coalition` | `company`  | **Coalition** (`coalitioninc.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/coalition/jobs`). | Spec 095, run #305. **Sixth plugin in the sixth fresh probe sweep.** Thin wrapper following the `source-company-billcom` pattern (the run-history-adjacent www-prefixed-slug-divergent-vanity cousin). **Three structural deviations** from the BILL template: (1) **D-04 wire-shape variant 25 (`www.`-prefixed slug-divergent vanity domain `coalitioninc.com` + root-level `/job-posting` (singular hyphenated) + single-`gh_jid` query — first cohort observation).** Coalition publishes `absolute_url` on `https://www.coalitioninc.com/job-posting?gh_jid=<id>`. Three sub-axes: (a) `www.`-prefixed brand-domain (same as variants 16/19/20/24); (b) **slug-divergent vanity** — slug `coalition` is a 9-byte bare brand-stem; domain `coalitioninc.com` concatenates legal-entity suffix `inc` AND TLD-stem `.com` — **second** cohort observation of slug-divergent vanity (after BILL's run-302 first observation `billcom` vs `bill.com`); (c) root-level `/job-posting` (singular hyphenated) with single-`gh_jid` query — same path-segment as variant 23 (Benevity) but with `www.` prefix. **First** plugin to use **wire-shape variant 25** — the **twenty-eighth distinct wire-shape variant**. Fallback uses canonical Greenhouse variant-2. (2) **D-09 omitted with FIRST-COHORT legal-entity comma-suffix wire form** — wire `company_name === 'Coalition, Inc.'` byte-for-byte (15 bytes — fully clean). The wire **includes** the comma-space-`Inc.` legal-entity suffix — the **first** cohort observation of a wire `company_name` that carries an embedded legal-entity suffix. All prior 42 D-09-omission plugins carried bare-brand wire forms; Coalition reverses the convention. **Forty-fourth cohort plugin to omit D-09**. (3) **D-11 omitted** (BILL applied with high-pad-rate trailing-pad form). 0 of 19 wire department names padded (`'Claims'`, `'Customer Success'`, `'Data'`, `'Engineering'`, `'Incident Response (CIR)'`, `'Legal'`, `'Security Engineering'`, `'Security Sales'` — clean multi-token forms with parenthesised initialism); **thirty-ninth cohort plugin** with fully-clean department pass-through. **D-10 application is shared** with BILL (Coalition is the **twenty-second cohort plugin to apply D-10**) but Coalition opens a **first-cohort leading-double-space pad sub-axis** — `'  Senior Incident Response Analyst'` carries 2 leading ASCII spaces; all prior leading-pad observations were single-leading-space, and Scopely's run-297 multi-byte trailing was the trailing equivalent. Standard `String.prototype.trim()` strips arbitrary-length runs. **D-08 entity-decode-then-tag-strip** (51st cohort member). The run-305 probe at start sampled **19 visible roles** (the run-300 sixth-sweep estimate of ~38 keys was probe-counter-inflated by ~2× via dept/office IDs). Selected from the **sixth fresh probe sweep** as the **alphabetically-sixth live-board hit** after Adyen, Benevity, BILL, Bobbie, and Cerebral. The remaining 11 live hits queue for runs #306+ in alphabetical order (`dollarshaveclub` next at run #306 with ~5 roles). |
| shipped    | `source-company-cerebral`  | `company`  | **Cerebral** (`getcerebral.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/cerebral/jobs`). | Spec 094, run #304. **Fifth plugin in the sixth fresh probe sweep.** Thin wrapper following the `source-company-adyen` pattern. **Zero structural deviations** from the Adyen template — making this the **tenth** Greenhouse-only company-direct plugin in run-history to ship as a clean re-spin (after Coursera off Chime, Flexport off Faire, Glossier off Flexport, Marqeta off Calendly, New Relic off Maven Clinic, Scopely off Marqeta, Adyen off Marqeta, Bobbie off Coursera, plus a corrected count). All five primary axes share with Adyen: D-04 wire-shape variant 2 (canonical Greenhouse host `job-boards.greenhouse.io/cerebral/jobs/<id>` — **twenty-fourth** plugin to use variant 2), D-08 entity-decode-then-tag-strip (50th cohort member), D-09 omitted with case-symmetric bare-brand wire (wire `company_name === 'Cerebral'` byte-for-byte — 8 bytes; case-symmetric with the lowercase 8-byte slug `cerebral`; **forty-third cohort plugin to omit D-09**), D-10 applied (1 of 6 wire titles padded — `'Therapy Associate - Connecticut '` trailing-space; ~16.7 % pad rate on a small page; **twenty-first cohort plugin to apply D-10**), D-11 omitted (0 of 6 wire department names padded — `'Client Support'`, `'Medical Care'`, `'Behavioral Care'` — clean multi-token forms; **thirty-eighth cohort plugin** with fully-clean department pass-through). The run-304 probe at start sampled **6 visible roles**. Selected from the **sixth fresh probe sweep** as the **alphabetically-fifth live-board hit** after Adyen, Benevity, BILL, and Bobbie. The remaining 12 live hits queue for runs #305+ in alphabetical order (`coalition` next at run #305 with ~38 roles). |
| shipped    | `source-company-bobbie`    | `company`  | **Bobbie** (`hibobbie.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/bobbie/jobs`). | Spec 093, run #303. **Fourth plugin in the sixth fresh probe sweep.** Thin wrapper following the `source-company-coursera` pattern. **Zero structural deviations** from the Coursera template — making this the **ninth** Greenhouse-only company-direct plugin in run-history to ship as a clean re-spin of a prior cohort plugin with no per-axis deviations (after Coursera off Chime, Flexport off Faire, Glossier off Flexport, Marqeta off Calendly, New Relic off Maven Clinic, Scopely off Marqeta, Adyen off Marqeta, and a corrected count for Typeform's near-miss). All five primary axes share with Coursera: D-04 wire-shape variant 2 (canonical Greenhouse host `job-boards.greenhouse.io/bobbie/jobs/<id>` — **twenty-third** plugin to use variant 2), D-08 entity-decode-then-tag-strip (49th cohort member), D-09 omitted with case-symmetric bare-brand wire (wire `company_name === 'Bobbie'` byte-for-byte — 6 bytes; case-symmetric with the lowercase 6-byte slug `bobbie`; **forty-second cohort plugin to omit D-09**), D-10 omitted (0 of 9 wire titles padded; **eighteenth cohort plugin to omit D-10**), D-11 omitted (0 of 9 wire department names padded — `'Operations'`, `'Brand & Marketing'`, `'Talent Pipeline'`, `'Commercial'`, `'Manufacturing'` — clean multi-token forms; **thirty-seventh cohort plugin** with fully-clean department pass-through). The run-303 probe at start sampled **9 visible roles**. Selected from the **sixth fresh probe sweep** as the **alphabetically-fourth live-board hit** after Adyen, Benevity, and BILL. The remaining 13 live hits queue for runs #304+ in alphabetical order (`cerebral` next at run #304 with ~6 roles). |
| shipped    | `source-company-billcom`   | `company`  | **BILL** (`bill.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/billcom/jobs`). | Spec 092, run #302. **Third plugin in the sixth fresh probe sweep.** Thin wrapper following the `source-company-typeform` pattern. **Three structural deviations** from the Typeform template: (1) **D-04 wire-shape variant 24 (`www.`-prefixed slug-divergent vanity domain `bill.com` + bare `/job` segment + DUAL-id query string `?<id>&gh_jid=<id>` — first cohort observation of dual-id query and first slug-vs-domain TLD-stem elision/re-insertion).** BILL publishes `absolute_url` on `https://www.bill.com/job?<id>&gh_jid=<id>` — the slug `billcom` (7 bytes, no-dot brand-stem with concatenated TLD-stem) maps to the customer-facing domain `bill.com` (8 bytes, dotted; brand-stem `bill` + dot + `com` TLD), opening a **first-of-its-kind slug-vs-domain TLD-stem elision/re-insertion sub-axis**; the bare numeric `<id>` query parameter (no key=value form) appears as the first query token immediately after `?`, followed by the canonical `gh_jid=<id>` form, opening a **first-of-its-kind dual-id query string sub-axis**. **First** plugin in the cohort to use **wire-shape variant 24** — the **twenty-seventh distinct wire-shape variant** in the company-direct cohort. Fallback uses canonical Greenhouse variant-2. (2) **D-09 omitted with slug-divergent uppercase 4-byte wire form** — wire `company_name === 'BILL'` byte-for-byte (4 bytes, uppercase post-2022-rebrand brand); slug `billcom` is 7 bytes, lowercase, **case-asymmetric AND length-divergent** from wire — first cohort observation of slug-vs-wire substring-divergence (wire `'bill'` casefolded is a 4-byte prefix of slug `'billcom'`). Second slug-divergence overall after Peloton's run-296 vanity-domain divergence. **Forty-first cohort plugin to omit D-09**. (3) **D-10 applied with first-cohort-observation leading-TAB pad sub-axis** — 2 of 46 wire titles padded (~4.3 %): one trailing-ASCII-space (`'Associate Fraud Strategy Data Scientist '`), one **leading-TAB** (`'\tSenior Product Manager - Developer Ecosystem & Partner Platform'` — first cohort observation of TAB U+0009 pad byte under D-10; standard `String.prototype.trim()` strips TAB so implementation is byte-identical to peers — only the wire-pad axis is novel). **Twentieth cohort plugin to apply D-10**. **D-11 applied with high-pad-rate trailing-pad form** — 18 of 46 listings padded with single-trailing-ASCII-space form (`'Engineering '`, `'Marketing '` — 2 of 9 unique department names) — ~39.1 % listing-level pad rate is the **highest D-11 pad rate observed** in the cohort (prior applications: Lattice ~13.6 % unique-form trailing-pad, DataCamp ~9.8 % leading-pad, Typeform ~13.6 % trailing-pad). **Fourth cohort plugin to apply D-11**. Other axes share with Typeform: D-08 entity-decode-then-tag-strip (48th cohort member). The run-302 probe at start sampled **46 visible roles**. Selected from the **sixth fresh probe sweep** as the **alphabetically-third live-board hit** after Adyen and Benevity. The remaining 14 live hits queue for runs #303+ in alphabetical order (`bobbie` next at run #303 with ~9 roles). |
| shipped    | `source-company-benevity`  | `company`  | **Benevity** (`benevity.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/benevity/jobs`). | Spec 091, run #301. **Second plugin in the sixth fresh probe sweep.** Thin wrapper following the `source-company-lookout` pattern. **One structural deviation** from the Lookout template: **D-04 wire-shape variant 23 (bare brand-domain `/job-posting` singular-hyphenated query-only-id — first cohort observation).** Benevity publishes `absolute_url` on a previously-unobserved shape `https://benevity.com/job-posting?gh_jid=<id>` with three distinguishing sub-axes: (a) bare brand-domain (same as variants 13/15/18); (b) **no `/careers/` prefix** — `/job-posting` is the entire path segment with no ancestor `/careers/`; same root-level positioning as variant 15 (Lattice's `/job`); distinct from variants 13/18/19/20 which all carry `/careers/` as the path-prefix; (c) `/job-posting` singular-hyphenated — distinct from variant 15's bare `/job` (no hyphen, no suffix), variant 20's `/careers/job-post` (`/careers/` prefix + slightly-different hyphenated form), and variant 19's `/careers/job` (`/careers/` prefix + bare `/job`). **First** plugin in the cohort to use **wire-shape variant 23** — the **twenty-sixth distinct wire-shape variant** in the company-direct cohort. Fallback uses canonical Greenhouse variant-2. All other axes share with Lookout: D-08 entity-decode-then-tag-strip (47th cohort member), D-09 omitted with case-symmetric bare-brand wire (wire `company_name === 'Benevity'` byte-for-byte — 8 bytes; **fortieth cohort plugin to omit D-09**), D-10 omitted (0 of 25 wire titles padded; **seventeenth cohort plugin to omit D-10**), D-11 omitted (0 of 25 wire department names padded — `'Engineering'`, `'Sales'`, `'Growth Marketing'`, `'Technology Operations'`, `'Finance'` — clean multi-token forms; **thirty-sixth cohort plugin** with fully-clean department pass-through). The run-301 probe at start sampled **25 visible roles**. Selected from the **sixth fresh probe sweep** as the **alphabetically-second live-board hit** after Adyen. The remaining 15 live hits queue for runs #302+ in alphabetical order (`billcom` next at run #302 with ~46 keys). |
| shipped    | `source-company-adyen`     | `company`  | **Adyen** (`adyen.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/adyen/jobs`). | Spec 090, run #300. **First plugin in the sixth fresh probe sweep.** Thin wrapper following the `source-company-marqeta` pattern. **Zero structural deviations** from the Marqeta template — making this the **eighth** Greenhouse-only company-direct plugin in run-history to ship as a clean re-spin (after Coursera off Chime, Flexport off Faire, Glossier off Flexport, Marqeta off Calendly, New Relic off Maven Clinic, Scopely off Marqeta, and Typeform-which-was-actually-one-deviation off Lattice). All five primary axes share with Marqeta: D-04 wire-shape variant 2 (canonical Greenhouse host — **twenty-second** plugin to use variant 2), D-08 entity-decode-then-tag-strip (46th cohort member), D-09 omitted with case-symmetric bare-brand wire (wire `company_name === 'Adyen'` byte-for-byte — 5 bytes; **thirty-ninth cohort plugin to omit D-09**), D-10 applied (26 of 260 wire titles padded — ~10 % pad rate; **twenty-fifth cohort plugin to apply D-10**), D-11 fully-clean department pass-through (0 of 260 wire department names padded — `'Account Management'`, `'Finance'`, `'Merchant Operations'`, `'Infrastructure'`, `'Compliance'` — clean multi-token forms; **thirty-fifth cohort plugin** with fully-clean department pass-through). The run-300 probe at start sampled **260 visible roles** via direct curl probe of `https://api.greenhouse.io/v1/boards/adyen/jobs?content=true`. **Run #300 launches the sixth fresh probe sweep** — probed ~80 candidate slugs and found **17 fresh non-empty live hits** forming the new candidate pool: `adyen` (260, run #300 shipped — this row), `benevity` (~25 keys), `billcom` (~46), `bobbie` (~9), `cerebral` (~6), `coalition` (~38), `dollarshaveclub` (~5), `hellofresh` (~1104 keys → likely large), `misfitsmarket` (~132 keys), `monzo` (~65), `n26` (~94), `planetscale` (~6), `sofi` (~2652 keys → very large), `stockx` (~50), `sweetgreen` (~132 keys), `xai` (~498 keys). Plus 2 deferred-empty (`gather` HTTP 200 with 0 jobs, `calm` HTTP 200 with 1 job — verify count). The remaining 16 live hits queue for runs #301+ in alphabetical order (`benevity` next at run #301). |
| shipped    | `source-company-typeform`  | `company`  | **Typeform** (`typeform.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/typeform/jobs`). | Spec 089, run #299. **Eleventh and last plugin in the fifth fresh probe sweep.** Thin wrapper following the `source-company-lattice` pattern. **One structural deviation** from the Lattice template — D-04 wire-shape variant 2 (canonical Greenhouse host; Lattice variant 15 bare brand-domain). All other axes share with Lattice: D-08 entity-decode-then-tag-strip (45th cohort member), D-09 omitted with case-symmetric bare-brand wire (wire `company_name === 'Typeform'` byte-for-byte; **thirty-eighth cohort plugin to omit D-09**), D-10 omitted (0 of 22 wire titles padded; **sixteenth cohort plugin to omit D-10**), **D-11 applied with trailing-pad form** (3 of 22 wire department names padded — `'Product '` × 2 plus one other trailing-pad case; ~13.6 % pad rate; **third cohort plugin to apply D-11** after Lattice's run-284 first-ever trailing-pad application and DataCamp's run-291 first-ever leading-pad application; Typeform's trailing-pad form lifts Lattice's first-ever observation from a one-off to a recurring axis). With Typeform shipped, the **fifth-fresh-sweep candidate pool is fully exhausted** (all 11 live hits — Bitwarden, Calendly, DataCamp, Fivetran, Lookout, Marqeta, New Relic, Peloton, Scopely, Squarespace, and Typeform — shipped across runs #289–#299). Subsequent runs (#300+) will pivot to a **sixth fresh probe sweep**. |
| shipped    | `source-company-squarespace` | `company` | **Squarespace** (`squarespace.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/squarespace/jobs`). | Spec 088, run #298. **Tenth plugin in the fifth fresh probe sweep.** Thin wrapper following the `source-company-marqeta` pattern. **One structural deviation** from the Marqeta template: **D-04 wire-shape variant 22 (HTTP-scheme `www.`-prefixed brand-domain `/about/careers` query-only-id — first cohort observation of HTTP scheme).** Squarespace publishes `absolute_url` on a previously-unobserved shape `http://www.squarespace.com/about/careers?gh_jid=<id>` with three new sub-axes: (a) **HTTP scheme** (not HTTPS) — distinct from every prior cohort variant which all use HTTPS; **first cohort observation of HTTP scheme** in the wire URL (the `http://` scheme is not a security concern — HSTS preload auto-upgrades in all modern browsers — but is a wire-side anomaly worth capturing); (b) `www.`-prefixed brand-domain — same as variants 16/19/20; (c) `/about/careers` path — distinct from variant 19's `/careers/job` and variant 20's `/careers/job-post` via the `/about/` marketing-page ancestor segment. **First** plugin in the cohort to use **wire-shape variant 22** — the **twenty-fifth distinct wire-shape variant** in the company-direct cohort. Fallback uses canonical Greenhouse variant-2 (HTTPS — same fallback strategy as ClassPass / Epic Games / fuboTV / Lattice / Stitch Fix / Udemy / Bitwarden / Fivetran / Lookout / Peloton). All other axes share with Marqeta: D-08 entity-decode-then-tag-strip (44th cohort member), D-09 omitted with case-symmetric bare-brand wire (wire `company_name === 'Squarespace'` byte-for-byte; **thirty-seventh cohort plugin to omit D-09**), D-10 applied (9 of 36 wire titles padded — ~25 % rate, all trailing-only; e.g. `'Connections & Community Lead '`, `'Lead FP&A Analyst, Consolidations '`, `'Manager, Detection & Incident Response '`; **twenty-fourth cohort plugin to apply D-10**), D-11 fully-clean department pass-through (0 of 36 wire department names padded — `'Engineering (Domains & Apps)'`, `'People'`, `'Finance'`, `'Domains & Applications'`, `'Legal & Communications'`, `'Engineering'`, `'Product'`, `'Customer Operations'` — clean multi-token forms with internal whitespace, ampersands, and parentheses; **thirty-fourth cohort plugin** with fully-clean department pass-through). The run-298 probe at start sampled **36 visible roles** (the run-289 probe-counter estimate of ~72 was inflated). Selected from the **fifth fresh probe sweep** as the **alphabetically-tenth live-board hit** after Bitwarden, Calendly, DataCamp, Fivetran, Lookout, Marqeta, New Relic, Peloton, and Scopely. The remaining one live hit queues for run #299 (`typeform` ~132). After pool exhaustion (#300+) runs will pivot to a **sixth fresh probe sweep**. |
| shipped    | `source-company-scopely`   | `company`  | **Scopely** (`scopely.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/scopely/jobs`). | Spec 087, run #297. **Ninth plugin in the fifth fresh probe sweep.** Thin wrapper following the `source-company-marqeta` pattern. **Zero structural deviations** from the Marqeta template — making this the **seventh** Greenhouse-only company-direct plugin in run-history to ship as a clean re-spin (after Coursera off Chime at run #278, Flexport off Faire at run #280, Glossier off Flexport at run #282, Marqeta off Calendly at run #294, and New Relic off Maven Clinic at run #295). All five primary axes share with Marqeta: D-04 wire-shape variant 2 (canonical Greenhouse host — **twentieth** plugin in the cohort to use variant 2), D-08 entity-decode-then-tag-strip (43rd cohort member), D-09 omitted with case-symmetric bare-brand wire (wire `company_name === 'Scopely'` byte-for-byte; case-symmetric with the lowercase 7-byte slug `scopely`; no legal-entity suffix on the wire — distinct from the legal-entity name "Scopely Inc." that appears in current SEC filings; **thirty-sixth cohort plugin to omit D-09**), D-10 applied (17 of 170 wire titles in the run-297 probe carry pad bytes — ~10.0 % pad rate; pad-form distribution: 2 leading-only, 12 trailing-only, 2 dual (`' D2C Program Manager '`, `' Senior Performance Marketing Manager '` — **second cohort observation of dual-pad on title axis** after New Relic's run-295 single dual-pad case; Scopely lifts dual-pad from a one-off to a recurring observation), 1 multi-byte trailing (`'Senior Software Engineer - Pikmin Bloom   '` — 3 trailing ASCII spaces; **first cohort observation of multi-byte trailing pad**), 1 NBSP-trailing (`'Senior Analytics Engineer '` — U+00A0 non-breaking space; **first cohort observation of NBSP pad byte**); standard `String.prototype.trim()` strips all five sub-axes in a single call — no implementation change vs Marqeta; **twenty-third cohort plugin to apply D-10**), D-11 fully-clean department pass-through (0 of 170 wire department names in the run-297 probe carry pad bytes — `'Slate Development Group'`, `'MonopolyGo'`, `'Finance'`, `'People'`, `'Legal'`, `'Slate Portfolio'`, `'Publishing'`, `'Operations'`, `'Niantic'`, `'Live Games Portfolio'`, `'Corporate'`, `'Playgami'` — clean multi-token forms with internal whitespace; **the `'Niantic'` and `'Playgami'` department names are operating-division banners reflecting the post-acquisition structure** — Scopely acquired Niantic's games division for $3.5B in September 2024 — not separate Greenhouse tenants; all 170 listings carry `company_name === 'Scopely'`; **thirty-third cohort plugin** with fully-clean department pass-through). The run-297 probe at start sampled **170 visible roles** (the run-289 probe-counter estimate of ~1190 was inflated by counting all `"id":` JSON keys including department/office IDs — same probe-counter inflation pattern observed for Peloton, New Relic, Marqeta, and DataCamp). Selected from the **fifth fresh probe sweep** as the **alphabetically-ninth live-board hit** after Bitwarden, Calendly, DataCamp, Fivetran, Lookout, Marqeta, New Relic, and Peloton. The remaining two live hits queue for runs #298+ in alphabetical order (`squarespace` next at run #298 with ~72 roles; `typeform` ~132). After pool exhaustion (#300+) runs will pivot to a **sixth fresh probe sweep**. |
| shipped    | `source-company-peloton`   | `company`  | **Peloton** (`onepeloton.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/peloton/jobs`). | Spec 086, run #296. **Eighth plugin in the fifth fresh probe sweep.** Thin wrapper following the `source-company-marqeta` pattern. **One structural deviation** from the Marqeta template: **D-04 wire-shape variant 21 (brand-host careers-subdomain with locale-prefix and multi-segment listing path with trailing slash, followed by `?gh_jid=<id>`).** Peloton publishes `absolute_url` on a previously-unobserved shape `https://careers.onepeloton.com/en/all-jobs/?gh_jid=<id>` (`careers.onepeloton.com` brand-host careers-subdomain — distinct from Toast's variant 8 `careers.toasttab.com/jobs?gh_jid=<id>` — single-segment path, no locale prefix, no trailing slash — and ZoomInfo's variant 9 `www.zoominfo.com/careers?gh_jid=<id>` — apex-www, single-segment path; Peloton adds a **locale-prefix path segment** `/en/` followed by a **multi-segment listing path with trailing slash** `/all-jobs/`). **First** plugin in the cohort to use **wire-shape variant 21** — the **twenty-second distinct wire-shape variant** in the company-direct cohort. Fallback uses canonical Greenhouse variant-2 (same fallback strategy as ClassPass / Epic Games / fuboTV / Lattice / Stitch Fix / Udemy / Bitwarden / Fivetran / Lookout). All other axes share with Marqeta: D-08 entity-decode-then-tag-strip (42nd cohort member), D-09 omitted with case-symmetric bare-brand wire (wire `company_name === 'Peloton'` byte-for-byte; case-symmetric with the lowercase 7-byte slug `peloton`; no legal-entity suffix on the wire — distinct from the legal-entity name "Peloton Interactive, Inc." that appears in current SEC filings under NASDAQ ticker `PTON`; **thirty-fifth cohort plugin to omit D-09**), D-10 applied (2 of 52 wire titles in the run-296 probe carry trailing ASCII-space padding — `'Senior Full Stack Software Engineer, Device Services '`, `'Software Engineer III, Social '`; ~3.85 % pad rate — **new cohort low**, undercutting Calendly's prior 5.0 % low and Marqeta's 6.1 % rate; pad-form distribution: 0 leading-only, 2 trailing-only, 0 dual; **twenty-second cohort plugin to apply D-10**), D-11 fully-clean department pass-through (0 of 52 wire department names in the run-296 probe carry pad bytes — `'Marketing'`, `'Sales'`, `'Data Analytics'`, `'Hardware'`, `'Supply Chain & Logistics'`, `'Software'`, `'People'`, `'Legal'`, `'Studio & Content Production'`, `'Product Development'`, plus two more — clean multi-token forms with internal whitespace and ampersands; **thirty-second cohort plugin** with fully-clean department pass-through). The run-296 probe at start sampled **52 visible roles** (the run-289 probe-counter estimate of ~104 was inflated by counting all `"id":` JSON keys including department/office IDs). Selected from the **fifth fresh probe sweep** as the **alphabetically-eighth live-board hit** after Bitwarden, Calendly, DataCamp, Fivetran, Lookout, Marqeta, and New Relic. The remaining three live hits queue for runs #297+ in alphabetical order (`scopely` next at run #297 with ~1190 roles; `squarespace` ~72; `typeform` ~132). After pool exhaustion (#300+) runs will pivot to a **sixth fresh probe sweep**. |
| shipped    | `source-company-newrelic`  | `company`  | **New Relic** (`newrelic.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/newrelic/jobs`). | Spec 085, run #295. **Seventh plugin in the fifth fresh probe sweep.** Thin wrapper following the `source-company-mavenclinic` pattern. **Zero structural deviations** from the Maven Clinic template — making this the **sixth** Greenhouse-only company-direct plugin in run-history to ship as a clean re-spin (after Coursera off Chime at run #278, Flexport off Faire at run #280, Glossier off Flexport at run #282, and Marqeta off Calendly at run #294). All axes share with Maven Clinic: D-04 wire-shape variant 2 (canonical Greenhouse host — **twentieth** plugin in the cohort to use variant 2), D-08 entity-decode-then-tag-strip (41st cohort member), D-09 omitted with **internal-whitespace wire asymmetry** (wire `company_name === 'New Relic'` byte-for-byte; 9 bytes — two-word brand with internal ASCII space at index 3; slug `newrelic` is 8 bytes — slug/wire-asymmetric, wire LONGER than slug by 1 byte; same shape as Maven Clinic / Stitch Fix / Scale AI; **fourth** internal-whitespace asymmetry case in the cohort; **thirty-fourth cohort plugin to omit D-09**, ninth slug/wire asymmetry case overall), D-10 applied (16 of 74 wire titles in the run-295 probe carry pad bytes — ~21.6 % pad rate, **second-highest D-10 pad rate in cohort history** after fuboTV's run-281 ~91 % outlier; **first cohort observation of BOTH-LEADING-AND-TRAILING-padded title** — `" Account Executive - Commercial "` carries pad bytes on both sides; standard `String.prototype.trim()` handles all three sub-axes (leading-only, trailing-only, dual) in a single call so the implementation is byte-identical to Maven Clinic's; **twenty-first cohort plugin to apply D-10**), D-11 fully-clean department pass-through (0 of 74 wire department names padded — `'Commercial'`, `'Enterprise'`, `'Technical Solution Sales'`, `'G&A, Executive'`, `'Alliances & Channels'`, `'Content, Creative & Communications'`, `'New Relic Global Enablement'` — clean multi-token forms with internal whitespace, ampersands, and commas; **thirty-first cohort plugin** with fully-clean department pass-through). The run-295 probe at start sampled **74 visible roles** (the run-289 probe-counter estimate of ~370 was inflated by counting all `"id":` JSON keys including department/office IDs). Selected from the **fifth fresh probe sweep** as the **alphabetically-seventh live-board hit** after Bitwarden, Calendly, DataCamp, Fivetran, Lookout, and Marqeta. The remaining four live hits queue for runs #296+ in alphabetical order (`peloton` next at run #296 with ~104 roles). |
| shipped    | `source-company-marqeta`   | `company`  | **Marqeta** (`marqeta.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/marqeta/jobs`). | Spec 084, run #294. **Sixth plugin in the fifth fresh probe sweep.** Thin wrapper following the `source-company-calendly` pattern. **Zero structural deviations** from the Calendly template — making this the **fifth** Greenhouse-only company-direct plugin in run-history to ship as a clean re-spin of a prior cohort plugin with no per-axis deviations (after Coursera off Chime at run #278, Flexport off Faire at run #280, and Glossier off Flexport at run #282). All axes share with Calendly: D-04 wire-shape variant 2 (canonical Greenhouse host — **nineteenth** plugin in the cohort to use variant 2), D-08 entity-decode-then-tag-strip (40th cohort member), D-09 omitted with case-symmetric bare-brand wire (wire `company_name === 'Marqeta'` byte-for-byte; case-symmetric with the lowercase slug `marqeta`; no legal-entity suffix on the wire — distinct from the legal-entity name "Marqeta, Inc." that appears in current SEC filings under NASDAQ ticker `MQ`; **thirty-third cohort plugin to omit D-09**), D-10 applied (2 of 33 wire titles in the run-294 probe carry trailing ASCII-space padding — `'Group Product Manager, Fraud '`, `'Senior Director, Global Strategic Partnerships '`; ~6.1 % pad rate, in line with Calendly's 1/20 ~5.0 % rate; **twentieth cohort plugin to apply D-10**), D-11 fully-clean department pass-through (0 of 33 wire department names in the run-294 probe carry pad bytes — `'Risk Operations'`, `'Marketing - General'`, `'Credit Engineering'`, `'Risk, Fraud, Disputes Product'`, `'CyberSecurity'`, `'Core Product'` — clean multi-token forms with internal whitespace, hyphens, and commas; **thirtieth cohort plugin** with fully-clean department pass-through). The run-294 probe at start sampled **33 visible roles** (the run-289 probe-counter estimate of ~330 was inflated by counting all `"id":` JSON keys including department/office IDs). Selected from the **fifth fresh probe sweep** as the **alphabetically-sixth live-board hit** after Bitwarden, Calendly, DataCamp, Fivetran, and Lookout. The remaining five live hits queue for runs #295+ in alphabetical order (`newrelic` next at run #295 with ~370 roles). |
| shipped    | `source-company-lookout`   | `company`  | **Lookout** (`lookout.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/lookout/jobs`). | Spec 083, run #293. **Fifth plugin in the fifth fresh probe sweep.** Thin wrapper following the `source-company-fivetran` pattern. **One structural deviation** from the Fivetran template: **D-04 wire-shape variant 20 (`www.`-prefixed brand-domain singular `/careers/job-post` query-only-id).** Lookout publishes `absolute_url` on a previously-unobserved shape `https://www.lookout.com/careers/job-post?gh_jid=<id>` (`www.`-prefixed brand-domain — same `www.` prefix as variant 19's Fivetran and variant 16's Stitch Fix; `/careers/job-post` SINGULAR path with `-post` suffix — distinct from variant 13's plural-with-id-in-path, variant 16's plural-with-duplicate-query, variant 18's id-in-path-with-trailing-slash, AND variant 19's singular-`/job` without `-post` suffix). **First** plugin in the cohort to use **wire-shape variant 20** — the **twenty-third distinct wire-shape variant** in the company-direct cohort. Fallback uses canonical Greenhouse variant-2 (same fallback strategy as ClassPass / Epic Games / fuboTV / Lattice / Stitch Fix / Udemy / Bitwarden / Fivetran). All other axes share with Fivetran: D-08 entity-decode-then-tag-strip (39th cohort member), D-09 **omitted** (Lookout 0/6 padded — fully clean 7-byte `'Lookout'` form, slug/wire case-symmetric — distinct from Fivetran's first-cohort D-09 application; Lookout returns to cohort-default D-09-omitted posture; **thirty-second cohort plugin** to omit D-09), D-10 omitted (Lookout 0/6 padded — fully clean — vs Fivetran 0/173 padded — both clean; **fifteenth** cohort plugin to omit D-10), D-11 fully-clean department pass-through (Lookout 0/6 populated padded; departments are bare role-domain names `'Engineering'` / `'Sales'` without the `' Department'` structural suffix that Fivetran's wire carries — **twenty-ninth cohort plugin** with fully-clean department pass-through). The run-293 probe at start sampled **6 visible roles** (the run-289 probe-counter estimate of ~12 was inflated by counting all `"id":` JSON keys including department/office IDs). Selected from the **fifth fresh probe sweep** as the **alphabetically-fifth live-board hit** after Bitwarden, Calendly, DataCamp, and Fivetran. The remaining six live hits queue for runs #294+ in alphabetical order (`marqeta` next at run #294 with ~330 roles). |
| shipped    | `source-company-fivetran`  | `company`  | **Fivetran** (`fivetran.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/fivetran/jobs`). | Spec 082, run #292. **Fourth plugin in the fifth fresh probe sweep.** Thin wrapper following the `source-company-bitwarden` pattern. **Two structural deviations** from the Bitwarden template: (1) **D-04 wire-shape variant 19 (`www.`-prefixed brand-domain singular `/careers/job` query-only-id).** Fivetran publishes `absolute_url` on a previously-unobserved shape `https://www.fivetran.com/careers/job?gh_jid=<id>` (`www.`-prefixed brand-domain — distinct from variant 18's bare `bitwarden.com`; `/careers/job` SINGULAR path — distinct from variant 13's plural-with-id-in-path, variant 16's plural-with-duplicate-query, and variant 18's id-in-path-with-trailing-slash). **First** plugin in the cohort to use **wire-shape variant 19** — the **twenty-second distinct wire-shape variant** in the company-direct cohort. Fallback uses canonical Greenhouse variant-2 (same fallback strategy as ClassPass / Epic Games / fuboTV / Lattice / Stitch Fix / Udemy / Bitwarden). (2) **D-09 APPLIED for the first time in cohort history.** Wire `company_name === 'Fivetran '` byte-for-byte (9 bytes — single trailing ASCII-space pad). Slug `fivetran` is 8 bytes. **All 173 of 173 wire `company_name` values in the run-292 probe carry the trailing pad** (100 % pad rate — systematic upstream pattern, not a one-off typo). The plugin applies `.trim()` to `listing.company_name` before emit so the emitted `companyName` is the 8-byte `'Fivetran'`. **First cohort plugin to apply D-09** — opening a brand-new sub-axis under D-09 alongside the existing thirty-one D-09 omission cases. Distinct from prior slug/wire asymmetry cases which all preserved the wire byte-for-byte (Honeycomb TLD; MasterClass case; Maven Clinic / Stitch Fix / Scale AI internal-space; Ramp Network shortening; fuboTV truncation). Fivetran is the first cohort case where the slug/wire asymmetry is **noise (whitespace pad) rather than signal** — so the plugin trims rather than preserving. Analogous to Lattice's first-cohort D-11 application at run #284 and DataCamp's first-cohort D-11 leading-pad at run #291 — all three use the standard `String.prototype.trim()` semantic on different axes. Shared with Bitwarden: D-08 entity-decode-then-tag-strip (38th cohort member), D-10 omitted (Fivetran 0/173 padded — fully clean — vs Bitwarden 1/11 padded ~9.1 %; Fivetran's posting hygiene cleaner on titles), D-11 fully-clean department pass-through (Fivetran 0/172 populated padded; departments mostly carry a `' Department'` suffix — `'Engineering Department'`, `'Sales Department'`, etc. — but this is structural data, not pad bytes). The run-292 probe at start sampled 173 visible roles (the run-289 probe-counter estimate of 346 was inflated by counting all `"id":` JSON keys including department/office IDs). Selected from the **fifth fresh probe sweep** as the **alphabetically-fourth live-board hit** after Bitwarden, Calendly, and DataCamp. The remaining seven live hits queue for runs #293+ in alphabetical order (`lookout` next at run #293 with ~12 roles). |
| shipped    | `source-company-datacamp`  | `company`  | **DataCamp** (`datacamp.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/datacamp/jobs`). | Spec 081, run #291. **Third plugin in the fifth fresh probe sweep.** Thin wrapper following the `source-company-masterclass` pattern — DataCamp shares MasterClass's wire-shape variant 2 (`job-boards.greenhouse.io/datacamp/jobs/<id>` — **eighteenth** plugin in the cohort to use variant 2), MasterClass's D-08 entity-decode-then-tag-strip description pipeline (**thirty-seventh** cohort member), MasterClass's D-09 omission with **case-only-asymmetric** wire `company_name === 'DataCamp'` (CamelCase brand; 8 bytes; slug `datacamp` is also 8 bytes — slug/wire EQUAL-byte-length but byte-distinct via case alone at byte index 4 — `c` vs `C`; same shape as MasterClass's slug `masterclass` / wire `'MasterClass'`), and MasterClass's D-10 omission (0 of 41 wire titles in the run-291 probe carry whitespace padding — `'Curriculum Manager - Data Science and AI'`, `'Senior Data Engineer'`, etc., the wire is fully clean; the plugin emits `listing.title` byte-for-byte without a `.trim()` — **thirteenth cohort plugin to omit D-10**). **One structural deviation** from the MasterClass template: **D-11 APPLIED with leading-pad form (first cohort observation of leading-space pad on the department axis).** 1 of 41 wire department names in the run-291 probe carries **leading** ASCII-space padding (`' IT'` — single-leading-space-padded; ~2.4 % pad rate; the leading-space form is distinct from every prior cohort observation, both Lattice's three trailing-space-padded departments at run #284 and Stitch Fix's run-287 trailing-space-padded title observations were trailing-pad). The plugin applies `.trim()` to `listing.departments?.[0]?.name` before downstream filters and emit, handling both leading AND trailing pad bytes via the standard `String.prototype.trim()` semantic. **Second cohort plugin to apply D-11** (after Lattice — the only cohort plugin so far to apply D-11; Stitch Fix's D-10 trailing-pad cohort observation does not affect D-11). **Thirty-first cohort plugin to omit D-09**, but the **eighth slug/wire asymmetry case overall** (after Ramp Network, Scale AI, fuboTV, Honeycomb, MasterClass, Maven Clinic, and Stitch Fix) — and the **second** equal-length-case-only asymmetry case after MasterClass (proving out the case-only asymmetry shape is a recurring axis). Department format is fully-clean single-token (`'Engineering'`, `'Sales'`, `'Marketing'`, `'Legal'`, `'Internship'`) and clean multi-token (`'Learner Product'`, `'Media & Communications'`, `'Finance & Accounting'`, `'Sales & Customer Success'`, `'Product Management'`) — structurally permissive of internal whitespace and ampersands within a single department string. The leading-space pad on `' IT'` is the **first cohort observation of leading-space padding on the department axis** — opening a new sub-axis under D-11. Selected from the **fifth fresh probe sweep** as the **alphabetically-third live-board hit** after Bitwarden and Calendly (41 roles confirmed at run-291 start). The remaining eight live hits queue for runs #292+ in alphabetical order (`fivetran` next at run #292 with ~346 roles). |
| shipped    | `source-company-calendly`  | `company`  | **Calendly** (`calendly.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/calendly/jobs`). | Spec 080, run #290. **Second plugin in the fifth fresh probe sweep.** Thin wrapper following the `source-company-bitwarden` pattern — Calendly shares Bitwarden's D-08 entity-decode-then-tag-strip description pipeline (thirty-sixth cohort member), Bitwarden's D-09 omission with case-symmetric bare-brand wire (wire `company_name === 'Calendly'` byte-for-byte; case-symmetric with the lowercase slug `calendly`; no legal-entity suffix on the wire — distinct from the legal-entity name "Calendly LLC" that may appear in corporate filings), Bitwarden's D-10 application (1 of 20 wire titles in the run-290 probe carries trailing ASCII-space padding — `'Sr. Director, Engineering '`, single-trailing-space-padded; ~5.0 % pad rate; the plugin applies `.trim()` to the wire `title` before downstream filters and emit — **nineteenth cohort plugin to apply D-10**), and Bitwarden's D-11 fully-clean department pass-through (0 of 20 wire department names in the run-290 probe carry trailing ASCII-space padding — `'Marketing'`, `'Engineering'`, `'Product'`, `'Customer Experience'`, `'Security'`; the plugin emits `listing.departments[0].name` byte-for-byte without a `.trim()`). **One structural deviation** from the Bitwarden template: **D-04 wire-shape variant 2 (canonical Greenhouse host).** Calendly publishes `absolute_url` on the canonical Greenhouse variant-2 shape `https://job-boards.greenhouse.io/calendly/jobs/<id>` — **returning to baseline** after Bitwarden's first-cohort variant-18 observation. The fallback `jobUrl` constructor reconstructs the same canonical variant-2 form (deterministic given the listing ID — no defence-in-depth divergence between wire and fallback, distinct from Bitwarden's variant-18 wire form vs variant-2 fallback split). **Eighteenth** plugin in the cohort to use variant 2 (after Vercel, Affirm, Gusto, Mercury, Buildkite, Netlify, Postman, Webflow, Attentive, Intercom, Mixpanel, Scale AI, Cameo, Carta, Honeycomb, MasterClass, and Maven Clinic). **Thirtieth cohort plugin to omit D-09**, returning to the case-symmetric bare-brand wire form (after the seven slug/wire asymmetry cases — Ramp Network, Scale AI, fuboTV, Honeycomb, MasterClass, Maven Clinic, and Stitch Fix). Department format is fully-clean single-token (`'Marketing'`, `'Engineering'`, `'Product'`, `'Security'`) and clean multi-token (`'Customer Experience'`) — structurally permissive of internal whitespace within a single department string. The wire-title pad rate ~5.0 % is the **lowest cohort observation among D-10-applying plugins** since Carta's run-276 ~10 % rate — Calendly's posting hygiene is slightly cleaner than the cohort baseline. Selected from the **fifth fresh probe sweep** as the **alphabetically-second live-board hit** after Bitwarden (20 roles confirmed at run-290 start, slightly under the run-289 probe-sweep estimate of 40 — the discrepancy is the run-289 probe-counter inflation from counting all `"id":` JSON keys including department/office IDs rather than just job IDs). The remaining nine live hits queue for runs #291+ in alphabetical order (`datacamp` next at run #291). |
| shipped    | `source-company-bitwarden` | `company`  | **Bitwarden** (`bitwarden.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/bitwarden/jobs`). | Spec 079, run #289. **First plugin in the fifth fresh probe sweep.** Thin wrapper following the `source-company-udemy` pattern — Bitwarden shares Udemy's D-08 entity-decode-then-tag-strip description pipeline (thirty-fifth cohort member), Udemy's D-09 omission with case-symmetric bare-brand wire (wire `company_name === 'Bitwarden'` byte-for-byte; case-symmetric with the lowercase slug `bitwarden`; no legal-entity suffix on the wire — distinct from the legal-entity name "Bitwarden, Inc." that may appear in corporate filings), Udemy's D-10 application (1 of 11 wire titles in the run-289 probe carries trailing ASCII-space padding — `'Senior Full Stack Software Engineer '`, single-trailing-space-padded; ~9.1 % pad rate; the plugin applies `.trim()` to the wire `title` before downstream filters and emit — **eighteenth cohort plugin to apply D-10**), and Udemy's D-11 fully-clean department pass-through (0 of 11 wire department names in the run-289 probe carry trailing ASCII-space padding — `'Engineering'`, `'Sales'`, `'Customer Success'`, `'Product'`; the plugin emits `listing.departments[0].name` byte-for-byte without a `.trim()`). **One structural deviation** from the Udemy template: **D-04 wire-shape variant 18 (bare brand-domain `/careers/<id>/`-trailing-slash query-with-id).** Bitwarden publishes `absolute_url` on a previously-unobserved **bare brand-domain shape** `https://bitwarden.com/careers/<id>/?gh_jid=<id>` (bare `bitwarden.com` brand-domain — no `www.` prefix, like variant 13's `epicgames.com` and variant 15's `lattice.com`; `/careers/<id>/` path with the listing ID embedded — distinct from variant 13's `careers/jobs/<id>?gh_jid=<id>` `/jobs/` segment; **trailing slash on the path** before the query — the trailing-slash-before-query is the most distinctive feature, distinct from every prior cohort variant which all omit the trailing slash before `?gh_jid=`; single `gh_jid` query parameter — same single-query-parameter shape as variants 10, 12, 13, 14, 15, 17). This is the **first** plugin in the cohort to use **wire-shape variant 18** — the **twenty-first distinct wire-shape variant** in the company-direct cohort. The plugin emits `listing.absolute_url` byte-for-byte to preserve the canonical destination (including the trailing slash before `?gh_jid=`); the **fallback** `jobUrl` constructor (when Greenhouse omits `absolute_url`) defaults to the canonical Greenhouse variant-2 form `https://job-boards.greenhouse.io/bitwarden/jobs/<id>` rather than reconstructing the bare-domain trailing-slash shape (same fallback strategy as ClassPass / Epic Games / fuboTV / Lattice / Stitch Fix / Udemy). **Twenty-ninth cohort plugin to omit D-09**, returning to the case-symmetric bare-brand wire form (after the seven slug/wire asymmetry cases — Ramp Network, Scale AI, fuboTV, Honeycomb, MasterClass, Maven Clinic, and Stitch Fix). Department format is fully-clean single-token (`'Engineering'`, `'Sales'`, `'Product'`) and clean multi-token (`'Customer Success'`) — structurally permissive of internal whitespace within a single department string. **Selected from the fifth fresh probe sweep — first run of the new sweep — as the alphabetically-first live-board hit.** Run #289's probe sweep across forty-one candidate slugs found exactly **eleven** fresh non-empty live boards on Greenhouse: `bitwarden` (11 jobs, run #289 shipped — this row), `calendly` (40, queued for run #290), `datacamp` (41), `fivetran` (346), `lookout` (12), `marqeta` (330), `newrelic` (370), `peloton` (104), `scopely` (1190), `squarespace` (72), `typeform` (132). Plus two HTTP-200-with-zero-jobs deferred slugs (`hubspot` — twenty-second consecutive empty re-probe across runs #262–#289; `allbirds` — first observation, may be a seasonal-hiring slowdown). The remaining twenty-eight candidate slugs returned HTTP 404. The remaining ten live hits queue for runs #290+ in alphabetical order. |
| shipped    | `source-company-udemy`     | `company`  | **Udemy** (`udemy.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/udemy/jobs`). | Spec 078, run #288. Thin wrapper following the `source-company-carta` pattern — Udemy shares Carta's D-08 entity-decode-then-tag-strip description pipeline (thirty-fourth cohort member), Carta's D-09 omission with case-symmetric bare-brand wire (wire `company_name === 'Udemy'` byte-for-byte; case-symmetric with the lowercase slug — same shape as Cameo `'Cameo'` / Carta `'Carta'` / Lattice `'Lattice'`; no legal-entity suffix on the wire — distinct from the legal-entity name "Udemy, Inc." that appears in current SEC filings under NASDAQ ticker `UDMY`), Carta's D-10 application (2 of 17 wire titles in the run-288 probe carry trailing ASCII-space padding — `'Join Our Talent Community '`, `'Sales Development Representative '`, both single-trailing-space-padded; ~11.8 % pad rate; the plugin applies `.trim()` to the wire `title` before downstream filters and emit — **seventeenth cohort plugin to apply D-10**), and Carta's D-11 fully-clean department pass-through (0 of 16 populated wire department names in the run-288 probe carry trailing ASCII-space padding — `'UB Sales - ADR'`, `'Sales'`, `'Consumer Partnerships'`, `'Product Design and UXR'`, `'UB Sales - Enterprise'`, `'Product Management'`, `'Engineering'`; one listing has empty `departments` array — the optional-chain emits `null` for that case; the plugin emits `listing.departments?.[0]?.name` byte-for-byte without a `.trim()`). **One structural deviation** from the Carta template: **D-04 wire-shape variant 17 (third-party-SaaS-host, CareerPuck).** Udemy publishes `absolute_url` on a previously-unobserved **third-party-SaaS-host shape** `https://app.careerpuck.com/job-board/udemy/job/<id>?gh_jid=<id>` (`app.careerpuck.com` — the third-party CareerPuck SaaS host proxying Greenhouse boards through its job-board front-end, distinct from every prior cohort variant which all use either Greenhouse-owned hosts (`boards.greenhouse.io`, `job-boards.greenhouse.io`) or the brand's own domain (bare, `www.`-prefixed, vanity-subdomain, or parent-domain — variants 10..16); `/job-board/udemy/` path with the slug embedded; singular `/job/<id>` path with the listing ID; single `gh_jid` query parameter — same single-query-parameter shape as variants 10, 12, 13, 14, 15). This is the **first** plugin in the cohort to use **wire-shape variant 17** — the **twentieth distinct wire-shape variant** in the company-direct cohort and the **first** to publish through a third-party SaaS career-board host (CareerPuck) rather than a brand-owned domain or a Greenhouse-owned host. The plugin emits `listing.absolute_url` byte-for-byte to preserve the canonical destination; the **fallback** `jobUrl` constructor (when Greenhouse omits `absolute_url`) defaults to the canonical Greenhouse variant-2 form `https://job-boards.greenhouse.io/udemy/jobs/<id>` rather than reconstructing the third-party-SaaS-host shape (same fallback strategy as ClassPass / Epic Games / fuboTV / Lattice / Stitch Fix). **Twenty-eighth cohort plugin to omit D-09**, returning to the case-symmetric bare-brand wire form (after the seven slug/wire asymmetry cases — Ramp Network, Scale AI, fuboTV, Honeycomb, MasterClass, Maven Clinic, and Stitch Fix). Department format is mixed clean single-token / multi-token descriptive (`'Engineering'`, `'Sales'`, `'Product Management'`, `'UB Sales - ADR'`, `'UB Sales - Enterprise'`, `'UB Sales - SDR'`, `'Consumer Partnerships'`, `'Go-To-Market Strategy & Operations'`, `'Region Marketing - APAC'`) — structurally permissive of internal whitespace, hyphens, and ampersands within a single department string. Selected from the **fourth fresh probe sweep** as the **alphabetically-fourteenth and last live-board hit** after Cameo, Carta, ClassPass, Coursera, Epic Games, Flexport, fuboTV, Glossier, Honeycomb, Lattice, MasterClass, Maven Clinic, and Stitch Fix (17 roles confirmed at run-288 start). With Udemy shipped, the **fourth-fresh-sweep candidate pool is fully exhausted** — runs #289+ pivot to a **fifth fresh probe sweep** targeting yet-untested large-employer candidate slugs. |
| shipped    | `source-company-stitchfix` | `company`  | **Stitch Fix** (`stitchfix.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/stitchfix/jobs`). | Spec 077, run #287. Thin wrapper following the `source-company-mavenclinic` pattern — Stitch Fix shares Maven Clinic's D-08 entity-decode-then-tag-strip description pipeline (thirty-third cohort member), Maven Clinic's D-09 omission with internal-whitespace wire asymmetry (wire `company_name === 'Stitch Fix'` carries the brand's two-word internal-whitespace form — 10 bytes; slug `stitchfix` is 9 bytes — slug/wire-asymmetric, wire LONGER than slug by 1 byte via the internal ASCII space at index 6 between `Stitch` and `Fix`), Maven Clinic's D-10 application (3 of 22 wire titles in the run-287 probe carry trailing ASCII-space padding — `'Principal Full-Stack Data Scientist - Recommendation Algorithms '`, `'Senior Manager of Data Engineering and AI Automation, Business Systems '`, `'Strategic Program Manager, Styling Enablement '`, all single-trailing-space-padded; ~13.6 % pad rate; the plugin applies `.trim()` to the wire `title` before downstream filters and emit — **sixteenth cohort plugin to apply D-10**), and Maven Clinic's D-11 fully-clean department pass-through (0 of 22 wire department names in the run-287 probe carry trailing ASCII-space padding — `'Engineering'`, `'Data Platform'`, `'Marketing'`, `'Product'`; the plugin emits `listing.departments[0].name` byte-for-byte without a `.trim()`). **One structural deviation** from the Maven Clinic template: **D-04 wire-shape variant 16 (bare-www brand-domain `/careers/jobs`-path duplicate-`gh_jid`-query).** Stitch Fix publishes `absolute_url` on a previously-unobserved **bare-www brand-domain shape** `https://www.stitchfix.com/careers/jobs?gh_jid=<id>&gh_jid=<id>` (`www.stitchfix.com` — `www` subdomain on the bare brand domain, distinct from variant 14's `careers.fubo.tv` vanity subdomain and variant 15's bare `lattice.com`; plural `/careers/jobs` path — distinct from variant 13's `careers/jobs/<id>?gh_jid=<id>` per-id-in-path shape and variant 15's singular `/job` path; the listing ID appears **twice** in the same `gh_jid` query parameter — the duplicate query parameter is the most distinctive feature and is distinct from every prior cohort variant where the same query parameter appears at most once). This is the **first** plugin in the cohort to use **wire-shape variant 16** — the **nineteenth distinct wire-shape variant** in the company-direct cohort. The plugin emits `listing.absolute_url` byte-for-byte to preserve the canonical destination (including the duplicate `gh_jid` query parameter); the **fallback** `jobUrl` constructor (when Greenhouse omits `absolute_url`) defaults to the canonical Greenhouse variant-2 form `https://job-boards.greenhouse.io/stitchfix/jobs/<id>` rather than reconstructing the bare-domain duplicate-query shape (same fallback strategy as ClassPass / Epic Games / fuboTV / Lattice). **Twenty-seventh cohort plugin to omit D-09**, but the **seventh** slug/wire asymmetry case overall (after Ramp Network slug `rampnetwork` / wire `'Ramp'`, Scale AI slug `scaleai` / wire `'Scale AI'`, fuboTV slug `fubotv` / wire `'Fubo'`, Honeycomb slug `honeycomb` / wire `'Honeycomb.io'`, MasterClass slug `masterclass` / wire `'MasterClass'`, and Maven Clinic slug `mavenclinic` / wire `'Maven Clinic'`) — and the **third** internal-whitespace asymmetry case in the cohort after Scale AI and Maven Clinic (same +1 byte differential, same single-internal-space delta — Stitch Fix proves out that the Scale AI / Maven Clinic shape is a stable recurring axis rather than a two-off pattern). Department format is fully-clean single-token / multi-token descriptive (`'Engineering'`, `'Data Platform'`, `'Marketing'`, `'Product'`) — structurally permissive of internal whitespace within a single department string. Selected from the **fourth fresh probe sweep** as the **alphabetically-thirteenth live-board hit** after Cameo, Carta, ClassPass, Coursera, Epic Games, Flexport, fuboTV, Glossier, Honeycomb, Lattice, MasterClass, and Maven Clinic (22 roles confirmed at run-287 start). The remaining one live hit queues for run #288 (`udemy` with 17 roles). |
| shipped    | `source-company-mavenclinic` | `company` | **Maven Clinic** (`mavenclinic.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/mavenclinic/jobs`). | Spec 076, run #286. Thin wrapper following the `source-company-honeycomb` pattern — Maven Clinic shares Honeycomb's wire-shape variant 2 (`job-boards.greenhouse.io/mavenclinic/jobs/<id>` — **seventeenth** plugin in the cohort to use variant 2 after Vercel, Affirm, Gusto, Mercury, Buildkite, Netlify, Postman, Webflow, Attentive, Intercom, Mixpanel, Scale AI, Cameo, Carta, Honeycomb, and MasterClass), Honeycomb's D-08 entity-decode-then-tag-strip description pipeline (thirty-second cohort member), Honeycomb's D-10 application (3 of 24 wire titles in the run-286 probe carry trailing ASCII-space padding — `'Clinical Outcomes Analyst '`, `'Director, Employer Sales '`, `'Manager, Member Services '`, all single-trailing-space-padded; ~12.5 % pad rate; the plugin applies `.trim()` to the wire `title` before downstream filters and emit — **fifteenth cohort plugin to apply D-10**), and Honeycomb's D-11 fully-clean department pass-through (0 of 24 wire department names in the run-286 probe carry trailing ASCII-space padding — `'Brand & Communications'`, `'Clinical Outcomes'`, `'Employer Sales'`, `'Member Services'`, `'Engineering'`; the plugin emits `listing.departments[0].name` byte-for-byte without a `.trim()`). **One structural deviation** from the Honeycomb template: **D-09 omitted with internal-whitespace wire asymmetry.** Maven Clinic's wire `company_name === 'Maven Clinic'` carries the brand's two-word internal-whitespace form (12 bytes; slug `mavenclinic` is 11 bytes — slug/wire-asymmetric, wire LONGER than slug by 1 byte via the internal ASCII space at index 5 between `Maven` and `Clinic`). The plugin reads `listing.company_name` directly with `'Maven Clinic'` as a defensive fallback, but the unit-test happy path asserts the emitted `companyName === 'Maven Clinic'` byte-for-byte AND distinct from the slug AND exactly 1 byte longer than the slug AND case-insensitively-with-space-collapsed equal to the slug to lock the internal-whitespace asymmetry observable. **Twenty-sixth cohort plugin to omit D-09**, but the **sixth** slug/wire asymmetry case overall (after Ramp Network slug `rampnetwork` / wire `'Ramp'`, Scale AI slug `scaleai` / wire `'Scale AI'`, fuboTV slug `fubotv` / wire `'Fubo'`, Honeycomb slug `honeycomb` / wire `'Honeycomb.io'`, and MasterClass slug `masterclass` / wire `'MasterClass'`) — and the **second** internal-whitespace asymmetry case in the cohort after Scale AI (same +1 byte differential, same single-internal-space delta — Maven Clinic proves out that Scale AI's internal-whitespace shape was not a one-off but rather a recurring asymmetry-axis case). Distinct from Honeycomb's TLD-suffix length asymmetry (wire 3 bytes longer), MasterClass's case-only asymmetry (equal byte length), fuboTV's brand-rebrand truncation (wire 2 bytes shorter), and Ramp Network's brand-shortening asymmetry (wire 7 bytes shorter). Department format spans fully-clean single-token (`'Engineering'`) and clean multi-token (`'Brand & Communications'`, `'Clinical Outcomes'`, `'Employer Sales'`, `'Member Services'`) — structurally permissive of internal whitespace and ampersands within a single department string. Selected from the **fourth fresh probe sweep** as the **alphabetically-twelfth live-board hit** after Cameo, Carta, ClassPass, Coursera, Epic Games, Flexport, fuboTV, Glossier, Honeycomb, Lattice, and MasterClass (24 roles confirmed at run-286 start). The remaining two live hits queue for runs #287+ in alphabetical order (`stitchfix` next at run #287 with 22 roles). |
| shipped    | `source-company-glossier`  | `company`  | **Glossier** (`glossier.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/glossier/jobs`). | Spec 072, run #282. Thin wrapper following the `source-company-flexport` pattern — Glossier shares Flexport's wire-shape variant 10 (legacy hosted-board apex `boards.greenhouse.io/glossier/jobs/<id>?gh_jid=<id>` — **fourth** plugin in the cohort to use variant 10 after Chime, Faire, and Flexport), Flexport's D-08 entity-decode-then-tag-strip description pipeline (twenty-eighth cohort member), Flexport's D-09 omission (wire `company_name === 'Glossier'` byte-for-byte; the single-token bare brand name; no legal-entity suffix on the wire — distinct from the legal-entity name "Glossier, Inc." that may appear in corporate filings), Flexport's D-10 application (at least 2 of 17 wire titles in the run-282 probe carry whitespace padding — one with a **leading** ASCII-space `' (Sales Associate, Part-Time) Editor, Los Angeles'` and one with a **double trailing** ASCII-space `'(Seasonal Sales Associate, Part-Time) Editor, Boston  '` — ~11.8 % pad rate; **Glossier is the first cohort plugin where the observed pad-byte distribution includes a leading-pad case AND a multi-byte trailing-pad case** — distinct from the trailing-single-pad uniform distributions of fuboTV (~91 %), Flexport (~9.7 %), and the prior D-10 cohort. Standard `String.prototype.trim()` handles both axes, so the existing one-line `.trim()` semantics carry through unchanged — **thirteenth cohort plugin to apply D-10**), and Flexport's D-11 fully-clean department pass-through (0 of 17 wire department names in the run-282 probe carry trailing ASCII-space padding; the plugin emits `listing.departments[0].name` byte-for-byte without a `.trim()`). **Zero structural deviations** from the Flexport template — making this the **third** Greenhouse-only company-direct plugin in run-history to ship as a clean re-spin of a prior cohort plugin with no per-axis deviations (after Coursera off Chime at run #278 and Flexport off Faire at run #280). The leading-pad and double-trailing-pad observability is a wire-side observation about pad-byte distribution, not a structural deviation in the plugin itself — the implementation is byte-identical to Flexport's `.trim()`-based handling. **Twenty-second cohort plugin to omit D-09**, returning to the single-word bare-brand wire form. Department format is fully-clean multi-token descriptive (`'Retail (Stores)'`, `'Creative'`, `'Product Development'`) — structurally permissive of internal whitespace and parentheses within a single department string. Selected from the **fourth fresh probe sweep** as the **alphabetically-eighth live-board hit** after Cameo, Carta, ClassPass, Coursera, Epic Games, Flexport, and fuboTV (17 roles confirmed at run-282 start). The remaining six live hits queue for runs #283+ in alphabetical order (`honeycomb` next at run #283 with 10 roles). The HubSpot re-probe at run-282 start returned HTTP 200 with `meta.total === 0` — twentieth-consecutive empty re-probe across runs #262–#282; HubSpot remains deferred. |
| shipped    | `source-company-masterclass` | `company` | **MasterClass** (`masterclass.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/masterclass/jobs`). | Spec 075, run #285. Thin wrapper following the `source-company-honeycomb` pattern — MasterClass shares Honeycomb's wire-shape variant 2 (`job-boards.greenhouse.io/masterclass/jobs/<id>` — **sixteenth** plugin in the cohort to use variant 2 after Vercel, Affirm, Gusto, Mercury, Buildkite, Netlify, Postman, Webflow, Attentive, Intercom, Mixpanel, Scale AI, Cameo, Carta, and Honeycomb), Honeycomb's D-08 entity-decode-then-tag-strip description pipeline (thirty-first cohort member), and Honeycomb's D-11 fully-clean department pass-through (0 of 6 wire department names in the run-285 probe carry trailing ASCII-space padding — `'Content Production'`, `'Marketing'` × 2, `'Content'`, `'Engineering'` × 2; the plugin emits `listing.departments[0].name` byte-for-byte without a `.trim()`). **Two structural deviations** from the Honeycomb template: (1) **D-09 omitted with equal-length-case-only wire asymmetry.** MasterClass's wire `company_name === 'MasterClass'` carries the brand's CamelCase form (11 bytes); slug `masterclass` is also 11 bytes — slug/wire EQUAL-byte-length but byte-distinct via case alone at byte index 6 (`c` vs `C`). The plugin reads `listing.company_name` directly with `'MasterClass'` as a defensive fallback, but the unit-test happy path asserts the emitted `companyName === 'MasterClass'` byte-for-byte AND distinct from the slug AND of equal byte length to the slug AND case-insensitively equal to the slug to lock the equal-length-case-only asymmetry observable. **Twenty-fifth cohort plugin to omit D-09**, but the **fifth** slug/wire asymmetry case overall (after Ramp Network slug `rampnetwork` / wire `'Ramp'`, Scale AI slug `scaleai` / wire `'Scale AI'`, fuboTV slug `fubotv` / wire `'Fubo'`, and Honeycomb slug `honeycomb` / wire `'Honeycomb.io'`) — and the **first** asymmetry case where slug and wire are equal-byte-length but byte-distinct via case alone. Distinct from Honeycomb's TLD-suffix length asymmetry (wire 3 bytes longer), Scale AI's internal-whitespace asymmetry (wire 1 byte longer), fuboTV's brand-rebrand truncation (wire 2 bytes shorter), and Ramp Network's brand-shortening asymmetry (wire 7 bytes shorter). MasterClass extends the asymmetry-axis taxonomy from length-only differences to case-only differences. (2) **D-10 omitted** — 0 of 6 wire titles in the run-285 probe carry whitespace padding (the wire is fully clean — `'AI Creator / Artist / Technical Artist (CONTRACTOR)'`, `'Communications Manager (TEMP)'`, `'Learning Experience Designer (TEMP)'`, `'Senior Product Marketing Manager'`, `'Staff AI / ML Engineer'`, `'Staff Software Engineer, Infrastructure'`); the plugin emits `listing.title` byte-for-byte without a `.trim()` — **twelfth cohort plugin to omit D-10**. Department format is fully-clean single-token / multi-token descriptive (`'Engineering'`, `'Marketing'`, `'Content'`, `'Content Production'`) — structurally permissive of internal whitespace within a single department string. Selected from the **fourth fresh probe sweep** as the **alphabetically-eleventh live-board hit** after Cameo, Carta, ClassPass, Coursera, Epic Games, Flexport, fuboTV, Glossier, Honeycomb, and Lattice (6 roles confirmed at run-285 start). The remaining three live hits queue for runs #286+ in alphabetical order (`mavenclinic` next at run #286 with 24 roles). |
| shipped    | `source-company-lattice`   | `company`  | **Lattice** (`lattice.com`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/lattice/jobs`). | Spec 074, run #284. Thin wrapper following the `source-company-honeycomb` pattern — Lattice shares Honeycomb's D-08 entity-decode-then-tag-strip description pipeline (thirtieth cohort member) and Honeycomb's D-09 omission (wire `company_name === 'Lattice'` byte-for-byte; the single-token bare brand name; case-symmetric with the lowercase slug `lattice`; no legal-entity suffix on the wire). **Three structural deviations** from the Honeycomb template: (1) **D-04 wire-shape variant 15 (bare brand-domain singular-`/job` query-only-id).** Lattice publishes `absolute_url` on a previously-unobserved **bare brand-domain shape** `https://lattice.com/job?gh_jid=<id>` (bare `lattice.com` rather than `www.lattice.com` or any vanity subdomain; **`/job`** singular fixed path — distinct from variant 13's `careers/jobs/<id>?gh_jid=<id>`; the listing ID appears **only** in the `gh_jid` query parameter, not in the path — distinct from variant 14's `careers.fubo.tv/fubotv-job-openings/?gh_jid=<id>` vanity-subdomain shape). This is the **first** plugin in the cohort to use **wire-shape variant 15** — the **eighteenth distinct wire-shape variant** in the company-direct cohort. The plugin emits `listing.absolute_url` byte-for-byte to preserve the canonical destination; the **fallback** `jobUrl` constructor defaults to the canonical Greenhouse variant-2 form `https://job-boards.greenhouse.io/lattice/jobs/<id>` rather than reconstructing the bare-domain shape (same fallback strategy as ClassPass / Epic Games / fuboTV). (2) **D-10 omitted** — 0 of 11 wire titles in the run-284 probe carry whitespace padding (the wire is fully clean); the plugin emits `listing.title` byte-for-byte without a `.trim()` — **eleventh cohort plugin to omit D-10**. (3) **D-11 APPLIED for the first time in cohort history** — 3 of 11 wire department names in the run-284 probe carry trailing ASCII-space padding (`'Customer Account Management '` × 1, `'Product '` × 2; ~27 % pad rate); the plugin applies `.trim()` to `listing.departments?.[0]?.name` before downstream filters and emit — **first cohort plugin to apply D-11**, opening the deviation axis from "fully-clean pass-through" to "trim-on-emit". **Twenty-fourth cohort plugin to omit D-09**, returning to the case-symmetric bare-brand wire form. Department format spans clean single-token (`'Account Executive'`, `'Engineering'`, `'Customer Care'`, `'Enablement'`) and trailing-padded multi-token (`'Customer Account Management '`, `'Product '`) — structurally permissive of internal whitespace and trailing pad bytes within a single department string. Selected from the **fourth fresh probe sweep** as the **alphabetically-tenth live-board hit** after Cameo, Carta, ClassPass, Coursera, Epic Games, Flexport, fuboTV, Glossier, and Honeycomb (11 roles confirmed at run-284 start). The remaining four live hits queue for runs #285+ in alphabetical order (`masterclass` next at run #285 with 6 roles). |
| shipped    | `source-company-honeycomb` | `company`  | **Honeycomb** (`honeycomb.io`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/honeycomb/jobs`). | Spec 073, run #283. Thin wrapper following the `source-company-carta` pattern — Honeycomb shares Carta's wire-shape variant 2 (`job-boards.greenhouse.io/honeycomb/jobs/<id>` — **fifteenth** plugin in the cohort to use variant 2 after Vercel, Affirm, Gusto, Mercury, Buildkite, Netlify, Postman, Webflow, Attentive, Intercom, Mixpanel, Scale AI, Cameo, and Carta), Carta's D-08 entity-decode-then-tag-strip description pipeline (twenty-ninth cohort member), Carta's D-10 application (at least 2 of 10 wire titles in the run-283 probe carry trailing ASCII-space padding — `'Staff Solution Architect '` × 2 listings, ~20 % pad rate; the plugin applies `.trim()` to the wire `title` before downstream filters and emit — **fourteenth cohort plugin to apply D-10**), and Carta's D-11 fully-clean department pass-through (0 of 10 wire department names in the run-283 probe carry trailing ASCII-space padding — `'Sales'`, `'Finance & Accounting'`, `'RevOps'`; the plugin emits `listing.departments[0].name` byte-for-byte without a `.trim()`). **One structural deviation** from the Carta template: **D-09 omitted with TLD-suffix wire asymmetry.** Honeycomb's wire `company_name === 'Honeycomb.io'` carries the brand's `.io` TLD as a 3-byte trailing suffix (12 bytes total; slug `honeycomb` is 9 bytes — slug/wire-asymmetric, wire LONGER than slug by 3 bytes). The plugin reads `listing.company_name` directly with `'Honeycomb.io'` as a defensive fallback, but the unit-test happy path asserts the emitted `companyName === 'Honeycomb.io'` byte-for-byte AND distinct from the slug AND exactly 3 bytes longer than the slug to lock the TLD-suffix observable. **Twenty-third cohort plugin to omit D-09**, but the **fourth** slug/wire asymmetry case in the cohort (after Ramp Network slug `rampnetwork` / wire `'Ramp'`, Scale AI slug `scaleai` / wire `'Scale AI'`, and fuboTV slug `fubotv` / wire `'Fubo'`) — and the **second** asymmetry case where the wire is longer than the slug after Scale AI (8 bytes vs. 7). Honeycomb is the **first** cohort plugin where the wire `company_name` carries the brand's TLD as a 3-byte trailing suffix (`.io`) — distinct from Scale AI's internal-whitespace asymmetry, fuboTV's brand-rebrand truncation, and Ramp Network's brand-shortening asymmetry. Department format is fully-clean single-token / multi-word descriptive (`'Sales'`, `'Finance & Accounting'`, `'RevOps'`) — structurally permissive of internal whitespace and ampersands within a single department string. Selected from the **fourth fresh probe sweep** as the **alphabetically-ninth live-board hit** after Cameo, Carta, ClassPass, Coursera, Epic Games, Flexport, fuboTV, and Glossier (10 roles confirmed at run-283 start). The remaining five live hits queue for runs #284+ in alphabetical order (`lattice` next at run #284 with 11 roles). The HubSpot re-probe at run-283 start returned HTTP 200 with `meta.total === 0` — twenty-first-consecutive empty re-probe across runs #262–#283; HubSpot remains deferred. |
| shipped    | `source-company-fubotv`    | `company`  | **fuboTV** (`fubo.tv`) — Greenhouse-hosted (`api.greenhouse.io/v1/boards/fubotv/jobs`). | Spec 071, run #281. Thin wrapper following the `source-company-classpass` pattern — fuboTV shares ClassPass's D-08 entity-decode-then-tag-strip description pipeline (twenty-seventh cohort member), ClassPass's D-09 omission (wire `company_name === 'Fubo'` byte-for-byte; the single-token bare brand name reflecting the 2023 rebrand from "fuboTV"; no legal-entity suffix on the wire — distinct from the legal-entity name "FuboTV Inc." that appears in current SEC filings under NYSE ticker `FUBO`), ClassPass's D-10 application (at least 10 of 11 wire titles in the run-281 probe carry trailing ASCII-space padding — `'Director, Content Strategy & Analysis '`, `'Senior Software Engineer, Backend '`, `'Live Events Engineer '`, plus 7 others, ~91 % pad rate — the **highest pad rate observed in the cohort to date**, edging out Flexport's run-280 ~9.7 % rate by an order of magnitude; the plugin applies `.trim()` to the wire `title` before downstream filters and emit — **thirteenth cohort plugin to apply D-10**), and ClassPass's D-11 fully-clean department pass-through (0 of 11 wire department names in the run-281 probe carry trailing ASCII-space padding; the plugin emits `listing.departments[0].name` byte-for-byte without a `.trim()`). **Two structural deviations** from the ClassPass template: (1) **D-04 wire-shape variant 14 (vanity-domain fixed-path query-only-id).** fuboTV publishes `absolute_url` on a previously-unobserved **vanity-domain shape** `https://careers.fubo.tv/fubotv-job-openings/?gh_jid=<id>` (vanity-domain `careers.fubo.tv` rather than the parent `fubotv.com`; `fubotv-job-openings/` fixed path; single `gh_jid` query parameter — the listing ID appears **only** in the query parameter, not in the path — distinct from variant 12's `careers/opportunities/<id>?gh_jid=<id>` and variant 13's `careers/jobs/<id>?gh_jid=<id>` shapes, where the ID is duplicated in path and query). This is the **first** plugin in the cohort to use **wire-shape variant 14** — the **seventeenth distinct wire-shape variant** in the company-direct cohort and the **first** to publish the listing ID **only** in the query parameter (no path-embedded ID). The plugin emits `listing.absolute_url` byte-for-byte to preserve the canonical destination; the **fallback** `jobUrl` constructor (when Greenhouse omits `absolute_url`) defaults to the canonical Greenhouse variant-2 form `https://job-boards.greenhouse.io/fubotv/jobs/<id>` rather than reconstructing the vanity-domain shape (same fallback strategy as ClassPass — Spec 067 § 10 D-04 — and Epic Games — Spec 069 § 10 D-04). (2) **D-12 location-side `.trim()` applied (new axis, first cohort application).** 11 of 11 wire `location.name` values in the run-281 probe carry trailing ASCII-space padding (`'New York, NY '` and `'Denver, CO '` — 100 % pad-rate). The plugin applies `.trim()` to `listing.location?.name` before constructing the `LocationDto({ city })` so downstream consumers see clean `'New York, NY'`. **First cohort plugin to apply D-12.** **Twenty-first cohort plugin to omit D-09**, returning to the single-word bare-brand wire form, but the **third** slug/wire asymmetry case in the cohort (after Ramp Network slug `rampnetwork` / wire `'Ramp'`, and Scale AI slug `scaleai` / wire `'Scale AI'`) — and the **first** asymmetry case where the wire `company_name` is **shorter** than the slug (wire `'Fubo'` is 4 bytes; slug `fubotv` is 6 bytes), reflecting the brand's 2023 rename from "fuboTV" to "Fubo" while the Greenhouse tenant retained the legacy `fubotv` slug. Department format is fully-clean single-token / multi-word descriptive (`'Content Strategy'`, `'Technology'`, `'Business Development'`). Selected from the **fourth fresh probe sweep** as the **alphabetically-seventh live-board hit** after Cameo, Carta, ClassPass, Coursera, Epic Games, and Flexport (11 roles confirmed at run-281 start). The remaining seven live hits queue for runs #282+ in alphabetical order (`glossier` next at run #282 with 17 roles). The HubSpot re-probe at run-281 start returned HTTP 200 with `meta.total === 0` — nineteenth-consecutive empty re-probe across runs #262–#281; HubSpot remains deferred. |
| shipped    | `source-ats-apploi`        | `ats`      | **Apploi** (`apploi.com`, US healthcare/hourly ATS, now part of Viventium). | Spec 405, run #413. Public anonymous JSON: `GET api.apploi.com/v1/company_profiles/{slug}` → `teams_to_show`, then `ats-integrations.apploi.com/search/jobs/?teams={csv}`; canonical `jobs.apploi.com/view/{id}`. **Verified live 2026-06-04.** |
| shipped    | `source-ats-kenjo`         | `ats`      | **Kenjo** (`kenjo.io`, DE/ES SMB HR & ATS).         | Spec 406, run #413. Public anonymous `GET {tenant}.kenjo.io/api/controller/career-site/public/{tenant}/positions` + per-role detail by `customUrl`; canonical `/positions/{customUrl}`. **Verified live 2026-06-03.** |
| shipped    | `source-ats-sesamehr`      | `ats`      | **Sesame HR** (`sesamehr.com`, ES/LATAM HR suite).  | Spec 407, run #413. Public per-tenant careers/jobs portal feed. **Verified live 2026-06-04.** |
| shipped    | `source-ats-hrone`         | `ats`      | **HROne** (`hrone.cloud`, India HRMS with recruitment). | Spec 408, run #413. DISTINCT from `source-ats-hron` (HR-ON Recruit, Danish). Public per-tenant career site; documented-but-unverified anonymously → defensive (verified=false). |
| shipped    | `source-ats-workwise`      | `ats`      | **Workwise** (`workwise.io`, German SMB recruiting). | Spec 409, run #413. Public job widget / board surface; documented best-effort → defensive (verified=false). |
| shipped    | `source-ats-recruiteze`    | `ats`      | **Recruiteze** (`recruiteze.com`, US SMB ATS).      | Spec 410, run #413. Public per-tenant career page/board. **Verified live 2026-06-04.** |
| shipped    | `source-ats-sense`         | `ats`      | **Sense** (`sensehq.com`, US recruiting CRM / talent engagement). | Spec 411, run #413. Public hosted career-site feed. **Verified live 2026-06-04.** |
| shipped    | `source-ats-radancy`       | `ats`      | **Radancy** (`radancy.com`, enterprise TA cloud / TalentBrew; formerly TMP Worldwide). | Spec 412, run #413. Public career-site feed. **Verified live 2026-06-04.** |
| shipped    | `source-ats-beamery`       | `ats`      | **Beamery** (`beamery.com`, enterprise talent CRM / career-site platform). | Spec 413, run #413. Public career-site surface; documented best-effort → defensive (verified=false). |
| shipped    | `source-ats-symphonytalent`| `ats`      | **Symphony Talent** (`symphonytalent.com`, enterprise recruitment marketing / career sites; incorporates SmashFly). | Spec 414, run #413. Public career-site feed. **Verified live 2026-06-04.** |

## Logic-improvement candidates (existing plugins)

| Status     | Plugin id              | Improvement                                                    |
| ---------- | ---------------------- | -------------------------------------------------------------- |
| proposed   | `@ever-jobs/common`    | European salary-format edge cases (e.g. `€179.600,00`, `R$ 1.234,56`, `$179.600.00`) — extend `extractSalary()` golden set and tighten `parseCurrency()` thousand-separator handling. |
| proposed   | `source-ats-workable`  | Audit recent upstream behaviour change in pagination/rate-limit handling — diff and absorb. |
| proposed   | `source-ats-workday`   | Add a search-URL discovery helper for tenants without a known career-site URL. |
| proposed   | `source-company-apple` | Adopt cache-manager + dedicated api-client decomposition for memory ergonomics. |
| shipped    | (seed lists)           | ≥ 25 sampled per vendor (Greenhouse 53 / Lever 30 / Workable 27 / SmartRecruiters 29 — refreshed Spec 017 runs #71..#74). |

## Conventions

- New ATS adopters MUST implement `IScraper` and decorate with
  `@SourcePlugin({ category: 'ats', isAts: true })`.
- New company-direct adopters use `category: 'company'`, `isAts: false`.
- Niche / talent-marketplace adopters use `category: 'niche'` or `freelance`.
- One spec per plugin (default) — if the user prefers a single bulk spec, see
  `docs/questions.md` Q-007.

---

_Last revised: 2026-05-02 (run #268)._
