# Tasks — Spec 1548: Sodexo Canada (SmartRecruiters company-direct)

- [x] T1 — Probe `SodexoCanadaLtd` against the public SmartRecruiters Posting API (≥3 live roles). ✔ (100 roles)
- [x] T2 — Assemble the descriptor (derive className/moduleName/enumKey from the display name).
- [x] T3 — Scaffold the `source-company-sodexocanada` package (module, service, index, test, fixture).
- [x] T4 — Wire `Site.SODEXO_CANADA`, barrel import, tsconfig alias, jest mapper.
- [x] T5 — `tsc --noEmit` + mocked unit suite green.
- [ ] T6 — (Optional) authenticated live verification; flip `verified` once confirmed.
