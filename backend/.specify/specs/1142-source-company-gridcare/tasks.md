# Tasks — Spec 1142: GridCARE (Ashby company-direct)

- [x] T1 — Probe `gridcare` against the public Ashby Posting API (≥3 live roles). ✔ (8 roles)
- [x] T2 — Assemble the descriptor (derive className/moduleName/enumKey from the display name).
- [x] T3 — Scaffold the `source-company-gridcare` package (module, service, index, test, fixture).
- [x] T4 — Wire `Site.GRIDCARE`, barrel import, tsconfig alias, jest mapper.
- [x] T5 — `tsc --noEmit` + mocked unit suite green.
- [ ] T6 — (Optional) authenticate live verification; flip `verified` once confirmed.
