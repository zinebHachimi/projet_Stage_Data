# Tasks — Spec 1073: Semgrep (Ashby company-direct)

- [x] T1 — Probe `semgrep` against the public Ashby Posting API (≥3 live roles). ✔ (26 roles)
- [x] T2 — Assemble the descriptor (derive className/moduleName/enumKey from the display name).
- [x] T3 — Scaffold the `source-company-semgrep` package (module, service, index, test, fixture).
- [x] T4 — Wire `Site.SEMGREP`, barrel import, tsconfig alias, jest mapper.
- [x] T5 — `tsc --noEmit` + mocked unit suite green.
- [ ] T6 — (Optional) authenticate live verification; flip `verified` once confirmed.
