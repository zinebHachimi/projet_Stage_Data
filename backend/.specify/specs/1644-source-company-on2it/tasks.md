# Tasks — Spec 1644: ON2IT (Recruitee company-direct)

- [x] T1 — Probe `on2it` against the public Recruitee careers API (≥3 live roles). ✔ (13 roles)
- [x] T2 — Assemble the descriptor (derive className/moduleName/enumKey from the display name).
- [x] T3 — Scaffold the `source-company-on2it` package (module, service, index, test, fixture).
- [x] T4 — Wire `Site.ON2IT`, barrel import, tsconfig alias, jest mapper.
- [x] T5 — `tsc --noEmit` + mocked unit suite green.
- [ ] T6 — (Optional) authenticated live verification; flip `verified` once confirmed.
