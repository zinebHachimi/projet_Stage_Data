# Tasks — Spec 1094: Fuse Energy (Ashby company-direct)

- [x] T1 — Probe `fuse` against the public Ashby Posting API (≥3 live roles). ✔ (17 roles)
- [x] T2 — Assemble the descriptor (derive className/moduleName/enumKey from the display name).
- [x] T3 — Scaffold the `source-company-fuseenergy` package (module, service, index, test, fixture).
- [x] T4 — Wire `Site.FUSE_ENERGY`, barrel import, tsconfig alias, jest mapper.
- [x] T5 — `tsc --noEmit` + mocked unit suite green.
- [ ] T6 — (Optional) authenticate live verification; flip `verified` once confirmed.
