# Tasks — Spec 1001: Cursor (Anysphere) (Ashby company-direct)

- [x] T1 — Probe `cursor` against the public Ashby Posting API (≥3 live roles). ✔ (110 roles)
- [x] T2 — Assemble the descriptor (derive className/moduleName/enumKey from the display name).
- [x] T3 — Scaffold the `source-company-cursoranysphere` package (module, service, index, test, fixture).
- [x] T4 — Wire `Site.CURSOR_ANYSPHERE`, barrel import, tsconfig alias, jest mapper.
- [x] T5 — `tsc --noEmit` + mocked unit suite green.
- [ ] T6 — (Optional) authenticate live verification; flip `verified` once confirmed.
