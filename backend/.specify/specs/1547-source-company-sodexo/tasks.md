# Tasks — Spec 1547: Sodexo (SmartRecruiters company-direct)

- [x] T1 — Probe `Sodexo` against the public SmartRecruiters Posting API (≥3 live roles). ✔ (100 roles)
- [x] T2 — Assemble the descriptor (derive className/moduleName/enumKey from the display name).
- [x] T3 — Scaffold the `source-company-sodexo` package (module, service, index, test, fixture).
- [x] T4 — Wire `Site.SODEXO`, barrel import, tsconfig alias, jest mapper.
- [x] T5 — `tsc --noEmit` + mocked unit suite green.
- [ ] T6 — (Optional) authenticated live verification; flip `verified` once confirmed.
