# Tasks — Spec 1549: SpaceKnow (SmartRecruiters company-direct)

- [x] T1 — Probe `SpaceknowInc` against the public SmartRecruiters Posting API (≥3 live roles). ✔ (4 roles)
- [x] T2 — Assemble the descriptor (derive className/moduleName/enumKey from the display name).
- [x] T3 — Scaffold the `source-company-spaceknow` package (module, service, index, test, fixture).
- [x] T4 — Wire `Site.SPACEKNOW`, barrel import, tsconfig alias, jest mapper.
- [x] T5 — `tsc --noEmit` + mocked unit suite green.
- [ ] T6 — (Optional) authenticated live verification; flip `verified` once confirmed.
