# Tasks — Spec 1638: Monizze (Recruitee company-direct)

- [x] T1 — Probe `monizze` against the public Recruitee careers API (≥3 live roles). ✔ (8 roles)
- [x] T2 — Assemble the descriptor (derive className/moduleName/enumKey from the display name).
- [x] T3 — Scaffold the `source-company-monizze` package (module, service, index, test, fixture).
- [x] T4 — Wire `Site.MONIZZE`, barrel import, tsconfig alias, jest mapper.
- [x] T5 — `tsc --noEmit` + mocked unit suite green.
- [ ] T6 — (Optional) authenticated live verification; flip `verified` once confirmed.
