# Tasks — Spec 1301: Metabase (Lever company-direct)

- [x] T1 — Probe `metabase` against the public Lever Postings API (≥3 live roles). ✔ (18 roles)
- [x] T2 — Assemble the descriptor (derive className/moduleName/enumKey from the display name).
- [x] T3 — Scaffold the `source-company-metabase` package (module, service, index, test, fixture).
- [x] T4 — Wire `Site.METABASE`, barrel import, tsconfig alias, jest mapper.
- [x] T5 — `tsc --noEmit` + mocked unit suite green.
- [ ] T6 — (Optional) authenticated live verification; flip `verified` once confirmed.
