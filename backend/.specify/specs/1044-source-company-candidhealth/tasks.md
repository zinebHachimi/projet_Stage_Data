# Tasks — Spec 1044: Candid Health (Ashby company-direct)

- [x] T1 — Probe `candidhealth` against the public Ashby Posting API (≥3 live roles). ✔ (40 roles)
- [x] T2 — Assemble the descriptor (derive className/moduleName/enumKey from the display name).
- [x] T3 — Scaffold the `source-company-candidhealth` package (module, service, index, test, fixture).
- [x] T4 — Wire `Site.CANDID_HEALTH`, barrel import, tsconfig alias, jest mapper.
- [x] T5 — `tsc --noEmit` + mocked unit suite green.
- [ ] T6 — (Optional) authenticate live verification; flip `verified` once confirmed.
