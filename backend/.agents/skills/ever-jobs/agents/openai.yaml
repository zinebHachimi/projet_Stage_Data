interface:
  display_name: "Ever Jobs"
  short_description: "Repo-specific patterns and workflows for ever-jobs"
  default_prompt: "Use the ever-jobs repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true